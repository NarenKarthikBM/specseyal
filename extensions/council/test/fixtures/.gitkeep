# Placeholder so this directory tracks in git ahead of the tasks that fill it: T024/T025
# add the arm-4 ceiling / no-ceiling fixtures, each dropping in its own self-contained
# test/fixtures/<name>/{cmd.sh,expected.txt} directory (convention documented verbatim in
# ../run.sh's header — see that file, never this one, for the contract). Safe to delete
# once the first real fixture directory lands.
