# FAIL — feature-mismatch.yaml: feature "999-nope" does not equal the containing directory name.
schema_version: "1.0"
feature: "999-nope"

full_auto: false
council_tier: full
deck_render: none

gates:
  council:
    mode: human
    max_rounds: 1
    reopen_tier: auto
  workforce:
    mode: human
