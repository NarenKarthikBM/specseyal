# FAIL — out-of-enum.yaml: council_tier "standrad" is not in {full, standard} (SC-009 typo class).
schema_version: "1.0"
feature: "profile"

full_auto: false
council_tier: standrad
deck_render: none

gates:
  council:
    mode: human
    max_rounds: 1
    reopen_tier: auto
  workforce:
    mode: human
