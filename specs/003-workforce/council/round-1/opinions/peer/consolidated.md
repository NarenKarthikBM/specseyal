---
lens: consolidated
---

All five opinions clear the merit bar — specific, grounded in deck/plan/schema citations rather than asserted, and actionable — so this round has no "weak" opinion in the pejorative sense, only different severity ceilings. Ranked as a set: **B and E are the strongest**, distinguished by verification discipline — both went past the deck's prose and read the actual installed scripts (`on-council-approve.sh`, `verify-gate.sh`, `extensions/git/extension/extension.yml`, `install.sh`) rather than trusting the deck's paraphrase of them, and both independently ran the same `graphify explain` query and got the same number, contradicting the deck. I re-ran `graphify explain ".specify/extensions.yml"` myself: it returns **Degree: 1**, confirming B and E and contradicting the deck's "degree 15." I also independently confirmed E's most severe claim by reading `extension.yml`'s complete `hooks:` block: only `after_council_approve` performs a `record-gate` write; there is no `after_agent-assign`/`after_agent-assign-approve` equivalent for `gate: workforce` anywhere in the installed or source tree, and `verify-gate.sh` is genuinely fail-closed on an absent binding — so E's claim that `before_implement` can never pass as currently scoped is correct, not speculative, and it is the single most severe finding in the round. **A and D cluster just behind** — both land precise, mechanism-level findings (A: CPython set-ordering; D: the SC-coverage matrix), and both independently converge on the same SC-006/D48 dead-branch finding, which I confirmed directly from the deck's own text ("every base `model: sonnet`," line 158). **C is weakest on verification intensity** but lands the single biggest architectural lever in the round (the extension-merge suggestion) and I independently confirmed its precedent claim (`extensions/council/` is one extension directory holding three skills). C's own weakest suggestion (#5, frontmatter-parser dedup) is honestly self-flagged low-confidence, as is E's #6 — both are calibrated hedges, not overreach, and I read that as a sign of a disciplined round, not padding.

The round's most important structural fact only appears when reading all five together: E's two sequencing findings (missing gate-write hook; no dedicated approve command) and C's extension-merge suggestion are three faces of one gap, and no single Stage-1 member could see this since none of them read each other. See `New`, below.

## Suggestions

- [sequencing] Endorse E: `before_implement`'s `verify-gate` (`gate: workforce`) is real and correctly fail-closed (confirmed: `verify-gate.sh` blocks on any missing/absent binding, R1-S10), but nothing writes the `workforce` binding into `gates.yml`. I read `extensions/git/extension/extension.yml`'s complete `hooks:` block end to end: the only `record-gate` action registered anywhere is `after_council_approve → gate: council`; there is no workforce-gate analog, and `on-council-approve.sh`'s own header says outright the workforce equivalent is "still M3 future work." `commands.md`'s claim that the pair is "registering nothing new for gate freshness" (line 66) is true for the *read* side and silently wrong for the *write* side. As specified, `/speckit-implement-parallel` is permanently unreachable for this feature, under both human and `full_auto` signer paths (D9/W4) equally, since neither has anywhere to write the binding. This should be the round's top-priority blocking item. (confidence: high)
- [risk] Endorse B, E: both independently ran `graphify explain ".specify/extensions.yml"` and got Degree 1 against the deck's "degree 15... single highest-collision file" claim (§3). I reproduced this myself — same result, one inbound edge. The underlying shared-registry collision concern (two new hook-coupled extensions merging into one file in one round) is real and independently substantiated by both members reading the installed file directly, but the specific number is not a graph fact and should not stand in the risk register as one; re-derive or drop it, and replace it with an actual install-order statement or a lock/atomic-rename on the merge script, per B's fix. (confidence: high)
- [testability] Endorse A, D: both independently found that SC-006's D48 Sonnet-floor guard can never fail under any fixture the plan describes — I confirmed directly from the deck's own seed table text, "every base `model: sonnet`" (line 158), and Haiku is retired for v1 (D18) — so the `else: hard-error` branch is dead code with respect to every described test. Two members reaching the identical mechanism independently, from different lenses (correctness vs. testability), is strong signal this is a real gap, not a nitpick. Add the synthetic non-Sonnet fixture-only base both members recommend. (confidence: high)
- [simplicity] Endorse C: the two-extension split (`speckit-ext-categorize` + `speckit-ext-agents`) for one strictly-sequential, no-independent-use pipeline is real, undefended cost — I confirmed C's precedent check myself (`find extensions/council -maxdepth 2`: one extension directory, three skills — `speckit-council`, `speckit-council-approve`, `speckit-council-triage`), which is exactly the opposite shape from what's proposed here, and the Complexity Tracking table's single Python-vs-Bash row never examines this specific choice despite the deck naming it as complexity driver #1 (§5). This is the round's highest-leverage suggestion if adopted — see `New`, below, for why it should be triaged together with E's gate-write finding rather than separately. (confidence: high)
- [correctness] Endorse A: `agent-library-schema.md` §3's `grants = ⋃{s.grants : s ∈ injected}` has no subsequent sort step before reaching the roster. This is real: CPython string-keyed `set` iteration order depends on the process hash seed (randomized per-process since 3.3 unless `PYTHONHASHSEED` is pinned), so two `assemble.py` invocations over identical input can legitimately iterate the same logical grant-set in different orders. If that unsorted order lands in `agents/assignment.md`'s Elevated-grants column, SC-005's byte-reproducibility claim is exposed on exactly the axis the deck built its Bash-rejection argument around (§2), just for a hazard native to Python instead. High-value, cheap fix (sort before serialize; assert order, not just membership, in the golden test). (confidence: high)
- [testability] Endorse B, D: both independently identified that the golden test backing R1's determinism mitigation is zero-AI and never invokes the skill-builder session (confirmed by `quickstart.md`'s own "CI-runnable without a model" framing, per D) — it proves `assemble.py` is internally deterministic, not that R1's actual fear (session judgment leaking into ranking) is prevented. Their fixes are complementary, not competing, and the chairman should route both into one suggestion rather than picking one: B's fix (make `assemble.py` itself the tool-call writer of `agents/assignment.md`, eliminating hand-transcription structurally) closes the transcription half of R1; D's fix (a real integration test that runs the actual builder on a gap fixture) is still needed even after B's fix lands, because it's the only one of the two that would catch the session influencing *ranking* rather than *transcription*. (confidence: high)
- [testability] Endorse D: the SC-coverage framing (only SC-004/005/006 have a committed golden test; SC-001/002/003/007/008/009 + S9 rely solely on `quickstart.md`'s manual steps) is the round's most useful chairman-facing artifact — it turns a vague "is this well-tested" impression into a concrete per-SC decision the chairman can force before `tasks.md` is generated, rather than something to relitigate suggestion-by-suggestion. (confidence: high)
- [simplicity] Endorse C: `agt_backend_service`/`agt_data_persistence`'s sole justification ("the taxonomy worked-example") fails the plan's own seeding bar in the same document — plan.md's own text lists the worked-example separately from "the two real dogfood features" it uses to justify everything else, and R3's overfit-risk language doesn't name these two at all. This is a precise, load-bearing citation, not a vibe. (confidence: high)
- [risk] Challenge B: B's own fix for the self-reinstall name-collision hazard ("skip reinstall if `origin: generated`") trades one silent failure for another. B's own writeup names the case of "a future 6th seed name that collides with something the builder already generated" — under B's proposed fix, that intentional new seed base would silently fail to install with no signal at all, which is arguably worse than the overwrite B is trying to prevent, since an overwrite is at least visible in a diff and a `built`-mark check while a skipped install is not. The collision itself should hard-fail (or explicitly warn and require a rename), not resolve silently in either direction. (confidence: med)
- [risk] Challenge/qualify B and C together: C's suggestion (#4, lock `web_search` Option A as the firm default) does not resolve B's suggestion (#4, name the builder's stub-vs-assert decision rule) — the two look like alternative framings of the same open question but aren't. Locking out a live web-search grant stops the builder from claiming live data; it does nothing to stop the builder from authoring a confidently-wrong claim from potentially-stale trained knowledge, which is exactly the failure mode B is naming. The chairman should not treat adopting C's suggestion as having satisfied B's — they're additive, and B's decision-rule ask survives even under Option A. (confidence: med)
- [sequencing] New: C's extension-merge suggestion and E's two gate-mechanics findings are one design decision wearing three names, and no Stage-1 member could see this since none of them read each other. If `speckit-ext-categorize`+`speckit-ext-agents` merge into one `workforce` extension (C), that extension is the natural, obvious home for an explicit third command — an `agent-assign-approve`, mirroring council's own `speckit-council-approve` — which simultaneously answers E's #2 (there's currently no dedicated approval event separate from drafting the roster) and gives E's #1 (nothing writes the `workforce` gates.yml binding) a clean, single event to hang the missing `record-gate` hook on, exactly mirroring how `after_council_approve` already does it for council. Recommend the chairman triage C-1, E-1, and E-2 as one linked suggestion rather than three independent accept/reject calls. (confidence: high)
- [correctness] Endorse A: nothing in `skill-module.md` §6's 8 validation rules or FR-006 checks that a generated `SKILL.md`'s `taxonomy.tags` actually intersect the originating task's tags — a structurally valid skill can still leave the triggering gap open, and the deck's risk register doesn't cover this one-hop-earlier version of the same "prose-enforced judgment, no code backstop" pattern it flags as R1 for the assembler boundary. (confidence: high)
- [sequencing] Endorse E: no `before_agent-assign`-style check confirms `categorization.md` is current before `assemble.py` consumes it — the same class of prose-only sequencing risk the deck's own R1 flags for ranking purity, left unaddressed for phase order itself. Worth an explicit accept-the-risk-or-add-a-check decision, not silence. (confidence: med)
- [testability] Endorse D: SC-002's claim is a conjunction (non-zero exit *and* no `categorization.md` written), but every named verification checks only the exit code — the write-gating half depends on the calling command correctly honoring that code, which is untested orchestration prose of exactly the kind the deck itself is skeptical of elsewhere. Assert file-absence directly rather than inferring it. (confidence: med)
