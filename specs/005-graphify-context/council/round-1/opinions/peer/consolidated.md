---
lens: consolidated
---

This is a strong round as a set. All five opinions independently reproduced the deck's headline graph claims (`verify-gate.sh` degree 5/all-intra-file, `path "verify-gate.sh" "implement-parallel"` → no path, `member-prompt.md` degree 16) before doing anything else, and all five correctly treated the doubly-disclosed plumbing-layer silence (S22) as the expected condition rather than a refutation — nobody emitted a false "ungrounded" charge against `.sh`/`.py` claims the graph structurally cannot see. Confidence tagging is well-calibrated throughout: high confidence tracks textual citation + graph receipt, low/med tracks inference. I re-verified a sample of the disputed and unique claims myself (below) rather than the already-quintuple-confirmed baseline checks, which needed no sixth repetition.

**My own verification, for the record (4 graph queries, plus direct reads of `technical.md`/`plan.md`/`spec.md` in full):** `graphify explain "member-prompt.md"` reproduces the silent wrong-node resolution exactly as A/B/C/D/E describe (returns the degree-1 concept node `001_council_extension_plan_member_prompt_interface` from spec 001, zero warning). `graphify explain` on both `.specify/templates/plan-template.md` and `.specify/templates/spec-template.md` returns "No node matching" for both, confirming A's unique template-coverage claim. `graphify path "member-prompt.md" "verify-gate.sh"` — a pair no member tried — printed explicit ambiguity warnings on **both** endpoints, including a literal score tie (`top score 47337.1, runner-up 47337.1`) on the target side, which is a more extreme ambiguity than the deck's own cited example and `path` still caught it while `explain` has now failed to catch a comparable case in every observed instance this round.

**Strongest vs. weakest, as a set.** D (testability lens) is the most rigorous single opinion: every claim is pinned to an exact `plan.md`/`spec.md` locator (L66, L113, L26, L161; FR-004/FR-011/FR-012), it explicitly separates deck-reproduction from novel findings, and all five of its suggestions independently checked out against source text. The round's single strongest *finding* is the `graphify explain` silent-ambiguity bug (below) precisely because it wasn't the object of anyone's review — all five sessions hit it as a side effect of routine grounding and four formalized it. The single weakest-by-comparison item (not bad, just the most speculative) is E's parallel-wave freshness-race suggestion: a plausible analogical inference from the shared/mutable collision list, correctly self-rated only `med`, but without a textual citation naming this specific hazard the way the round's other high-confidence items have one.

**Redundancy map for the chairman** — four apparent "multiple independent findings" are each one finding under several names; corroboration is real signal (below), but don't let the letter-count multiply the underlying point count: the `explain` ambiguity bug is 1 finding (A/C/D/E formal + B prose), the arm-2 stale-survivor fixture gap is 1 finding (A/B/C/D), the ceiling-`N` tier mismatch is 1 finding (B/E), and the ceiling-hit disclosure's two-write-path fragility is 1 finding (A/C).

## Suggestions

- [risk] Endorse A(#4)/C(#1)/D(#2)/E(#2), all high-confidence: `graphify explain` silently resolves an ambiguous short filename to the wrong node with zero warning, while `graphify path` reliably warns on comparable collisions (B independently hit the identical failure in prose without escalating it to a suggestion — so 5/5 sessions encountered it, 4/5 formalized it). This is the round's strongest finding: unplanned, cross-session, reproducible, and load-bearing for D10's entire grounding methodology plus arm 4's coming query-cost accounting. Recommend triage treat this as a concrete pre-arm-1 ask (harden `explain`'s ambiguity threshold to match `path`'s, or mandate qualified-path queries in the deck-prep/council convention) rather than a deck-quality footnote. (confidence: high)
- [testability] Endorse A(#3)/B(#2)/C(#2)/D(#2): arm 2's stale-survivor guard — the direct fix for the M3 86-node incident — has zero fixture coverage on its recovery branch (survivors > 0 → prune-or-rebuild); only the 0-survivor pass case is committed. Note for the chairman: the deck's own §7e already names this gap explicitly, so the four members' real contribution is the *escalation* (B and D both argue it should block arm-2 sign-off, not stay a disclosed-and-shipped gap) rather than discovery — that escalation call is the part worth weighing, not the underlying gap-spotting. (confidence: high)
- [risk] Endorse B(#1)/E(#4), independently convergent and verified against `plan.md` L105/L111/L163: arm 4's ceiling `N` is calibrated from *this* round's per-member counts, but this round runs `standard` tier with lazy loading — structurally low query counts by design (D62/D56) — while `N` is declared tier-aware. Neither the deck nor `plan.md` states whether a separate baseline gets collected before a `full`-tier ceiling is set; deriving a `full`-tier `N` by extrapolation from a lazy-tier round risks false-positive ceiling hits on exactly the thorough eager reviews arm 4 also has to bound. (confidence: high)
- [risk] Endorse A(#6, low)/C(#5, med), same underlying architectural point reached independently — weight this above either individual rating: SC-008's "ceiling-hit is never silent" guarantee is split across a mechanically-enforced trace flag and a model-authored disclosure sentence in the opinion itself. The trace is the real guarantee; the opinion-file sentence depends on a capped member correctly free-texting it at precisely the moment (mid-ceiling-scramble) its prompt-following is least reliable. Worth the plan stating explicitly why a mechanically-templated disclosure line was passed over for free text, or the chairman auditing the trace field, not the prose, as the actual guarantee. (confidence: med)
- [correctness] Endorse A(#1), unique and unrebutted by any other member: neither `plan.md` nor the deck states whether arm 2's incremental `refresh.sh` re-invokes arm 1's `augment.sh` post-extraction pass on the changed scope. If it doesn't, every incremental refresh silently regresses arm 1's coverage win on exactly the files that changed — which would falsify SC-004's "equivalent to a full regen" claim, since a refresh-then-no-augment graph is not equivalent to a full-regen-then-augment one — and no named fixture would catch it, since §7a tests each arm independently. This is a genuine composition gap, not a restated single-arm risk. (confidence: high)
- [correctness] Endorse A(#2), unique and independently reproduced by me live: FR-001 promises `.md` prompt/template/skill nodes, but `.specify/templates/plan-template.md` and `.specify/templates/spec-template.md` both return "No node matching" today — worse than the `.sh` case, which at least gets a degree-5 node. None of FR-002's three edge kinds models a "skill/script reads template" relationship, so scaffolding-template-heavy features stay blast-radius-invisible after arm 1 ships, understating how far SC-001's "majority of tasks" promise actually reaches. (confidence: high)
- [simplicity] Endorse C(#3), verified against both deck §2 and `plan.md`'s summary paragraph: "four independently-shippable, independently-reviewable arms" is the headline, but the very next paragraph (deck §2; `plan.md` L113–117) discloses that only arm 1 has a working fallback and arms 2–4 are "core, never-detach." One caveat C didn't note: this exact phrase is not deck-invented framing — `spec.md`'s D74 constraint ("Severability, not a split") *requires* the plan to say "four independently shippable concerns." So this is inherited, spec-mandated language the deck had no real choice but to carry forward, not deck salesmanship — the ask to lead with the 1-vs-3 asymmetry still stands, but it's a spec-level phrasing issue, not a deck-integrity one. (confidence: med)
- [testability] Endorse D(#3), verified directly against `spec.md`: SC-004's "materially lower cost" and SC-006's "spend... lower than an equivalent unbounded loop" both carry zero numeric threshold in the spec text — only qualitative comparatives. Both are structurally unfalsifiable as committed tests; SC-006 at least has a real comparator already banked (this round's own uncapped baseline, once it exists), SC-004 has no anchor at all. (confidence: high)
- [sequencing] Endorse E(#1), verified against `plan.md` L75–76: "hard-warns and routes to regeneration" never states whether the common stale case invokes cheap `refresh.sh` or the full ~753k-token ritual, and "prune-or-rebuild" never states what decides between the two. This compounds directly with the stale-survivor fixture gap above — the recovery branch is simultaneously unspecified *and* untested, which is worse than either alone. Recommend the plan add the explicit state-transition line E proposes before `tasks.md` generation. (confidence: high)
- [testability] Endorse D(#5): none of the ten named fixtures (four per-arm + six consumer) runs in a detached configuration, so D74-2's severability claim — arms 2+3+4 degrade gracefully with arm 1 absent — is presently rationale-only, the same prose-vs-mechanism gap the deck itself calls out elsewhere (§2, the sectioned-file rejection). Since detach is a real, named triage option rather than a hypothetical, this is worth a scoped fixture or an explicit "out of scope for this round" line rather than silence. (confidence: med)
- [testability] Endorse D(#4): "deterministic parsing (no LLM)" (plan.md L26) is necessary but not sufficient for the "byte-deterministic... fixture-checkable" claim (§3/§7a) — byte-for-byte golden diffing also requires canonical serialization (stable node/edge ordering, fixed JSON key order, no filesystem-iteration dependence), which neither the deck nor the plan commits to. This is a specific, classic flaky-golden-test source that nobody else in the round named. (confidence: med)
- [risk] New: the deck's own §7e names exactly four branch-coverage gaps with equal explicitness — arm-2 stale-survivor (SC-004), arm-2 false-positive-stale (SC-003), arm-1 fallback-labeling (SC-002), arm-4 non-disclosure-default (SC-008). The round's adversarial attention landed unevenly on these: stale-survivor and fallback-labeling got piled on by three-plus members each, while the other two named gaps — an unmutated worktree correctly reporting fresh with no false warning, and an ordinary (non-ceiling-hit) round correctly carrying no disclosure line — got zero mentions across all five stage-1 opinions. Both are lower-stakes than the two that got attention (the deck itself calls the disclosure one "softer"), but the false-positive-stale branch guards against the opposite failure mode from SC-003's tested case (crying wolf vs. missing a real staleness) and deserves at least a line, not silent omission from every review. (confidence: med)
- [sequencing] Challenge B(#3): "nothing blocks a subsequent council round from convening before arm 4 ships" conflates two different scenarios. A future feature's council round predating 005's own completion is an unavoidable property of sequencing — no feature can retroactively protect rounds that ran before it shipped, and 005's own round (this one) is necessarily one of them by design (it's arm 4's calibration baseline). The sharper, genuinely actionable version of B's concern is a mid-implementation `--reopen delta` on 005 *itself*, after arm 4 is spec'd but before it's actually wired into the orchestrator — that scenario could plausibly still dispatch the pre-ceiling member-prompt and would be worth a guard. Recommend narrowing to that case rather than the inherently-unfixable general one. (confidence: med)
