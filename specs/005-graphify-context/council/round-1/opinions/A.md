---
lens: correctness
---

**Overall**: the chosen approach holds up. The four problems in §1 are each backed by a specific, citable telemetry incident (004's degree-4 `verify-gate.sh`, M3's 86 stale survivors, D62's 3/5→1/5 pull-drop, the 002 25–38-call/5.25M-token round), the four rejected alternatives are rejected on named, specific grounds (D75/D29, the FR-002 clarification, the D53 prose-vs-mechanism lesson, the FR-011 clarification) rather than strawmanned, and the severability/detach-order argument (arm 1 detaches first because it alone has a *designed* fallback — FR-004's labeled-assertion path — versus arms 2–4 reverting to a *known-buggy* status quo, not a graceful one) survives pressure-testing. This is also an unusually self-aware deck: §4's "blind spot, stated plainly" and §7's SC tally already volunteer most of their own weakest points before a reviewer has to find them. My job below is mostly to push past where that self-disclosure stops, plus verify the graph claims it does make rather than take them on faith (D10).

**Graph verification.** I reproduced the deck's flagship claims live against `graphify-out/graph.json` and they check out exactly: `explain "verify-gate.sh"` → degree 5, all five edges intra-file (`contains` ×1 + `defines` ×4: die/mark_stale/usage/count_lines), zero cross-file — matches §4 verbatim. `path "verify-gate.sh" "implement-parallel"` → "No path found" — matches. `explain` on the arm-4 home file (once resolved to its correct node, see Suggestion 3 below) → degree 16 with exactly the connections §4 lists (`member_lenses`, `plan.md artifact`, `reduced grounding note (FR-019)`, etc.). The arm-3 home (`speckit-graphify-context` SKILL.md) → degree 2, referenced by tasks-graph + implement-parallel, also exact. `graph-baseline-measure.md`'s node-type breakdown (277 rationale / 541 concept) matches §4's count precisely, and — more importantly — it shows the "0 survivors" figure the deck cites as "the guard proven by hand at step-0" came from a genuine `build_merge` incremental merge (76-file `detect_incremental` degeneration, folding 446 new nodes into the 1343-node M4 base), not a vacuous full-regen-with-no-merge measurement. I went in doubting that number specifically and it held up.

**Risk register.** R1/R5 (the self-referential blind spot) are honestly priced as "disclosed, not solved" rather than dressed up as mitigated, which is the right call given S22 — my confidence on every `.sh`/`.py` plumbing claim in this deck is correspondingly reduced exactly where the deck says it should be, and I'm not treating that as a defect. R2/R4 are standard, adequately mitigated. R3 is worth one flag not in the register: "the ceiling is generous relative to the median member" is asserted about a number (`N`) that doesn't exist yet — it will be set *from this round's own uncapped data*, so R3's "Low" likelihood is really "Low, contingent on a calibration that happens after this review closes," not a currently-checkable fact. Minor, not suggestion-worthy on its own.

The six items below are where I think the deck under-covers something load-bearing.

## Suggestions
- [correctness] Arm 2's `refresh.sh` "wraps the upstream `build_merge`" (§3) but neither the deck nor `plan.md` (grepped for `augment.sh`/`refresh.sh`/`freshness.sh` — no composition language found) states whether an incremental refresh also re-invokes arm 1's `augment.sh` post-extraction pass. If it doesn't, every incremental refresh silently regresses arm 1's coverage win on the changed scope, which would falsify SC-004's "equivalence to a full regen" claim (a refresh-then-no-augment graph is *not* equivalent to a full-regen-then-augment one) — and since §7a's per-arm fixtures test arm 1 and arm 2 independently, no named fixture would catch this composition gap. This is a plumbing-layer claim the graph can't adjudicate (arm 2's own script is graph-invisible per S22), so my confidence rests on the absence of stated composition, not a graph fact — worth an explicit line in the plan either way. (confidence: high)
- [correctness] FR-001 promises nodes for ".md prompt/template/skill files," but the two canonical scaffolding templates I checked live — `.specify/templates/plan-template.md`, `.specify/templates/spec-template.md` — return "No node matching" today, a *worse* state than the `.sh` case (which at least gets a degree-5 node). None of FR-002's three edge kinds (hook-registration, install-copy, script→script) models a "skill/script reads template" relationship, so these files likely gain isolated nodes but stay blast-radius-invisible after arm 1 ships — undermining FR-003/SC-001's "majority of tasks" promise for any feature whose plumbing is template-heavy. The deck's own §4 "graph-grounded" `.md` example (member-prompt.md, degree 16) is actually the opposite case — an already richly-connected rationale doc, not a template — which risks implying the `.md` side of coverage is closer to solved than the template case shows it is. (confidence: high)
- [testability] §7e already names the gap, but a gap named isn't a gap closed: SC-002's fallback-labeling branch and SC-004's survivor>0 recovery branch have zero committed negative-path fixtures — every per-arm fixture in §7a tests only the happy path (known topology modeled correctly / 0 survivors). A guard that has only ever seen passing input is unverified in the one branch that actually matters (does the labeled-fallback fire honestly instead of silently omitting; does prune-or-rebuild actually engage and recover). Recommend this be a blocking ask at triage, not a disclosed-and-shipped gap, given SC-002 is literally the honesty guarantee the rest of the coverage story depends on. (confidence: high)
- [correctness] The `explain`/`path` fuzzy-matcher is less reliable than the deck's clean citations imply. Live this round: `graphify explain "member-prompt.md"` (the deck's own filename) silently returned an unrelated degree-1 concept node from `specs/001-council-extension/plan.md` — no ambiguity warning at all; the real degree-16 file only surfaced under its exact node label. Separately, reproducing the deck's own headline example, `path "verify-gate.sh" "implement-parallel"`, triggered explicit "source match was ambiguous" / "target match was ambiguous" CLI warnings that §4's "verified live" citation doesn't mention. Arm 1 is about to add many more same-shaped `.sh`/`.yml`/`.md` nodes across extension directories, growing exactly this collision surface right as SC-001's stated exit test leans on this query pattern. (confidence: med)
- [correctness] SC-006 ("round spend lower than an equivalent unbounded loop") is explicitly measurement-only this round, since the ceiling it would compare against doesn't exist yet (§7c). No concrete trigger or owner is named for when that comparison actually happens once arm 4 ships — without one, SC-006 risks staying permanently deferred rather than ever being verified true. Suggest naming the specific follow-up event (e.g., the first standard-tier round after arm 4 ships records the before/after in its own deck) as a plan commitment. (confidence: med)
- [correctness] Arm 4's "ceiling-hit is never silent" guarantee (§3) leans on two channels of unequal strength: the trace's `ceiling_hit` flag is mechanically enforced, but the opinion file's disclosure sentence is authored by the live model session itself — the same "unenforced prose" shape the deck invokes elsewhere (§2) to justify structural guarantees over prose ones. SC-008's fixture shows the mechanism can work once in a scripted scenario, but per the plan's own mechanical-script-vs-live-model-session distinction (§3 preamble), a fixture pass doesn't guarantee every future round's member reliably remembers to self-disclose the way a mechanical script would. Worth a note that the trace, not the opinion sentence, is the load-bearing guarantee a chairman should actually audit. (confidence: low)
