---
lens: risk
---

The deck is unusually self-auditing — §4's blind-spot admission and §7's SC-002/SC-006 no-fixture calls and four named branch-coverage gaps (§7e) already disclose more than most decks volunteer. That transparency is a point in the plan's favor, and I've tried not to just restate what it already owns.

I spot-checked the graph-native claims that should be checkable rather than trusting "verified live" at face value: `explain "verify-gate.sh"` → degree 5, all five edges intra-file (`contains`/`defines`×4), confirmed; `path "verify-gate.sh" "implement-parallel"` → no path found, confirmed; the `speckit-graphify-context` SKILL.md node → degree 2, referenced by tasks-graph + implement-parallel, confirmed; and `extensions/council/extension/templates/member-prompt.md` → degree 16 with exactly the three cited edges (`member_lenses`, `plan.md`, the reduced-grounding note), confirmed — though this last one took a retry: my first query on the literal filename resolved ambiguously to an unrelated concept node sourced from `specs/001-council-extension/plan.md`, not the actual document node, which is itself a small data point on how easy it is to mis-cite this graph by name alone. All four deck claims held. That's the concept/rationale layer, and it grounds exactly as advertised.

Per the doubly-reduced-grounding condition §4 discloses: every claim about arm-1/arm-2's own plumbing (`augment.sh`, `freshness.sh`, `refresh.sh` reach) is necessarily engineer assertion, not graph fact, because those files are exactly the coverage gap under repair. I'm treating graph silence on them as neither confirmation nor refutation, and my confidence on that layer is lower than on the spec/rationale layer for that structural reason — not because the plan is thin there.

Five risk-relevant gaps, roughly in descending order of how much each would move my sign-off:

## Suggestions
- [risk] Arm 4's ceiling `N` is calibrated from this round's own query counts (plan.md L111, L163), but the ceiling is declared tier-aware (L105) — and R3's generosity argument itself leans on "the 25–38 range" (deck §1, item 4), which is `002`'s historical count, not this round's. This round runs `standard` tier with lazy loading, which structurally suppresses queries (that's the entire point of D62's 3/5→1/5 cut) — its actual counts will likely land well under 25–38. Neither plan.md nor the deck states that a separate baseline gets collected for `full`-tier (eager) rounds before their `N` is set. If full-tier `N` is derived from this round's low lazy-tier counts, the thorough eager reviews arm 4 also has to bound are the ones most likely to false-positive into a ceiling-hit. (confidence: high)
- [testability] Arm 2's stale-survivor guard is the direct fix for a real incident (M3, 86 stale duplicate nodes) — and the deck's own §7e says its recovery branch (survivors > 0 → prune-or-rebuild) has zero fixture coverage; only the 0-survivor pass case is committed (§7a fixture (b)). I'd escalate this past "noted gap": it's the single highest-value invariant in the feature, guarding the one failure mode with a documented real-world occurrence, and it's the one piece proven only on the input that was never the problem. I'd treat a synthetic survivors>0 fixture as a precondition for arm-2 sign-off, not a nice-to-have. (confidence: high)
- [sequencing] "The last round that will ever run without a ceiling" (plan.md L111) is an owner directive, not a structural guarantee. The detach order (D74-2, plan.md L113–117) governs only which arm to cut under pressure, not the order arms 2/3/4 get built in — tasks.md doesn't exist yet (deck §6). Nothing blocks a subsequent council round (a `--reopen delta` on 005, or 006's own plan-defense) from convening before arm 4 ships, which would silently run uncapped again with no gate checking "has arm 4 landed" before dispatching the pre-ceiling member-prompt. (confidence: med)
- [risk] Arm 1's fixture (§7a) proves byte-determinism on a "known" topology only — nothing in the testability section exercises messy real `.sh` patterns (a commented-out `source`, a conditionally-executed `cp`, an indirect call via `"$VAR/script.sh"`) that a pattern-based extractor could turn into a silently *wrong* edge rather than a missing one. FR-004/S22's labeled-assertion fallback (and the deck's own SC-002 admission, §7c/7e) cover "couldn't model it → label it"; neither covers "modeled it, but got it wrong." A false-positive edge presented as fact is a regression from today's honest file-disjointness fallback, not a neutral miss — and it's the one failure mode arm 1's own guarantee language ("byte-deterministic... fixture-checkable") doesn't visibly address. (confidence: med)
- [risk] R4's mitigation for `graphifyy` drift — "the wrapper pins to the CLI/API surface it uses... `graphifyy` is a versioned dependency" (plan.md L164) — reads as code-usage discipline (only calling stable-looking functions), not an actual version constraint; neither the deck nor plan.md cites a lockfile/manifest pin. For a risk rated Medium–High impact, the stated mitigation is reactive (survival fixtures catch a break when they're run) rather than preventive (nothing stops a contract-breaking upstream release from reaching the pipeline between test runs). Worth confirming an actual pin exists before treating R4 as adequately mitigated. (confidence: med)
