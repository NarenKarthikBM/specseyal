# Suggestions — Round 1

**Feature**: `005-graphify-context`
**Prepared by**: Session B — chairman (Opus, xhigh, D18)
**Reads**: `round-1/opinions/*.md`, `round-1/opinions/peer/*.md` (chairman-only inputs; never quoted verbatim below)

> Format: markdown v1 (D15). This is the **compression boundary** (`data-model.md`) — the ONLY file under `council/` the main thread reads (FR-007, principle 1), and the sole input to `/speckit-council-triage`. Round-scoped: written once as `round-1/suggestions.md` and **never overwritten**.

*Chairman's stage-3 synthesis of stage 1 (five independent opinions A–E) and stage 2 (one consolidated, anonymized peer critique — `standard` tier, D56). Consolidated, classified, ID'd. Opinion prose is never forwarded — `opinions/` stays chairman-only for the life of the feature (FR-006/FR-007/SC-005).*

---

**Verdict:** 3 blocking · 11 strong · 6 consider

---

## Suggestions

| ID | Class | Suggestion | Sources | Target |
|----|-------|-----------|---------|--------|
| R1-S01 | blocking | Commit a negative-path fixture that manufactures stale survivors (>0) and asserts arm-2's guard detects them and actually prune-or-rebuilds — not merely that an ordinary refresh reports 0. The guard fixes the documented M3 86-stale-node incident yet is exercised only on passing input; multiple members ask that this fixture be a precondition for arm-2 sign-off, not a disclosed-and-shipped gap. | A, B, C, D | plan.md §Arm 2 (Fixture) |
| R1-S02 | blocking | Specify arm-2's stale/recovery control flow before tasks.md: state which concrete operation "routes to regeneration" invokes (cheap incremental `refresh.sh` vs. the ~753k-token full ritual) and what decides prune vs. rebuild on survivors>0. SC-004's "materially lower cost" holds only if the common stale path runs cheap `refresh.sh`; the recovery branch is currently both unspecified and untested (compounds S01). | E | plan.md §Arm 2 (Shape) |
| R1-S03 | blocking | Commit a fixture that feeds arm-1's augmentation pass a relationship kind outside the three modeled edge kinds and asserts the labeled-assertion fallback appears (not silence). SC-002's "zero unlabeled fallback claims" is the feature's honesty guarantee, yet arm-1's only fixture exercises the successful-modeling branch — the fallback branch that delivers the guarantee has no coverage. | A, D | plan.md §Arm 1 (Fixture) |
| R1-S04 | strong | Harden `graphify explain` to warn on ambiguous short-name matches the way `graphify path` already does, or adopt a qualified-path/label query convention for deck-prep and council members, and note it in the plan. Round's strongest find (5/5 members hit it live): `explain` silently resolved a deck-cited filename to the wrong node with no warning; arm 1 grows this same-named-node collision surface and arm-4's ceiling-limited members burn scarce query budget chasing wrong resolutions. Peer flags this as a pre-arm-1 ask, not a footnote. | A, B, C, E | plan.md §Arm 1 (contract surface) / §Risks R1 |
| R1-S05 | strong | State how the full-tier query ceiling `N` is calibrated: this baseline round runs standard tier (lazy loading structurally suppresses query counts, D62), yet `N` is declared tier-aware and R3's generosity leans on the 25–38 range (002's history, not this round). Deriving a full-tier `N` from this lazy round's low counts risks false-positive ceiling-hits on exactly the thorough eager reviews arm 4 must also bound; say whether a separate full-tier baseline is collected. | B, E | plan.md §Arm 4 (Baseline) / §Risks R3 |
| R1-S06 | strong | State whether arm-2's incremental refresh re-invokes arm-1's augment pass on the changed scope. If it doesn't, every incremental refresh silently regresses arm-1's coverage on exactly the changed files and falsifies SC-004's "equivalent to a full regen" — and no per-arm fixture catches it, since fixtures test each arm in isolation. Add a cross-arm composition fixture or state the invariant. | A | plan.md §Arm 2 (Shape) |
| R1-S07 | strong | Give SC-004's "materially lower cost" a numeric threshold in the plan and commit SC-006 to a concrete, owned follow-up comparison (first post-arm-4 round vs. this round's uncapped baseline). Both success criteria are qualitative-only today, so neither can go red — an SC with no number can only be eyeballed, and SC-006 risks staying permanently deferred with no named trigger. | A, D | plan.md §Performance Goals / §Arm 4 (Baseline) |
| R1-S08 | strong | Scope arm-1's `.md` coverage honestly: the canonical scaffolding templates (`plan-template.md`, `spec-template.md`) have no graph node today — worse than the `.sh` case — and none of FR-002's three edge kinds models a "skill/script reads template" relationship, so template-heavy plumbing stays blast-radius-invisible after arm 1 ships. Either add a template-read edge kind or state that FR-003/SC-001's "majority of tasks" reach excludes template-heavy features. | A | plan.md §Arm 1 (Shape) |
| R1-S09 | strong | Make explicit that the trace `ceiling_hit` flag, not the member's free-texted opinion sentence, is the load-bearing guarantee behind SC-008's "ceiling-hit is never silent" — or have the orchestrator mechanically append the disclosure line the instant it enforces the cap. A member scrambling at its ceiling is when prompt-following is least reliable, so splitting a "never silent" guarantee across a mechanical flag and model prose is a self-imposed failure path (peer weighted this above its individual low/med ratings). | A, C | plan.md §Arm 4 (Shape) |
| R1-S10 | strong | Address the "modeled but wrong" failure mode: arm-1's pattern-based extractor could turn a commented-out `source`, a conditional `cp`, or an indirect `"$VAR/script.sh"` call into a silently incorrect edge presented as fact — a regression from today's honest file-disjointness fallback, which FR-004/S22's labeled-assertion path ("couldn't model it") does not cover. Exercise messy real `.sh` patterns in the arm-1 golden, not just a clean known topology. | B | plan.md §Arm 1 (Shape/Fixture) |
| R1-S11 | strong | Pin the serialization discipline behind arm-1's "byte-deterministic" claim before `augment.sh` is written — stable node/edge ordering, canonical JSON key order, no filesystem-iteration-order dependence. "No LLM" (§Testing) is necessary but not sufficient for byte-for-byte golden diffing and is a classic flaky-golden source. | D | plan.md §Arm 1 (Fixture) / §Technical Context (Testing) |
| R1-S12 | strong | Add a detached-configuration fixture proving D74-2's severability claim — that arms 2+3+4 function correctly with arm 1 absent, per the fallback story — or explicitly scope it out with a line. All ten named fixtures assume every shipped arm is present, so severability is presently rationale-only; since detach is a live triage option, the plan's own prose-vs-mechanism concern applies to its own severability claim. | D | plan.md §Detach order / §Non-regression fixtures |
| R1-S13 | strong | Either name an explicit cross-product coherence fixture for arm 3 (assert all three diets carry matching provenance — same graph hash/generation id — from one generator run) or state honestly that R2's "shared-provenance header" mitigation is process discipline. As written, the arm-3 fixture checks only a golden per product (each correct in isolation) — the same "unenforced prose" shape the plan rejected the sectioned-file alternative for (D53). | C | plan.md §Arm 3 (Fixture) / §Risks R2 |
| R1-S14 | strong | State the intra-cycle execution order across arms 1→2→3 (augment → freshness/refresh → generate) in the plan or tasks.md. Arm-3's generator "reads the current augmented, fresh graph," which presumes arms 1 and 2 already ran this cycle, and consumer fixture 4 needs arm-3's product to exist to be exercisable — build/test order and severability are being treated as one question but are distinct. | E | plan.md §The Four Severable Arms (execution order) |
| R1-S15 | consider | Lead the severability framing with the 1-vs-3 asymmetry: only arm 1 has a working fallback and is genuinely detachable; arms 2–4 are "core, never-detach" with no fallback, so "four independently-shippable arms" reads stronger than the next paragraph delivers. Peer caveat: this exact phrasing is spec-mandated by D74's "four independently shippable concerns," so it's an inherited spec-level phrasing issue, not deck salesmanship — the underlying detach reasoning is sound. | C | plan.md §Summary / §Detach order |
| R1-S16 | consider | Confirm an actual `graphifyy` version pin (lockfile/manifest) exists before treating R4 as adequately mitigated. R4's stated mitigation — "the wrapper pins to the CLI/API surface it uses; `graphifyy` is a versioned dependency" — reads as code-usage discipline, not a real constraint, and is reactive (fixtures catch a break when run) rather than preventive for a Medium–High-impact risk. | B | plan.md §Risks (R4) |
| R1-S17 | consider | Consider a guard against a mid-implementation `--reopen delta` on 005 dispatching the pre-ceiling member-prompt after arm 4 is spec'd but before it is wired. Raised as "nothing blocks a subsequent uncapped round"; peer contested and narrowed it — a future feature's round predating 005's completion is unavoidable and this round is arm-4's baseline by design, so only the mid-implementation 005 self-reopen case is actionable. | B | plan.md §Arm 4 (Baseline) |
| R1-S18 | consider | Give at least a line to the two deck-disclosed branch-coverage gaps that drew zero member attention: the false-positive-stale branch (SC-003 — an unmutated worktree must report fresh with no false warning, the crying-wolf inverse of the tested case) and the non-disclosure-default branch (SC-008 — an ordinary non-ceiling round must carry no disclosure line). Both are lower-stakes than S01/S03 but shouldn't be silently omitted from coverage. | peer | plan.md §Arm 2 (Fixture) / §Arm 4 (Fixture) |
| R1-S19 | consider | Either argue why parallel-wave hooks can't hit a freshness check-then-use race or add a fixture that tries: a `freshness.sh` check passing for one wave-task's `before_implement` hook could be invalidated by a sibling task's concurrent edit before the first task reads `graphify-context.md` — same failure family as the mtime lag, concurrent rather than sequential, and the plan's own shared/mutable collision list establishes concurrency as live. Peer notes this is the round's most speculative item (self-rated med, no textual citation). | E | plan.md §Arm 2 / §Risks |
| R1-S20 | consider | Confirm and state that `freshness.sh`'s report is recomputed at every consumption point, not cached/reused across hook calls within one session. The "no state file / derived property" framing (D32) likely already ensures this, but the invocation contract isn't stated explicitly — a consumer trusting a report computed earlier in the same session would recreate, one layer up, the very stale-artifact-trusted-without-recheck failure this arm exists to close. | C | plan.md §Arm 2 (Shape) |

---

## Chairman's note

All five members independently reproduced the deck's headline graph claims and correctly treated the plan's *doubly-disclosed* plumbing-layer blindness — the graph cannot see the `.sh`/`.yml`/`.py` files this feature fixes, so those claims are labeled engineer assertion (S22) — as the plan's own healthy, disclosed condition, not a grounding failure; this is a well-argued, unusually self-auditing plan and the round's strongest find is a cross-session graphify tooling footgun (`explain` silently resolves ambiguous short names to the wrong node while `path` warns — S04), not a plan defect. The blocking core is narrow and coherent: arm-2's stale/recovery branch is both unspecified and untested (S01–S02) and arm-1's SC-002 honesty-guarantee fallback branch has no fixture (S03) — each a guard for a central success criterion exercised only on passing input, all resolvable in one revision. Members converged with no substantive splits; the sole peer contest narrowed one consider-level edge case (S17).

### Chairman delta check — 2026-07-13T18:09:18Z

- R1-S01 — RESOLVED — Arm 2's fixture now carries case **(d)**: a negative-path fixture that manufactures > 0 stale survivors (the M3 86-node incident in miniature) and asserts the guard both **detects** them and actually performs the **prune-or-rebuild** recovery, made an explicit precondition for arm-2 sign-off rather than a disclosed-and-shipped gap.
- R1-S02 — RESOLVED — Arm 2's Shape now specifies a concrete stale/recovery branch table: the common stale path runs the cheap `refresh.sh` incremental **always** (the path SC-004's "materially lower cost" binds to), survivors > 0 routes to **prune + targeted re-extract of the affected scope only**, and the ~753k-token full-corpus regen fires **only** on an extractor-version change or explicit operator demand — the branch is no longer unspecified.
- R1-S03 — RESOLVED — Arm 1's fixture now has branch **(2)**: a fallback-branch fixture feeding the augmentation pass a relationship kind *outside* the three modeled edge kinds and asserting the **labeled-assertion fallback appears** (silence named as the failure mode), so SC-002's "zero unlabeled fallback claims" honesty guarantee is exercised, not just the success branch.

**Delta verdict:** all clear, ready for the gate
