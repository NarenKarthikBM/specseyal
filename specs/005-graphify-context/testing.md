---
feature: 005-graphify-context
phase: testing
executed: none
---

## Coverage map

| id | approach | grounding | evidence-source | status |
|---|---|---|---|---|
| FR-001 | Contract-validate the augment pass's node-emission goldens (`.sh`/`.yml`/`.md`) in the graphify test harness; inspect a post-augment graph and confirm `explain` resolves a `.sh`/`.yml`/`.md` file to a real node instead of "No node matching". | Completed §Arm1: "augment.sh + augment_merge.py (post-extraction pass: .sh/.yml/.md nodes ... 3 byte-goldens green)"; Integration status: "+476 nodes/+64 edges" measured on the real repo graph. | report-claimed | covered |
| FR-002 | Contract-validate the three edge-kind byte-goldens (`registers_hook`/`installs`/`invokes`) in the graphify test harness; inspect the post-augment graph for at least one instance of each kind (e.g., the cited `registers_hook: extensions.yml → speckit.git.verify-gate`). | Completed §Arm1 edge-kind list; Partial/Degraded #1: "the three ratified edge kinds create extensions.yml → command"; Integration status: "+64 edges, incl. registers_hook: extensions.yml → speckit.git.verify-gate". | report-claimed | covered |
| FR-003 | — | — | — | **GAP** |
| FR-004 | Contract-validate the labeled-fallback (contract-of-record) fixture(s) in the graphify test harness; inspect a fallback-covered claim in a generated context product for the literal label "convention-derived / engineer assertion, not graph fact". | Partial/Degraded #1: "The three modeled edge kinds and the labeled fallback (contract-of-record fixtures) all pass; the gap is the specific SC-001 path." | report-claimed | covered |
| FR-005 | Contract-validate `freshness.sh`'s two-check staleness fixture(s) in the graphify test harness; manually verify by touching a working-tree file after generating a context product and confirming the check reports stale rather than silently passing, then routes to regeneration (not a hard-block). | Completed §Arm2: "freshness.sh (two-check staleness, calls the shared provenance.sh, no state file)"; Integration status: 28/0 harness pass including the arm-2 fixtures. | report-claimed | covered |
| FR-006 | Contract-validate the arm-2 regenerate-vs-rewrite fixture(s): confirm a stale product is replaced wholesale by the `refresh.sh`/full-regen path, never hand-patched in place. | Completed §Arm2: "Clean-replace-vs-conservative-upsert model satisfies all four arm-2 fixtures." | report-claimed | covered |
| FR-007 | Contract-validate the four committed arm-2 fixtures (incremental merge, stale-survivor guard/prune recovery, the S06 augment re-invoke) covering both named extractor-drift fixes; confirm zero stale survivors and correct partial-vs-full refresh behavior for a bounded change set. | Completed §Arm2: "refresh.sh + refresh_merge.py (incremental merge + stale-survivor guard + prune recovery + the S06 augment re-invoke; version-pin → full-regen fallback)"; "all four arm-2 fixtures" satisfied. | report-claimed | covered |
| FR-008 | Contract-validate the version-pin → full-regen fallback fixture: force a version mismatch / unmet incremental invariant and confirm `refresh.sh` falls back to full regeneration rather than shipping an inequivalent graph. | Completed §Arm2: "version-pin → full-regen fallback"; §Arm1: `graphify-version.pin`. | report-claimed | covered |
| FR-009 | Contract-validate the arm-3 tiered-generator fixture: run the generator once and confirm three distinct products (blast-radius/shared-mutable/`[P]`; concept/rationale receipts; per-file type-signal) are emitted from the single pass. | Completed §Arm3: "the generator emits three token-bounded diets from one pass, kept mutually coherent by a single provenance.sh header stamp; deck-prep + categorizer lockstepped onto the receipts / type-signal diets." | report-claimed | covered |
| FR-010 | Inspect each of the three generated products for the shared token-bound header (`provenance.sh` stamp) and confirm none re-serves, or is priced against, another consumer's full slice. | Completed §Arm3: "three token-bounded diets from one pass, kept mutually coherent by a single provenance.sh header stamp." | report-claimed | covered |
| FR-011 | Contract-validate the arm-4 ceiling fixtures (`arm4-ceiling`/`arm4-noceiling`) in the council test harness; inspect `council-config.yml` for the tier-aware `query_ceiling` values (`standard: 15`, `full: null`) and confirm `ceiling-check.sh` enforces, not merely targets, the cap. | Completed §Arm4: "ceiling-check.sh, council-config.yml tier-aware query_ceiling (standard: 15, full: null)"; Integration status: "extensions/council/test/run.sh → 9 passed / 0 failed (arm4-ceiling/noceiling + §3 reinstall)." | report-claimed | covered |
| FR-012 | Run the `arm4-ceiling` fixture and confirm the trace carries the `graph_queries`/`ceiling_hit` fields (`trace-schema.md` amendment) and that a ceiling-hit run's member output carries the mechanical disclosure append. | Completed §Arm4: "the trace-schema.md graph_queries/ceiling_hit amendment, and the member-prompt + orchestrator enforcement (mechanical disclosure append)"; Integration status: 9/0 arm4 fixture pass. | report-claimed | covered |
| FR-013 | Run both named test harnesses end to end and confirm all 6 named consumer non-regression fixtures pass; inspect the shared-file integration note for the three-way-touched files to confirm wave-serialized lockstep (no co-scheduling regression). | Cross-cutting Completed: "6 named consumer non-regression fixtures (FR-013 tripwires)"; Integration status: both harnesses fully green (28/0, 9/0) plus the "Shared-file integration" paragraph. | report-claimed | covered |
| FR-014 | Run the §3 reinstall-survival stage in both harnesses; manually verify the installed `provenance.sh`/`ceiling-check.sh` are functional in the installed (`.specify/`) layout after `install.sh`'s rm-rf+cp and a reinstall. | Cross-cutting Completed: "the §3 reinstall-survival stage in both harnesses (D57)"; Integration status: "Reinstall-survival (D57): all 005 source edits survive install.sh's rm -rf+cp and a reinstall; the installed provenance.sh and ceiling-check.sh were exercised functional in the installed layout." | report-claimed | covered |
| SC-001 | — | — | — | **GAP** |
| SC-002 | Contract-validate the labeled-fallback (contract-of-record) fixtures as the zero-unlabeled-claim guarantee; inspect a generated product's fallback-covered entries for the literal "assertion, not graph fact" label. | Partial/Degraded #1: "The three modeled edge kinds and the labeled fallback (contract-of-record fixtures) all pass"; Key results: "The one headline GAP (SC-001) was surfaced honestly by the integration gate, not fabricated over." | report-claimed | covered |
| SC-003 | Manually verify: generate a context product, touch a plan file it should ground, and confirm `freshness.sh` flags it stale and routes to regeneration (hard-warn, not a hard-block) before any consumer reads it — per the committed arm-2 fixture. | Completed §Arm2: "freshness.sh (two-check staleness ...)"; "all four arm-2 fixtures" satisfied. | report-claimed | covered |
| SC-004 | Contract-validate the incremental-equivalence mechanism now (zero stale survivors, all new nodes present, via the committed arm-2 fixtures); the materially-lower-cost *number* vs. the ~1M-token full-corpus regen is a deferred live measurement against a real pipeline round. | Completed §Arm2 (stale-survivor guard, prune recovery, all four arm-2 fixtures); Partial/Degraded #2: "SC-004 token-cost budget" listed among "Deferred live measurements (mechanism verified by a committed fixture; the number awaits a real pipeline round)". | report-claimed | covered |
| SC-005 | Contract-validate the arm-3 tiered-diet mechanism now (three distinct per-consumer products from one graph pass); the member on-demand-pull-reduction *number* relative to the one-diet (D62) baseline is a deferred live measurement. | Completed §Arm3 (three token-bounded diets; deck-prep + categorizer lockstepped); Partial/Degraded #2: "SC-005 member pull-reduction" listed among the deferred live measurements. | report-claimed | covered |
| SC-006 | Contract-validate the arm-4 ceiling/telemetry mechanism now (bounded + trace-visible, via the 9/0 arm4-ceiling/noceiling fixtures); the actual spend-vs-unbounded-loop comparison at a standard-tier round is a deferred live measurement, explicitly booked against `006`'s council round. | Completed §Arm4; Integration status 9/0 arm4-ceiling/noceiling pass; Partial/Degraded #2: "SC-006 spend-vs-unbounded (booked vs 006's round)". | report-claimed | covered |
| SC-007 | Run both named test harnesses (28/0, 9/0) as the mechanism check that all six consumers function with no regression now; the full "spec → … → testing" end-to-end closure is a deferred live check that completes once this testing phase (and any subsequent phase) concludes. | Cross-cutting Completed (6 named fixtures) + Integration status (both harnesses green, reinstall-survival, shared-file integration); Partial/Degraded #2: "SC-007 end-to-end" listed among the deferred live measurements. | report-claimed | covered |
| SC-008 | Run the `arm4-ceiling` fixture and confirm a ceiling-exhausted run's member opinion carries the reduced-grounding disclosure AND its trace records the ceiling-hit field, per the trace-schema amendment. | Completed §Arm4: "the trace-schema.md graph_queries/ceiling_hit amendment, and the member-prompt + orchestrator enforcement (mechanical disclosure append)"; Integration status: 9/0 arm4-ceiling/noceiling pass. | report-claimed | covered |
| SC-009 | Run both named test harnesses and confirm each of the 6 named, committed consumer regression fixtures passes on this pipeline pass. | Cross-cutting Completed: "6 named consumer non-regression fixtures (FR-013 tripwires)"; Integration status: "Both test harnesses fully green" (28/0, 9/0). | report-claimed | covered |

## Verified by reading vs. would-execute in v2

All 21 `covered` rows rest on `completion-report.md` prose alone (`evidence-source: report-claimed`); `implement.log.md` was not opened this pass — 0 rows are `log-verified`. The report's claims were concrete and specific enough (exact node/edge deltas, exact pass/fail counts named per harness, named fixtures per arm, and an explicitly-reasoned GAP with its own technical explanation) that no single claim raised the kind of concrete doubt that would justify the lazy third read (R1-S05); the default therefore holds across the board.

A v2 test-executing tester would, for the bulk of rows (FR-001/002/004 through FR-014, SC-002/003/008/009), actually run `extensions/graphify/test/run.sh` and `extensions/council/test/run.sh` and reproduce the 28-passed/0-failed and 9-passed/0-failed results this pass only cites, rather than trusting the count. For FR-003/SC-001 it would re-run the live probe (`graphify path "verify-gate.sh" "implement-parallel"`) once the booked fourth edge-kind (I-25/D78, a command→implementing-script or `.md`-skill→command edge) lands, to confirm the GAP has actually closed rather than re-trusting today's negative result. For the four deferred-measurement rows — SC-004 (token-cost budget), SC-005 (member pull-reduction), SC-006 (spend-vs-unbounded, explicitly booked against `006`'s round), and SC-007 (the full spec-to-testing-to-close end-to-end, self-referentially gated on this very phase concluding) — a v2 tester would capture the actual number or measurement from a real pipeline round instead of accepting this pass's mechanism-only fixture evidence; this pass marks all four `covered` on the strength of that mechanism evidence alone, per the report's own framing of them as "mechanism verified by a committed fixture; the number awaits a real pipeline round."

Two things worth flagging. First, the one genuine GAP: FR-003/SC-001 — the headline live check `path "verify-gate.sh" "implement-parallel"` still returns "No path found" post-augment because the relationship is command-mediated and none of the three ratified edge kinds bridges command → implementing-script; the report surfaces this honestly rather than fabricating coverage, and books a disposed follow-up (I-25/D78) rather than silently deferring it. Second, a bare `FR-019` appears several times in `spec.md`'s prose (the Reading note, the Constraints & Assumptions section, and inline inside FR-012's and SC-008's own text) but is never itself defined by a `- **FR-019**:` bullet in this spec's own Functional Requirements section, which defines only FR-001 through FR-014 — so `FR-019` is a citation to a requirement from an earlier feature's spec (the D10 reduced-grounding lineage) and correctly receives no row here, the same as an explicit `NNN-FR-` cross-reference would.
