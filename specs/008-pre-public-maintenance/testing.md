---
feature: 008-pre-public-maintenance
phase: testing
executed: none
---

## Coverage map

| id | approach | grounding | evidence-source | status |
|---|---|---|---|---|
| FR-001 | Manually verify by running `bootstrap.sh --self-test` (both fetch branches) and inspecting the fetch+delegate acquire-then-install flow; a true-outsider end-to-end run is blocked pending the `complete/008` tag and the visibility flip. | `### Completed (18/18)` T003–T006 row; `### Partial/Degraded` item 1 (self-test passes, outsider path blocked) | report-claimed | covered |
| FR-002 | Inspect the README quickstart's install section for a single documented command with no undocumented acquisition step; check the README's own leading "not available yet" notice discloses the current blocker rather than hiding it. | `### Completed (18/18)` T006 README clone-free quickstart; `### Partial/Degraded` item 1 | report-claimed | covered |
| FR-003 | Run `install.sh <target>` twice and diff sha256 of the resulting `.claude/`+`.specify/` tree for byte-identity; confirm the existing local `install.sh` path is unmodified. | `### Partial/Degraded` item 1 ("SC-003's local-route half is already fully discharged... sha256 ... byte-identical") | report-claimed | covered |
| FR-004 | Run `check-conformance.py` against the seven committed both-branch fixtures and confirm each violation names the offending artifact and rule via that fixture's own `VIOLATION.md`. | `### Key results` (checker exists, delegates by subprocess); `### Integration status` "Seven both-branch fixture verdicts — 7/7 correct" | report-claimed | covered |
| FR-005 | Inspect the checker's rule definitions to confirm they derive from `docs/contracts/` schemas directly rather than the retired M0 fixture-coupled verifier; cross-check via the `--self-test` contract-header assertions. | `### Integration status` "`check-conformance.py --self-test` — exit 0, 89/89 assertions — the R1-S19 contract-header self-check" | report-claimed | covered |
| FR-006 | Run the checker against `specs/000-sample` and against a D50 carve-out case (`specs/001-council-extension`); carve-out honoring is directly verified, but the literal "PASSES on `specs/000-sample`" clause is a disclosed, unresolved deviation — the PASS assertion was pinned to `fixtures/conformant/` instead because `specs/000-sample` itself predates D72/D77 and is nonconformant on 12 unrelated counts. | `### Partial/Degraded` item 2 (full deviation account); `### Integration status` "Checker vs. a real live feature — zero false positives... D50 rule-5 meta-feature carve-outs honored" | report-claimed | covered |
| FR-007 | Inspect the checker's single-command CLI invocation and stdlib-only imports; review the double-run byte-diff determinism assertion. | `### Integration status` "Double-run byte-diff determinism (R1-S21) — byte-identical..."; `### Key results` (stdlib-only, no third-party deps) | report-claimed | covered |
| FR-008 | Inspect the static no-import guard (bite-tested) confirming `check-conformance.py` delegates via `subprocess` and never source-`import`s the three existing validators. | `### Integration status` "Static no-import guard (R1-S22) — ok... bite-tested"; `### Key results` (FR-008 composition note) | report-claimed | covered |
| FR-009 | Inspect the seven committed both-branch fixtures (`conformant/` + six `violation-*/`) and confirm the checker's verdict against each matches its `VIOLATION.md` expectation. | `### Integration status` "Seven both-branch fixture verdicts — 7/7 correct" | report-claimed | covered |
| FR-010 | Inspect `print_manual_block()` output against `extension.yml`'s full hook list; rely on the orchestrator's own bite test (hook removed from block → detected, restored → clean). | `### Integration status` "I-23 guard derives from the manifest, not a hardcoded list — proven" | report-claimed | covered |
| FR-011 | Run `extensions/git/test/run.sh`'s static divergence guard and confirm it fails on injected manifest/block divergence (bite-tested). | `### Integration status` "I-23 guard derives from the manifest..." row; "`extensions/git/test/run.sh` — 61 passed / 0 failed" | report-claimed | covered |
| FR-012 | Run `extensions/graphify/test/run.sh`'s new inline-comment-stripping fixtures (incl. whitespace variants) and confirm `dequote()` is untouched by diff inspection. | `### Integration status` graphify row (40/0, +12 new from T014); "I-26 strip is scoped to `parse_hook_commands()`... `dequote()` byte-for-byte unmodified" | report-claimed | covered |
| FR-013 | Inspect the `decision-record.md`/`suggestions.md` template for the new `extensions/council/` HEAD provenance field; live confirmation from an actual council round is a separate, disclosed gap (see SC-008). | `### Completed (18/18)` T013 I-29 council provenance; `### Key results` (H3 discrepancy, I-35) | report-claimed | covered |
| FR-014 | Inspect the implement-parallel trace-writer's null-out logic for gitignored/untracked sole outputs; cite T007's trace-schema violation fixture as the indirect proof, since no live gitignored-output task has run since the rule shipped. | `### Completed (18/18)` T015 I-31 gitignored-artifact rule; `### Partial/Degraded` item 3 ("I-31 is pinned indirectly by T007's trace-schema violation fixture") | report-claimed | covered |
| FR-015 | Run the FR-015 branch-scope allowlist guard (T002) over the complete branch diff and confirm zero strays outside the declared allowlist. | `### Integration status` "FR-015 branch-scope allowlist guard... PASS — 169 paths, zero strays" | report-claimed | covered |
| FR-016 | Grep `docs/90-DECISIONS-AND-IDEAS.md` for the six I-row ids and confirm each carries a "Built at 008" resolution marker. | `### Integration status` "SC-010 six ripe I-rows resolved — 6/6... each carries a `[Bb]uilt at .008.` (D84… 2026-07-20) marker" | report-claimed | covered |
| SC-001 | Once the `complete/008` tag exists and the repo goes public, run the documented bootstrap command from a machine with no prior clone against a throwaway target repo and confirm install with no separate clone/download step — currently blocked and explicitly provisional. | `### Partial/Degraded` item 1; `### Integration status` "Not verified by this run: SC-001/002/003's unauthenticated outsider path" | report-claimed | covered |
| SC-002 | Once unblocked, have a true outsider follow only the README's documented steps and confirm no undocumented acquisition step remains. | `### Partial/Degraded` item 1; `### Integration status` "Not verified by this run" line | report-claimed | covered |
| SC-003 | Local-route idempotency: inspect the already-run double-run sha256 byte-identity check. Outsider-route idempotency: pending, same blocker as SC-001/002. | `### Partial/Degraded` item 1 ("SC-003's local-route half is already fully discharged... byte-identical") | report-claimed | covered |
| SC-004 | Inspect the seven both-branch fixture verdicts (7/7 correct) for the fixture-set clause; the literal "passes on `specs/000-sample`" clause was not executed as specified — a disclosed, human-decided substitution (PASS pinned to `fixtures/conformant/` instead) rather than a silent skip. | `### Integration status` "Seven both-branch fixture verdicts — 7/7 correct"; `### Partial/Degraded` item 2 ("SC-004 is met in substance, deviating from the fixture the contract names — recorded, not silent") | report-claimed | covered |
| SC-005 | Review the double-run byte-diff determinism assertion (byte-identical stdout/stderr/exit code) replacing the former manual re-run-and-eyeball step. | `### Integration status` "Double-run byte-diff determinism (R1-S21)..."; `### Key results` "SC-005 moved from manual to code-verified" | report-claimed | covered |
| SC-006 | Rely on the orchestrator's bite test of the manual-fallback block (hook removed → detected as missing; restored → clean) as proof the guard derives from the manifest. | `### Integration status` "I-23 guard derives from the manifest, not a hardcoded list — proven" | report-claimed | covered |
| SC-007 | Run `extensions/graphify/test/run.sh`'s +12 new fixture assertions covering inline-comment whitespace variants and confirm zero `# …` captured in resulting ids. | `### Integration status` graphify row (40/0, +12 new from T014) | report-claimed | covered |
| SC-008 | Observe the next live council round's `decision-record.md`/`suggestions.md` and confirm it records the `extensions/council/` HEAD sha — explicitly not yet executed, since no council round has run since the rule shipped. | `### Partial/Degraded` item 3; `### Integration status` "Not verified by this run: ... SC-008's live council-round observation" | report-claimed | covered |
| SC-009 | Observe the next `/speckit-implement-parallel` run containing a sole-gitignored-output task and confirm its trace records `artifact: null` — explicitly not yet executed for the same reason as SC-008. | `### Partial/Degraded` item 3; `### Integration status` "Not verified by this run: ... SC-009's live gitignored-output trace" | report-claimed | covered |
| SC-010 | Grep `docs/90-DECISIONS-AND-IDEAS.md` for the six I-row ids and confirm each carries a resolution marker, plus check the FR-015 guard's zero-strays result as the gate-semantics-noninterference evidence. | `### Integration status` "SC-010 six ripe I-rows resolved — 6/6"; `### Key results` ("an FR-015 gate sign-off stated on concrete diff evidence rather than on the SC-010 grep") | report-claimed | covered |

## Verified by reading vs. would-execute in v2

Every row above rests on `completion-report.md` prose alone (`report-claimed`); `implement.log.md` was never opened. The report is unusually specific and self-disclosing throughout — exact counts (169 paths, 89/89 assertions, 7/7 fixtures, 40/0 with a stated baseline delta), named bite tests with their exact before/after outcomes, and even two self-reported corrections (a retracted fabrication accusation, an inaccurate `hardening-invariants.md` site description recorded as I-35) — so no individual claim read as thin, generic, or too convenient to take at face value, and the lazy cross-check was never triggered.

A v2, test-*executing* tester would run rather than read: the five harnesses' `run.sh` scripts directly instead of citing their reported pass counts; `check-conformance.py --self-test` and its seven both-branch fixtures directly instead of citing their 89/89 and 7/7 verdicts; `install.sh <target>` twice with a live sha256 diff instead of citing the report's own double-run result; and, once unblocked, the actual outsider `bootstrap.sh` install (SC-001/002/003), a live council round's provenance write (SC-008), and a live `/speckit-implement-parallel` run against a sole-gitignored-output task (SC-009) — the three criteria this report itself buckets as "not-executed" or "provisional" rather than code-verified.

Two things worth flagging explicitly rather than smoothing over: first, FR-006 and SC-004 both contain a clause — "passes on `specs/000-sample`" — that the report admits is **not** literally true; the checker's PASS assertion was deliberately pinned against `fixtures/conformant/` instead, by disclosed human decision, because `specs/000-sample` predates D72/D77 and fails on 12 unrelated counts. Both rows are still marked `covered` here because the deviation itself is genuinely grounded in the report's own prose, not because the literal clause was satisfied. Second, SC-008 and SC-009 are the report's own explicit "NOT EXECUTED" items (tied to I-29's disclosed gap) — distinct from a fabricated pass, and distinct from a `GAP` row, since the report gives a genuine reason and a named trigger condition (the next live council round; the next gitignored-output task) rather than no evidence at all.
