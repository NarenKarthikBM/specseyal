# Autonomy profile — conformant
# Fixture (T007, specs/008-pre-public-maintenance/fixtures). Both gates human (D9 default).
# See docs/contracts/profile-schema.md.
schema_version: "1.0"
feature: "conformant"

full_auto: false
council_tier: full
deck_render: none            # renders nothing; the default (D73(3), profile-schema.md S1.2).

gates:
  council:
    mode: human
    max_rounds: 1
    reopen_tier: auto
  workforce:
    mode: human
