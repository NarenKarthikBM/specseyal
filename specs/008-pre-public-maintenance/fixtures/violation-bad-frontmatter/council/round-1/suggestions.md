# Suggestions — Round 1 — violation-bad-frontmatter

> Fixture. Stage 3, chairman. The only file under `council/` the main thread reads
> (`artifact-layout.md` §4).

| ID | Class | Suggestion |
|---|---|---|
| R1-S01 | consider | Name the fixture more descriptively than `violation-bad-frontmatter` |
