---
lens: consolidated
---

All five stage-1 opinions independently re-ran the deck's own graph claims rather than trusting its citations — that adversarial discipline raises the floor of this round well above a typical pass, and I re-verified the two highest-confidence graph-grounded corrections myself before trusting them: `find` confirms A's `extension.yml` path claim (`extensions/git/extension/extension.yml`, not a sibling of `install.sh`), and a direct scan of `graphify-out/graph.json` for `*print_manual_block*` nodes confirms C's count of **five** same-named nodes (deck-render/git/graphify/testing/workforce community IDs 262/64/65/66/55), not the "one other" the deck's §4 prose implies. I also confirmed `data-model.md`'s "Cross-entity invariants" section names only E2/E4/E5 under "Both-branch fixture discipline" — E6 (I-29) and E7 (I-31) are absent entirely, and E5's listed coverage is scoped to "whitespace variants," not the "other callers unaffected" half of its own invariant. Every load-bearing citation the five opinions built their strongest points on checks out against the primary sources.

**Ranking as a set.** B and E are this round's strongest opinions. B lands the single highest-impact catch of the whole round — FR-015/SC-010's "load-bearing" gate-semantics-neutral claim has no actual verification mechanism, only a bookkeeping grep — and pairs it with a concrete, correctly-scoped `--ref` security finding. E delivers the most complete ordering/sequencing analysis (guard falsifiability sub-order, mirror-resync timing, FR-016 fan-in against the wave model) plus the round's one genuinely novel, unduplicated find (I-29's self-reference into the council apparatus this very round runs under). A is close behind: the most rigorous independent-verification discipline of the five, and its path-error and Risk Register catches are both real and precise, though its closing D57 nitpick (self-rated low) adds little. D is comparably strong, contributing the sharpest reframing of the I-26/I-29/I-31 cluster (below) and correctly diagnosing that `/speckit-testing`'s own doc-only contract means "manual" SCs risk being recorded as covered without ever running. C is solid — its collision-count correction is the round's best single graph catch — but two of its four suggestions (Risk Register footnote-promotion, close-out ordering) are thinner restatements of ground A and E already cover more specifically; no opinion in this round is weak, but C's marginal value drops off fastest after its first point.

**The biggest corroboration cluster.** A (#2/#3), B (#5), and D (#2/#4) independently converge on the same underlying defect from different angles: I-26/I-29/I-31 each land a fixture for the primary fix but not for the invariant the fix is supposed to preserve. This is one systemic finding wearing three costumes, not three unrelated ones — the chairman should consolidate it into a single remediation item rather than three. D's version is the strongest to anchor on: it correctly separates I-26 (a genuine fixture gap, fixable the same way) from I-29/I-31 (architecturally different — they edit LLM-interpreted `SKILL.md` prompt text, not code a `run.sh` harness can exercise the same way) and offers a concrete alternate path for I-31 via `check-conformance.py`'s own contract-check step. A's contribution to this cluster (revising the deck's "two exceptions" framing to the real 3-of-4 count) is independently verified and should survive as the correction to §2's prose specifically.

**A related, smaller redundancy.** A's Risk Register suggestion (I-29/I-31 zero-fixture deserves its own row) and C's Risk Register suggestion (the aggregate 6/10-manual tally deserves its own row) point at the same root problem — §5 underrepresents what §7 documents — at two granularities. One Risk Register pass resolves both; they shouldn't become two separate tasks.

**A genuinely new point all five missed.** None of the five opinions asks whether `check-conformance.py` (I-11) is ever wired into anything that runs it. Per the deck's own E2 (§3) and `plan.md` line 11, it is purely arg-driven — `<feature-dir>` passed by a human — with no `before_*`/`after_*` hook registration and no CI invocation named anywhere in the plan, unlike e.g. `validate-categorization.py`, which gates `categorization.md` on a pass (per `speckit-categorize`'s own contract). The deck's own problem restatement (§1) is that conformance drift is "caught by human review, not by machine" — as currently scoped, I-11 adds a tool a human *can* run, but doesn't change the enforcement point: nothing stops the checker from being built, correct, and never invoked once council/implement wraps. This is worth a plan-level answer (wire it into an existing hook, or explicitly accept "detectable on demand" as the actual, narrower scope) before `/speckit-tasks` locks the item's definition of done.

## Suggestions

- [risk] Endorse B: FR-015/SC-010's gate-semantics-neutral claim — asserted twice in the deck as the invariant that justifies bundling six items into one feature — has no real verification mechanism; SC-010's only check (grep `docs/90` for I-codes) proves close-out bookkeeping, not that `extensions/council/**`/`extensions/workforce/**`/gate schema files went untouched. Strongest single finding of the round. (confidence: high)
- [correctness] Endorse A: §4's Project Structure table misstates `extension.yml`'s path as a sibling of `install.sh`/`test/run.sh` directly under `extensions/git/`; independently re-verified via `find` — it is actually at `extensions/git/extension/extension.yml`. (confidence: high)
- [simplicity] Endorse C: the deck's §4 prose implies one other `print_manual_block()` collision; independently re-verified against `graphify-out/graph.json` — there are five (deck-render, git, graphify, testing, workforce). I-23 correctly scopes to git's copy, but the deck understates the size of the collision-prone pattern it uses as its own cautionary example. (confidence: high)
- [testability] Endorse A, B, and D as one cluster, not three separate findings: I-26/I-29/I-31 each land a fixture for the primary fix but not for the invariant the fix must preserve. Independently confirmed against `data-model.md`'s "Cross-entity invariants" — only E2/E4/E5 carry both-branch fixture discipline; E6 (I-29) and E7 (I-31) are absent entirely, and E5's (I-26) coverage stops at "whitespace variants," not "other callers unaffected." D's framing is the strongest to build the single remediation task on: I-26 is a fixable fixture gap, I-29/I-31 are architecturally different (LLM-interpreted `SKILL.md`, not `run.sh`-exercisable code) and need a different closure mechanism, e.g. D's proposed fold of I-31 into `check-conformance.py`'s own contract check. (confidence: high)
- [sequencing] Endorse E: the I-23 lock-step task needs its internal sub-order pinned — generalize the guard, run it and observe it fail pre-fix, apply the manifest/block fix, re-run and observe it pass — or H1's "prove the guard fails pre-fix" contract (SC-006) is satisfiable in appearance without ever being genuinely witnessed. (confidence: high)
- [testability] Endorse D: 6 of 10 SCs rest entirely on a human running `quickstart.md`, but `/speckit-testing`'s own contract is doc-only ("executed: none always") — nothing in the plan names an actual executor for that walkthrough, so these six risk being recorded as covered in `testing.md` without the quickstart having run, immediately before the D73 flip where SC-001/002/003 and SC-008/009/010 matter most. (confidence: high)
- [sequencing] Endorse E: I-29's self-reference (it edits `speckit-council/SKILL.md` and `speckit-council-triage/SKILL.md` — the very files this 008 council round is running under) is genuinely novel among the five opinions and unaddressed by the deck or plan.md; worth an explicit answer given the repo's own dogfooding rule. (confidence: med)
- [risk] Endorse B: `bootstrap.sh`'s `--ref` default is never committed to a concrete pinned value anywhere in the artifact set, and no contract row validates/escapes it before interpolation into `git clone`/`sparse-checkout` — a ref beginning with `-` is a known argument-injection class. Both are cheap to close before a public curl-and-run script exists to point at an untrusted ref. (confidence: med)
- [sequencing] Prefer E over C on the close-out ordering point: both flag that FR-016's same-session, all-six close-out is a hard join-point, but E's treatment (explicit fan-in framing against `speckit-implement-parallel`'s independent-wave model, with a concrete question of whether close-out is the final serialized wave) is more developed than C's low-confidence footnote-level version — the chairman should use E's phrasing and drop C's as subsumed. (confidence: med)
- [testability] Endorse D: the FR-011 divergence guard's own grep must be scoped to `extensions/git/install.sh` specifically, not a bare `print_manual_block` name match — the same five-way collision C's correction surfaces is a live risk to the guard's own implementation, not just to correctly locating the I-23 fix. Complementary to C's count-correction, not redundant with it. (confidence: med)
- [testability] Endorse A and C together: both flag that §5's Risk Register underrepresents what §7 documents (A: the I-29/I-31 zero-fixture gap has no row despite being more severe than I-26's, which does; C: the aggregate 6-of-10-manual tally has no row of its own) — same root problem at two granularities, resolvable in one Risk Register pass rather than two separate edits. (confidence: med)
- [testability] New: none of the five opinions asks whether `check-conformance.py` (I-11) is ever invoked by anything other than a human choosing to run it — no hook registration, no CI step named anywhere in the plan or deck. I-11's own problem statement is that drift is "caught by human review, not by machine" (deck §1); as scoped, the fix adds a tool that *can* detect drift but doesn't change who's responsible for running it. Worth an explicit plan-level answer — wire it to an existing hook, or accept "on-demand only" as the item's actual, narrower scope — before `/speckit-tasks` locks the definition of done. (confidence: med)
- [risk] Challenge B: the check-conformance.py stderr-format/exit-code "frozen interface" suggestion is the thinnest bullet in an otherwise strong opinion — no evidence is cited that the three delegated validators' error text has ever drifted or is likely to, and it's explicitly self-rated low. Lowest priority in the set. (confidence: low)
