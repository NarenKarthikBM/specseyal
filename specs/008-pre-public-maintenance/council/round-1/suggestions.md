# Suggestions — Round 1

**Feature**: `008-pre-public-maintenance`
**Prepared by**: Chairman synthesis (Opus, xhigh, D18)
**Reads**: `round-1/opinions/*.md`, `round-1/opinions/peer/*.md` (chairman-only inputs; never quoted verbatim below)

> Format: markdown v1 (D15). This is the **compression boundary** (`data-model.md`) — the ONLY file under `council/` the main thread reads (FR-007, principle 1), and the sole input to `/speckit-council-triage`. Round-scoped: written once as `round-1/suggestions.md` and **never overwritten**.

*Chairman's stage-3 synthesis of stage 1 (five independent opinions A–E) and stage 2 (one consolidated peer critique, standard tier): consolidate, classify, ID, stop. Opinion prose is never forwarded — `opinions/` stays chairman-only for the life of the feature (FR-006, FR-007, SC-005). §-references are the shared defense-deck `technical.md` section numbering (§1 Problem · §2 Rejected Alternatives · §3 Architecture · §4 Project Structure/Graph · §5 Risk Register · §6 Cost · §7 Testability).*

---

**Verdict:** 1 blocking · 13 strong · 10 consider

---

## Suggestions

| ID | Class | Suggestion | Sources | Target |
|----|-------|-----------|---------|--------|
| R1-S01 | blocking | Give FR-015's gate-semantics-neutral invariant — asserted twice as the load-bearing justification for bundling six items — a real verification mechanism: a check that diffs/greps `extensions/council/**`, `extensions/workforce/**`, and the gate-schema files between the feature-branch base and tip and fails if anything outside the six named edit sites changed, or at minimum an explicit human sign-off item distinct from SC-010's `docs/90` I-code grep (which only proves close-out bookkeeping, not non-interference). | A, B | plan.md §7 |
| R1-S02 | strong | Correct §4's Project Structure table: git-ext's `extension.yml` lives at `extensions/git/extension/extension.yml`, not as a sibling of `install.sh` / `test/run.sh` directly under `extensions/git/` (`find`-verified). | A | plan.md §4 |
| R1-S03 | strong | Require a regression fixture per *preserved invariant*, not just per primary fix, across I-26/I-29/I-31 (the round's largest corroboration cluster): for I-26 add cases exercising `dequote()`'s other two callers (`parse_shell`, `resolve_target`) in graphify's `run.sh`; for I-29/I-31 — which edit LLM-interpreted `SKILL.md` prose no `run.sh` can exercise — name the alternate closure (e.g. fold I-31's trace-schema rule into `check-conformance.py` against a hand-authored violating/clean fixture) rather than leaving the invariant unproven. | A, B, D | plan.md §7 |
| R1-S04 | strong | Promote the gaps §7 discloses but §5 omits into first-class Risk Register rows in one pass — both the I-29/I-31 zero-fixture gap (strictly worse than the I-26 gap that already has a row) and the aggregate 6-of-10-manual-SC tally — so triage tracks them explicitly rather than reconstructing them from a testability-table footnote. | A, C | plan.md §5 |
| R1-S05 | strong | Before implementation, pin `bootstrap.sh`'s `--ref` to a concrete released tag (not a moving branch — an undecided default contradicts R1's own pinned-posture rationale) and add a contract row that validates/escapes `--ref` (and `<target-repo-dir>`) before interpolation into `git clone` / `sparse-checkout`; a ref beginning with `-` is a known git argument-injection vector. Also surface the `<extension-name>` closed-enum constraint (already in the bootstrap-install contract) in §3/E1. | B | plan.md §3 |
| R1-S06 | strong | Flag SC-001/002/003's manual quickstart sign-off as provisional: run pre-D73 by an authenticated maintainer against a still-private repo, it cannot exercise the unauthenticated true-outsider path it claims, so require re-confirmation against the real public repo immediately after the D73 visibility flip. | B | plan.md §7 |
| R1-S07 | strong | State in §4/§5 that the graph holds *five* structurally-identical `print_manual_block()` nodes (deck-render, git, graphify, testing, workforce), not the one the §4 prose implies; I-23 is correctly scoped to git's copy, but note the other four are equally susceptible to the same manifest/manual-block drift so a reader doesn't assume I-23 closed the pattern repo-wide. | C | plan.md §4 |
| R1-S08 | strong | Reduce `bootstrap.sh`'s fetch surface — the sparse partial-clone plus codeload-tarball fallback is the bundle's largest branch-count increase yet lands in its least-tested item (SC-001/002/003 all Manual): either collapse to a single tarball-only fetch or add a minimal automated smoke test exercising both branches. | C | plan.md §3 |
| R1-S09 | strong | Name an explicit executor for the six manual-only SCs (SC-001/002/003, SC-008/009/010) — e.g. bind `quickstart.md` to the quickstart-integration-gate discipline before `/speckit-complete`, or state in plan.md's Testing field who runs the walkthrough and when — since `/speckit-testing` is doc-only ("executed: none") and these are the guarantees load-bearing for the D73 flip. | D | plan.md §7 |
| R1-S10 | strong | Scope the FR-011 divergence guard's grep to `extensions/git/install.sh` specifically, not a bare `print_manual_block` name match — the five-way collision means an unscoped guard could be silently satisfied by an unrelated extension's same-named function. | D | plan.md §7 |
| R1-S11 | strong | Pin the internal sub-order of I-23's single lock-step task: generalize the guard, run it and witness it fail against the current pre-fix manifest/block, then apply the `extension.yml`/`install.sh` fix, then re-run and witness it pass — otherwise H1/SC-006's "guard fails pre-fix" contract is satisfiable only in appearance, with no git-observable moment where the pre-fix failure was witnessed. | E | plan.md §4 |
| R1-S12 | strong | Require the four §4 collision-watch constraints (`docs/90`, git's `extension.yml`+`install.sh` pair, README, the mirror resync) to be encoded as explicit depends-on edges when `/speckit-tasks(-graph)` generates tasks.md, rather than left as deck prose a graph-driven wave builder won't read. | E | plan.md §4 |
| R1-S13 | strong | Answer the I-29 self-reference: it edits `speckit-council/SKILL.md` and `speckit-council-triage/SKILL.md` — the very files this 008 council round runs under — so state whether this round's own `suggestions.md`/decision-record retroactively carry the new provenance line once I-29 ships within this same feature, given the repo's own dogfooding rule. | E | plan.md (whole) |
| R1-S14 | strong | Decide I-11's actual definition of done: `check-conformance.py` is arg-driven with no `before_*`/`after_*` hook or CI invocation named anywhere, so as scoped it adds a tool a human *can* run without changing the enforcement point (§1's own "caught by human review, not machine" problem). Either wire it into an existing hook or explicitly accept "detectable on demand" as the narrower scope before `/speckit-tasks` locks the item. | peer | plan.md §3 |
| R1-S15 | consider | Revise §2's "each landing with a both-branch committed fixture (with two exceptions)" to match §7/data-model: only I-23 has clean coverage, I-26 is partial, and I-29/I-31 have none — 3 of 4 hardening items carry a real gap, not two minor exceptions. | A | plan.md §2 |
| R1-S16 | consider | Before `/speckit-tasks` fixes the fixture set, make explicit whether `check-conformance.py`'s planned `4 violation-*/` dirs exercise all six directly-checked contracts (`artifact-layout`, `decision-record`, `completion-report`, `testing-doc`, `trace-schema`, `agent-library-schema`) or leave some without any injected-violation test (bearing on FR-009 coverage). | A | plan.md §7 |
| R1-S17 | consider | Sharpen or drop §3's D57 citation as precedent for `check-conformance.py`'s shell-out — D57 concerns not patching another extension's installer-overwritten source, not runtime reads of another extension's output; a plainer "this is a read, not a write" argument (or a more apt precedent) is more accurate. | A | plan.md §3 |
| R1-S18 | consider | Add one contract line naming the three delegated validators' stderr-format/exit-code as a frozen interface, so a future edit to a validator's error text can't silently change what `check-conformance.py` reports as the cause. (Lowest priority — no evidence of past or likely drift; peer-flagged as the round's thinnest bullet.) | B | plan.md §3 |
| R1-S19 | consider | Add a lightweight self-check to `check-conformance.py`'s direct-check step (e.g. a `_self_test()` asserting the section headers/field names it parses still exist in each `docs/contracts/*.md`) so a future contract-doc edit fails loudly instead of silently desyncing the only code kept in sync with that prose. | C | plan.md §3 |
| R1-S20 | consider | State whether FR-016's same-session, all-six `docs/90` close-out is the final serialized wave gated on the other five piles' completion (the apparent intent) or gates earlier — the §4 collision watch makes a single flat parallel wave unlikely, and SC-010's post-hoc I-code grep can't distinguish "closed in order" from "closed out of order but all present." | C, E | plan.md §6 |
| R1-S21 | consider | Automate the SC-005 determinism check (a double-run byte-diff assertion folded into `extensions/workforce/test/run.sh`) instead of leaving it the manual re-run-and-eyeball step `quickstart.md` excludes from the both-branch suite — moving SC-005 from partial/manual to code-verified. | D | plan.md §7 |
| R1-S22 | consider | Add a one-line static guard that `check-conformance.py`'s source contains no source-level import of the existing validators (only subprocess/shell-out calls), converting the "no duplication" design property from an assertion into a mechanically-caught regression. | D | plan.md §7 |
| R1-S23 | consider | Sequence the `.specify/extensions.yml` mirror resync (if the I-23 edit triggers a reinstall) strictly *before* running `extensions/git/test/run.sh`'s guard, not as an independently-ordered "separate commit" — otherwise the guard may validate a stale installed mirror. | E | plan.md §5 |
| R1-S24 | consider | Specify `bootstrap.sh`'s behavior when step 3 (delegating to the extension's `install.sh`) fails partway: name the target-repo state and the exit-code-propagation vs temp-cleanup order (trap-based EXIT), rather than leaving partial-install handling inherited-by-assumption from `install.sh`'s idempotency. | E | plan.md §3 |

---

## Chairman's note

The council found an unusually well-grounded, deliberately boring plan — all five members independently re-ran and confirmed its graph claims — so the verdict is narrow: one blocking item, that FR-015's load-bearing "no gate-semantics change" invariant (the entire justification for bundling six items) has no verification mechanism at all, plus a strong cluster the members converged on from five angles (I-26/I-29/I-31 each fixture the primary fix but not the invariant it must preserve) and a well-scoped set of security, sequencing, and testability refinements. The round's two genuinely novel finds were the self-reference of I-29 into the very council `SKILL.md` files this round runs under, and that `check-conformance.py` (I-11) is never wired to anything that runs it. Resolve R1-S01 and the plan is ready for the gate.
