# Decision Record — 008-pre-public-maintenance

> Append-only. Rounds are appended, never edited. Rejection requires reasoning (D13.5).

## Metadata

| Field | Value |
|---|---|
| feature | `008-pre-public-maintenance` |
| spec-id | `008-pre-public-maintenance` |
| profile | `gates.council=human, gates.workforce=human` (no `profile.yaml` — P1 default) |
| rounds-run | 1 |
| schema-version | `1.0` |

---

## Round 1 — 2026-07-20T07:57:19.000Z

**Verdict:** 1 blocking · 13 strong · 10 consider
**Deck reviewed:** `council/defense-deck/technical.md` @ `c5baa6f`
**Plan reviewed:** `plan.md` @ `f24be73`

| ID | Class | Suggestion | Disposition | Rationale | Plan delta |
|---|---|---|---|---|---|
| R1-S01 | blocking | Give FR-015's gate-semantics-neutral invariant — asserted twice as the load-bearing justification for bundling six items — a real verification mechanism: a check that diffs/greps `extensions/council/**`, `extensions/workforce/**`, and the gate-schema files between the feature-branch base and tip and fails if anything outside the six named edit sites changed, or at minimum an explicit human sign-off item distinct from SC-010's `docs/90` I-code grep (which only proves close-out bookkeeping, not non-interference). | accepted | Blocking default (no operator override). The load-bearing FR-015 invariant now has a real mechanism: a committed allowlist check `git diff --name-only $(git merge-base <base_branch> HEAD)..HEAD` that fails on any edit outside the six declared sites, backed by an explicit gate sign-off distinct from SC-010's I-code grep. **Delta-check: RESOLVED.** | `plan.md §Constitution Check` @ `dca56c4` |
| R1-S02 | strong | Correct §4's Project Structure table: git-ext's `extension.yml` lives at `extensions/git/extension/extension.yml`, not as a sibling of `install.sh` / `test/run.sh` directly under `extensions/git/` (`find`-verified). | accepted | `find`-reproduced. Tree rewritten to show `extension/extension.yml` nested under `extension/`, not flattened as a sibling of `install.sh`/`test/run.sh`. | `plan.md §Project Structure` @ `dca56c4` |
| R1-S03 | strong | Require a regression fixture per *preserved invariant*, not just per primary fix, across I-26/I-29/I-31 (the round's largest corroboration cluster): for I-26 add cases exercising `dequote()`'s other two callers (`parse_shell`, `resolve_target`) in graphify's `run.sh`; for I-29/I-31 — which edit LLM-interpreted `SKILL.md` prose no `run.sh` can exercise — name the alternate closure (e.g. fold I-31's trace-schema rule into `check-conformance.py` against a hand-authored violating/clean fixture) rather than leaving the invariant unproven. | accepted | The round's largest corroboration cluster (A, B, D). Testing now requires a fixture per preserved invariant: I-26 exercises `dequote()`'s `parse_shell`/`resolve_target` callers; I-29/I-31's SKILL.md-prose invariants get a hand-authored violating/clean fixture folded into `check-conformance.py`. | `plan.md §Technical Context (Testing)` @ `dca56c4` |
| R1-S04 | strong | Promote the gaps §7 discloses but §5 omits into first-class Risk Register rows in one pass — both the I-29/I-31 zero-fixture gap (strictly worse than the I-26 gap that already has a row) and the aggregate 6-of-10-manual-SC tally — so triage tracks them explicitly rather than reconstructing them from a testability-table footnote. | accepted | The disclosed §7 gaps are promoted to first-class tracked risks in Testing — the I-29/I-31 zero-fixture gap and the 6-of-10-manual-SC tally — so they aren't reconstructed from a footnote. | `plan.md §Technical Context (Testing)` @ `dca56c4` |
| R1-S05 | strong | Before implementation, pin `bootstrap.sh`'s `--ref` to a concrete released tag (not a moving branch — an undecided default contradicts R1's own pinned-posture rationale) and add a contract row that validates/escapes `--ref` (and `<target-repo-dir>`) before interpolation into `git clone` / `sparse-checkout`; a ref beginning with `-` is a known git argument-injection vector. Also surface the `<extension-name>` closed-enum constraint (already in the bootstrap-install contract) in §3/E1. | accepted | Real argument-injection surface. `--ref` now pins to a concrete released tag; `--ref` + `<target-repo-dir>` validated/escaped before `git clone`/`sparse-checkout` interpolation; `<extension-name>` closed-enum surfaced in the §3/E1 contract. | `plan.md §Summary` @ `dca56c4` |
| R1-S06 | strong | Flag SC-001/002/003's manual quickstart sign-off as provisional: run pre-D73 by an authenticated maintainer against a still-private repo, it cannot exercise the unauthenticated true-outsider path it claims, so require re-confirmation against the real public repo immediately after the D73 visibility flip. | accepted | Correct — a maintainer-run, still-private sign-off can't exercise the true-outsider path. SC-001/002/003 marked provisional, re-confirmed against the real public repo immediately after the D73 flip. | `plan.md §Technical Context (Testing)` @ `dca56c4` |
| R1-S07 | strong | State in §4/§5 that the graph holds *five* structurally-identical `print_manual_block()` nodes (deck-render, git, graphify, testing, workforce), not the one the §4 prose implies; I-23 is correctly scoped to git's copy, but note the other four are equally susceptible to the same manifest/manual-block drift so a reader doesn't assume I-23 closed the pattern repo-wide. | accepted | `grep -rl` + `graphify explain` confirmed five identical `print_manual_block()` nodes. Structure Decision now states this; I-23 stays scoped to git's copy but the note prevents assuming the pattern is closed repo-wide. | `plan.md §Project Structure` @ `dca56c4` |
| R1-S08 | strong | Reduce `bootstrap.sh`'s fetch surface — the sparse partial-clone plus codeload-tarball fallback is the bundle's largest branch-count increase yet lands in its least-tested item (SC-001/002/003 all Manual): either collapse to a single tarball-only fetch or add a minimal automated smoke test exercising both branches. | accepted | The largest branch-count increase sits in the least-tested item. Transport kept (research-resolved), but a minimal automated smoke test now exercises both the sparse-partial-clone and codeload-tarball fallback branches. | `plan.md §Summary` @ `dca56c4` |
| R1-S09 | strong | Name an explicit executor for the six manual-only SCs (SC-001/002/003, SC-008/009/010) — e.g. bind `quickstart.md` to the quickstart-integration-gate discipline before `/speckit-complete`, or state in plan.md's Testing field who runs the walkthrough and when — since `/speckit-testing` is doc-only ("executed: none") and these are the guarantees load-bearing for the D73 flip. | accepted | `/speckit-testing` is doc-only, so the six manual-only SCs need a named executor. Bound to the `quickstart-integration-gate` discipline run before `/speckit-complete`, with owner/timing stated. | `plan.md §Technical Context (Testing)` @ `dca56c4` |
| R1-S10 | strong | Scope the FR-011 divergence guard's grep to `extensions/git/install.sh` specifically, not a bare `print_manual_block` name match — the five-way collision means an unscoped guard could be silently satisfied by an unrelated extension's same-named function. | accepted | The five-way collision makes a bare name-match guard falsely satisfiable. Guard grep scoped to `extensions/git/install.sh` specifically. | `plan.md §Project Structure` @ `dca56c4` |
| R1-S11 | strong | Pin the internal sub-order of I-23's single lock-step task: generalize the guard, run it and witness it fail against the current pre-fix manifest/block, then apply the `extension.yml`/`install.sh` fix, then re-run and witness it pass — otherwise H1/SC-006's "guard fails pre-fix" contract is satisfiable only in appearance, with no git-observable moment where the pre-fix failure was witnessed. | accepted | Without a pinned sub-order the "guard fails pre-fix" contract is only appearance-satisfiable. I-23 fixed as one lock-step task: generalize → witness FAIL pre-fix → apply fix → witness PASS. | `plan.md §Project Structure` @ `dca56c4` |
| R1-S12 | strong | Require the four §4 collision-watch constraints (`docs/90`, git's `extension.yml`+`install.sh` pair, README, the mirror resync) to be encoded as explicit depends-on edges when `/speckit-tasks(-graph)` generates tasks.md, rather than left as deck prose a graph-driven wave builder won't read. | accepted | A graph-driven wave builder won't read deck prose. The four collision-watch constraints must be explicit `depends-on` edges in `tasks.md` generation. | `plan.md §Project Structure` @ `dca56c4` |
| R1-S13 | strong | Answer the I-29 self-reference: it edits `speckit-council/SKILL.md` and `speckit-council-triage/SKILL.md` — the very files this 008 council round runs under — so state whether this round's own `suggestions.md`/decision-record retroactively carry the new provenance line once I-29 ships within this same feature, given the repo's own dogfooding rule. | accepted | Genuinely novel self-reference. Answered in Phase Notes: the provenance line is generation-time additive metadata; this round's artifacts predate I-29 shipping so they don't retroactively carry it, and no back-fill is implied — the dogfooding rule applies forward. | `plan.md §Phase Notes` @ `dca56c4` |
| R1-S14 | strong | Decide I-11's actual definition of done: `check-conformance.py` is arg-driven with no `before_*`/`after_*` hook or CI invocation named anywhere, so as scoped it adds a tool a human *can* run without changing the enforcement point (§1's own "caught by human review, not machine" problem). Either wire it into an existing hook or explicitly accept "detectable on demand" as the narrower scope before `/speckit-tasks` locks the item. | accepted | Genuinely novel (peer). Decided the narrower scope: `check-conformance.py` is **detectable-on-demand** (maintainer/CI runs it, like `/speckit-validate-profile`), deliberately NOT wired into a `before_*`/`after_*` hook — wiring an enforcement point would touch the gate/phase semantics FR-015 forbids. | `plan.md §Summary` @ `dca56c4` |
| R1-S15 | consider | Revise §2's "each landing with a both-branch committed fixture (with two exceptions)" to match §7/data-model: only I-23 has clean coverage, I-26 is partial, and I-29/I-31 have none — 3 of 4 hardening items carry a real gap, not two minor exceptions. | accepted | Accurate — 3 of 4 hardenings carry a real gap. Summary's fixture-coverage claim corrected: I-23 both-branch, I-26 partial, I-29/I-31 none-via-`run.sh` (pinned by conformance-checker fixtures instead). | `plan.md §Summary` @ `dca56c4` |
| R1-S16 | consider | Before `/speckit-tasks` fixes the fixture set, make explicit whether `check-conformance.py`'s planned `4 violation-*/` dirs exercise all six directly-checked contracts (`artifact-layout`, `decision-record`, `completion-report`, `testing-doc`, `trace-schema`, `agent-library-schema`) or leave some without any injected-violation test (bearing on FR-009 coverage). | accepted | Bears on FR-009 coverage. Testing now requires the violation fixtures to exercise all six directly-checked contracts, calling out any left uncovered. | `plan.md §Technical Context (Testing)` @ `dca56c4` |
| R1-S17 | consider | Sharpen or drop §3's D57 citation as precedent for `check-conformance.py`'s shell-out — D57 concerns not patching another extension's installer-overwritten source, not runtime reads of another extension's output; a plainer "this is a read, not a write" argument (or a more apt precedent) is more accurate. | accepted | Correct — D57 is about not patching another extension's installer-overwritten source, not a runtime read. Citation dropped; replaced with the plain "a read is not a write" seam argument. | `plan.md §Project Structure` @ `dca56c4` |
| R1-S18 | consider | Add one contract line naming the three delegated validators' stderr-format/exit-code as a frozen interface, so a future edit to a validator's error text can't silently change what `check-conformance.py` reports as the cause. (Lowest priority — no evidence of past or likely drift; peer-flagged as the round's thinnest bullet.) | rejected | Peer-flagged as the round's thinnest bullet, with the flagging opinion itself noting no evidence of past or likely drift. Freezing another extension's stderr-format/exit-code as a relied-upon interface manufactures speculative cross-component coupling; the composition already shells out to versioned installed copies. Revisit only if a validator's error-text change is ever observed to silently alter a conformance verdict — not worth pre-committing the plan to a frozen inter-validator contract on a hypothetical. | — |
| R1-S19 | consider | Add a lightweight self-check to `check-conformance.py`'s direct-check step (e.g. a `_self_test()` asserting the section headers/field names it parses still exist in each `docs/contracts/*.md`) so a future contract-doc edit fails loudly instead of silently desyncing the only code kept in sync with that prose. | accepted | On-theme with the feature's anti-drift purpose and cheap. `check-conformance.py` gets a lightweight self-test asserting the contract section headers/field names it parses still exist. | `plan.md §Technical Context (Testing)` @ `dca56c4` |
| R1-S20 | consider | State whether FR-016's same-session, all-six `docs/90` close-out is the final serialized wave gated on the other five piles' completion (the apparent intent) or gates earlier — the §4 collision watch makes a single flat parallel wave unlikely, and SC-010's post-hoc I-code grep can't distinguish "closed in order" from "closed out of order but all present." | accepted | The collision watch makes a flat parallel wave unlikely and SC-010's grep can't prove order. Phase Notes now pin FR-016's all-six close-out as the final serialized step gated on the other five piles. | `plan.md §Phase Notes` @ `dca56c4` |
| R1-S21 | consider | Automate the SC-005 determinism check (a double-run byte-diff assertion folded into `extensions/workforce/test/run.sh`) instead of leaving it the manual re-run-and-eyeball step `quickstart.md` excludes from the both-branch suite — moving SC-005 from partial/manual to code-verified. | accepted | Fully mechanizable. SC-005 determinism now a double-run byte-diff assertion in `extensions/workforce/test/run.sh`, moving it from manual to code-verified. | `plan.md §Technical Context (Testing)` @ `dca56c4` |
| R1-S22 | consider | Add a one-line static guard that `check-conformance.py`'s source contains no source-level import of the existing validators (only subprocess/shell-out calls), converting the "no duplication" design property from an assertion into a mechanically-caught regression. | accepted | Cheap and enforces FR-008. A static guard asserts `check-conformance.py` has no source-level import of the validators (only shell-out), making no-duplication a caught regression. | `plan.md §Technical Context (Testing)` @ `dca56c4` |
| R1-S23 | consider | Sequence the `.specify/extensions.yml` mirror resync (if the I-23 edit triggers a reinstall) strictly *before* running `extensions/git/test/run.sh`'s guard, not as an independently-ordered "separate commit" — otherwise the guard may validate a stale installed mirror. | accepted | Correct sequencing (matches the install-mirror-drift discipline). The `.specify/extensions.yml` resync is ordered strictly before the git guard runs, so it never validates a stale mirror. | `plan.md §Project Structure` @ `dca56c4` |
| R1-S24 | consider | Specify `bootstrap.sh`'s behavior when step 3 (delegating to the extension's `install.sh`) fails partway: name the target-repo state and the exit-code-propagation vs temp-cleanup order (trap-based EXIT), rather than leaving partial-install handling inherited-by-assumption from `install.sh`'s idempotency. | accepted | Real partial-install robustness gap. `bootstrap.sh` now specifies trap-based `EXIT` temp cleanup + non-zero exit propagation and a named target-repo state on mid-install failure. | `plan.md §Summary` @ `dca56c4` |

### Chairman delta check — 2026-07-20T07:54:35.000Z

- R1-S01 — RESOLVED — The revised §Autonomy & Gates row gives FR-015 both mechanisms the finding asked for: a committed check that runs `git diff --name-only $(git merge-base <base_branch> HEAD)..HEAD` and fails if any changed path falls outside the enumerated six-edit-site allowlist, plus an explicit human-gate sign-off line confirming no gate-schema/semantics file changed — expressly distinct from SC-010's `docs/90` I-code grep.

**Delta verdict:** all clear, ready for the gate

---

## Carried Constraints

*Rebuilt from every round's accepted rows; `/speckit-tasks` reads this section and nothing else from this file.*

- `R1-S01` — Ship a committed FR-015 verification: an allowlist `git diff --name-only $(git merge-base <base_branch> HEAD)..HEAD` check that fails if any changed path falls outside the six declared edit sites, plus an explicit gate sign-off line distinct from SC-010's `docs/90` I-code grep.
- `R1-S02` — Render the git-ext tree with `extension.yml` nested at `extensions/git/extension/extension.yml`, not flattened as a sibling of `install.sh`/`test/run.sh`.
- `R1-S03` — One regression fixture per preserved invariant, not just per primary fix: I-26 covers `dequote()`'s `parse_shell` + `resolve_target` callers; I-29/I-31 get a hand-authored violating/clean fixture folded into `check-conformance.py` (e.g. I-31's trace-schema rule).
- `R1-S04` — Track the I-29/I-31 zero-fixture gap and the 6-of-10-manual-SC tally as first-class risks, not testability-table footnotes.
- `R1-S05` — `bootstrap.sh --ref` pins to a concrete released tag; validate/escape `--ref` and `<target-repo-dir>` before `git clone`/`sparse-checkout` interpolation (a `-`-leading ref is arg-injection); constrain `<extension-name>` to the contract's closed enum.
- `R1-S06` — Mark SC-001/002/003's manual quickstart sign-off provisional and re-confirm against the real public repo immediately after the D73 visibility flip.
- `R1-S07` — Note the five identical `print_manual_block()` nodes (deck-render/git/graphify/testing/workforce); I-23 closes git's copy only, not the pattern repo-wide.
- `R1-S08` — Add a minimal automated smoke test exercising both `bootstrap.sh` fetch branches (sparse-partial-clone + codeload-tarball fallback), not the manual quickstart alone.
- `R1-S09` — Name an executor for the six manual-only SCs (SC-001/002/003, SC-008/009/010): bind `quickstart.md` to the `quickstart-integration-gate` discipline before `/speckit-complete`, with owner/timing stated.
- `R1-S10` — Scope the I-23 divergence guard's grep to `extensions/git/install.sh` specifically, not a bare `print_manual_block` name match.
- `R1-S11` — I-23 is one lock-step task in fixed sub-order: generalize guard → witness FAIL against the pre-fix manifest/block → apply the `extension.yml`/`install.sh` fix → witness PASS (H1/SC-006).
- `R1-S12` — Encode the four collision-watch constraints (`docs/90`; git's `extension.yml`+`install.sh` pair; `README.md`; `.specify/extensions.yml` mirror resync) as explicit `depends-on` edges in `tasks.md`.
- `R1-S13` — I-29's provenance line is generation-time additive metadata; this round's own artifacts predate I-29 and are not back-filled — the dogfooding rule applies forward only.
- `R1-S14` — `check-conformance.py` (I-11) is detectable-on-demand (maintainer/CI-run, like `/speckit-validate-profile`); do NOT wire it into a `before_*`/`after_*` hook (FR-015 gate-semantics-neutral).
- `R1-S15` — State fixture coverage honestly: I-23 both-branch, I-26 partial, I-29/I-31 no `run.sh` fixture (pinned by conformance-checker fixtures) — three of four hardenings carry a real gap.
- `R1-S16` — `check-conformance.py`'s violation fixtures must exercise all six directly-checked contracts (`artifact-layout`, `decision-record`, `completion-report`, `testing-doc`, `trace-schema`, `agent-library-schema`); call out any left uncovered (FR-009).
- `R1-S17` — Justify `check-conformance.py`'s shell-out as a runtime read ("a read is not a write"), not via D57 (which is about not patching another extension's installer-overwritten source).
- `R1-S19` — `check-conformance.py` carries a lightweight self-test asserting the contract section headers/field names it parses still exist in each `docs/contracts/*.md`.
- `R1-S20` — FR-016's all-six `docs/90` close-out is the final serialized step, gated on the other five owner piles completing (not a flat parallel wave).
- `R1-S21` — Automate SC-005 determinism as a double-run byte-diff assertion in `extensions/workforce/test/run.sh` (from manual to code-verified).
- `R1-S22` — Static guard: `check-conformance.py` has no source-level `import` of the validators (only `subprocess`/shell-out), enforcing FR-008 no-duplication as a caught regression.
- `R1-S23` — Order the `.specify/extensions.yml` mirror resync strictly before `extensions/git/test/run.sh`'s guard runs, so the guard never validates a stale mirror.
- `R1-S24` — `bootstrap.sh` handles mid-install (step-3) failure with trap-based `EXIT` temp cleanup + non-zero exit propagation and a named target-repo state.
