# Graphify Context — 001-council-extension

_Generated 2026-07-09 from `graphify-out/graph.json` (638 nodes, 574 edges, 76 communities, scope: repo). Deterministic extraction (`graphify update`, no LLM); nodes are doc/section + shell symbols. Disposable — regenerated by `before_*` hooks; stale after large merges._

## Graph scope
- Repo graph: `graphify-out/graph.json` · This run used: **repo**.
- **Honest caveat:** this repo is markdown-dominant (docs, contracts, skills, templates). The extractor indexes document sections and shell symbols; there are few code-import edges to trace, and `explain` on config files (`extension.yml`) returns nothing. The council extension's real dependencies are **artifact-flow** — which command reads/writes which `council/` artifact — and those are defined by `docs/contracts/`, not by the graph. Grounding below therefore leans on the **exemplar extension's structure**, which the graph confirms lives entirely under `extensions/graphify/`.

## Relevant existing modules (the council extension mirrors / touches these)
- `extensions/graphify/` — **the exemplar**, the only existing speckit extension. Layout to follow: `install.sh` + `uninstall.sh`; `extension/extension.yml` (manifest + hooks); `extension/commands/*.md` (command provenance); `skills/<name>/SKILL.md` (the user-invocable skills); `extension/README.md`; `extension/graphify-config.yml`.
- `.claude/skills/speckit-*` — stock spec-kit command skills; the install-target namespace and the front-matter shape for the council's three commands.
- `.specify/extensions.yml` — the hook/registration registry (graphify registered `before_plan|tasks|implement` here).
- `docs/contracts/` — the normative schemas the council artifacts MUST conform to: `artifact-layout.md` (the `council/` subtree), `decision-record.md`, `trace-schema.md`, `profile-schema.md`.
- Design + spec sources: `docs/10-COUNCIL-EXTENSION-SPEC.md`, `specs/001-council-extension/spec.md`.

## Blast radius (per anchor)
- **council extension (all new)** — `extensions/council/` does not exist yet.
  - reads: `plan.md`, `spec.md`, `graphify-out/graph.json` (via the member query tool), the contracts.
  - depended on by: `/speckit-tasks` (reads the decision record's *Carried Constraints*); the human gate.
  - follow the pattern in: `extensions/graphify/` (packaging) and `.claude/skills/speckit-plan/SKILL.md` + `extensions/graphify/skills/speckit-implement-parallel/SKILL.md` (skill shape + subagent dispatch).

## Shared / mutable files (collision watch)
> Tasks touching any of these serialize — never two in one parallel wave.
- The council extension's own `extension/extension.yml` + `install.sh` — the manifest listing all three commands; any task that registers a command touches it.
- `.specify/extensions.yml` — shared registry (append/merge only). *(The council registers no `before_*` hooks — it is command-invoked — so it may not touch this at all.)*
- `.claude/skills/` — install destination; each command skill is its own directory, so distinct commands are disjoint.
- Per-feature `council/decision-record.md` — single append-only writer (triage + gate); never co-written.

## Patterns to follow
- **Packaging**: mirror `extensions/graphify/install.sh` — idempotent copy of `extension/` → `.specify/extensions/<id>/`, skills → `.claude/skills/`. The council needs no `before_*` hook YAML-merge (it is command-invoked), so its installer is a *simpler* graphify installer.
- **Skill shape**: each command is a `SKILL.md` with spec-kit front-matter (`name`, `description`, `argument-hint`, `compatibility`, `metadata.author`, `user-invocable: true`) + body (Pre-Execution hook checks · Outline · Mandatory Post-Execution Hooks · Completion Report).
- **Session offload**: deck-prep, members, and chairman run as subagents (Agent tool), mirroring `speckit-implement-parallel`'s "one subagent per unit, dispatched in one turn" for the parallel opinion stage — the wave-review discipline maps onto stage barriers.
- **Trace append**: every session appends one line to `traces.jsonl` per `trace-schema.md`; the council extension owns this for its own sessions.
