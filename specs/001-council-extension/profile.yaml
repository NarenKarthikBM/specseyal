# Autonomy profile — 001-council-extension
# Both gates human: the default posture (D9). See docs/contracts/profile-schema.md.
schema_version: "1.0"
feature: "001-council-extension"

full_auto: false

gates:
  council:
    mode: human
    max_rounds: 1
    reopen_tier: auto
  workforce:
    mode: human
