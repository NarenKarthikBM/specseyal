---
feature: 004-testing-completion
phase: testing
executed: none
---

## Coverage map

| id | approach | grounding | evidence-source | status |
|---|---|---|---|---|
| FR-001 | Inspect completion-report.md's own authorship and artifact count: confirm it was authored by the main-thread orchestrator (no dispatched model session) and that the `complete` phase produced no artifact besides this file. | Key results — explicit self-citation: authored entirely by the main-thread orchestrator, no new model role (FR-001). | report-claimed | covered |
| FR-002 | Contract-validate completion-report.md against docs/contracts/completion-report.md via the testing-ext validator. | Integration status — testing-ext self-tests (43 passed, 0 failed) include the two-golden completion-report validation with 6 adversarial rejections. | report-claimed | covered |
| FR-003 | Inspect the frontmatter `status:` key and confirm its value is one of success/partial/failed; cross-check against the same validator's enum enforcement. | This file's own frontmatter (`status: success`) plus Integration status's completion-report golden-test coverage. | report-claimed | covered |
| FR-004 | Diff the report's heading sequence against the contract's required core-section list (status, Implementation Complete summary, Completed N/N, Partial/Degraded, Failed, Integration status, Key results) and confirm any milestone-close material sits demarcated outside that set. | The report's own structure (an explicit HTML-comment boundary before the appendix) plus Integration status's two-golden test — appendix-bearing and appendix-free both validate (SC-005). | report-claimed | covered |
| FR-005 | Manually verify by reading the report's own claim that this file's bytes, unreshaped, serve as the D19 `complete`-event `artifact.body`, idempotent on `artifact.sha256`. | Milestone-close context (appendix) — explicit "This file IS the D19 `phase.completed` `artifact.body` (FR-005)" statement. | report-claimed | covered |
| FR-006 | Confirm the testing phase is dispatched as an isolated Sonnet session distinct from the main thread, with context-in exactly completion-report.md + spec.md, writing only testing.md — self-evidenced by this tester run itself. | Key results — "the doc-only testing phase (a separate-session Sonnet tester, executed: none)". | report-claimed | covered |
| FR-007 | Contract-validate testing.md against docs/contracts/testing-doc.md via the testing-ext validator. | Integration status — a golden testing.md 27-id bijection test with a real GAP row and 7 rejections, part of the 43/0 self-test run. | report-claimed | covered |
| FR-008 | Run the SC/FR coverage validator against this testing.md and confirm one row per id, each with a verification approach grounded in Integration-status claims, and any uncovered id marked GAP. | Integration status frames itself as the grounding source ("testing.md's coverage map grounds its verification approaches here") and cites the 27-id validator. | report-claimed | covered |
| FR-009 | Inspect testing.md's frontmatter for `executed: none` and confirm the document contains no test-execution commands or self-generated pass/fail verdicts, only citations to existing evidence. | Key results — "executed: none" framing of the testing phase. | report-claimed | covered |
| FR-010 | Inspect testing.md for the mandated "Verified by reading vs. would-execute in v2" section and confirm it legibly separates what was verified-by-reading from what a v2 executor would run. | Key results' `executed: none` framing plus the report's repeated doc-only-boundary emphasis. | report-claimed | covered |
| FR-011 | Inspect the appended traces.jsonl record post-dispatch for `role: tester`, model `claude-sonnet-5`, exactly one record, and a `context_in` field naming completion-report.md + spec.md. | Decisions & log F5 — the tester-trace `context_in` field was schema-amended (trace-schema 1.3/D72) before this session, so "M4's first live tester trace is schema-conforming from the start"; Key results' "separate-session Sonnet tester". | report-claimed | covered |
| FR-012 | Run `git log` and confirm `complete(004-testing-completion)` and `testing(004-testing-completion)` phase-tagged commits exist at their respective boundaries. | Integration status — git-ext self-tests §7, "SC-008 seam survival (`complete(<id>)`/`testing(<id>)` commits + hook routing)". | report-claimed | covered |
| FR-013 | Inspect git-ext's own source (not an installed copy) for the testing-phase enum/hook addition, and run git-ext's §7 reinstall-survival regression (plain reinstall + foreign-extension reinstall). | Integration status — git-ext self-tests §7 surviving both a git-ext reinstall and a foreign-extension reinstall (R1-S08/S15). Note: F3 (Decisions & log) flags a latent staleness in `install.sh`'s PyYAML-less manual-fallback block, but the report states the tested auto-merge path (T008/T018) is unaffected — a disclosed, booked (I-23) nuance, not a failure of the exercised path. | report-claimed | covered |
| FR-014 | Inspect the per-wave `verify-gate workforce` audit lines for waves 2–6 and confirm each cleared the freshness check via the checkbox-delta branch with zero hand-assistance markers. | Integration status — "SC-010 proven live on this run (the headline)... each emitting the durable S09 audit line". | report-claimed | covered |
| FR-015 | Run git-ext self-test §6 (forward-flip PASS + reverse-flip/unpaired/edited-text/scope-guard BLOCK cases) and confirm the reinstall event in Install state re-applied the fix byte-identically. | Integration status — git-ext self-tests §6, the I-17 checkbox-delta survival regression; Install state — "git-ext was reinstalled once carrying the bundled I-17 + seam fix". | report-claimed | covered |
| FR-016 | Inspect the wave/task table and pre-flight log, confirming T003–T008 (the I-17 fix + regressions) executed and passed strictly before Wave 1's T001, i.e. in the pre-flight slot. | Overview + Completed (19/19) table — "pre-flight (not a wave) \| T003–T008"; Milestone-close context's refuse-then-release narrative. | report-claimed | covered |
| FR-017 | — | — | — | **GAP** |
| SC-001 | Contract-validate completion-report.md and cite the two-golden test plus 6 adversarial rejections as automated confirmation. | Key results — explicit self-citation: "This report is itself the evidence for SC-001". | report-claimed | covered |
| SC-002 | Run the testing-doc contract validator against this testing.md, confirming frontmatter shape, the two required `##` headings in order, and the five-column table structure. | Integration status — golden testing.md 27-id bijection + GAP row + 7 rejections. | report-claimed | covered |
| SC-003 | Confirm the tester's reply to main is a status-only line with no excerpted body from either input file, and that the appended trace's `context_in` names the files without embedding their content. | Key results — "separate-session Sonnet tester". | report-claimed | covered |
| SC-004 | Run the SC/FR coverage validator against this testing.md and confirm exactly 27 rows (10 SC + 17 FR), zero missing/duplicated ids, and that GAP rows are genuine rather than fabricated as covered. | Integration status — "exactly 27 ids = 10 SC + 17 FR; the naive grep's 28 is asserted wrong; fails-on-any-gap". | report-claimed | covered |
| SC-005 | Contract-validate the report's core section set independent of any appendix. | Integration status — "the two-golden completion-report validation (appendix-bearing + appendix-free both validate — SC-005)". | report-claimed | covered |
| SC-006 | Run `git log --oneline` and confirm both the `complete(<id>)` and `testing(<id>)` phase-tagged commits exist. | Integration status — git-ext self-tests §7 exercising "`complete(<id>)`/`testing(<id>)` commits + hook routing". | report-claimed | covered |
| SC-007 | Grep traces.jsonl to confirm the only net-new role across both phases is `role: tester` (Sonnet), the `complete` phase's own trace carries no dispatched model call, and confirm `ANTHROPIC_API_KEY` is unset in the live environment. | Key results — "no new model role (FR-001)"; Observability — "`ANTHROPIC_API_KEY` is unset everywhere (D28, subscription-only)". | report-claimed | covered |
| SC-008 | Run git-ext self-test §7 and confirm the commit path fires correctly after both a git-ext reinstall and a foreign-extension reinstall. | Integration status — explicit "SC-008 seam survival... both surviving a git-ext reinstall AND a foreign-extension reinstall (R1-S08/S15)". | report-claimed | covered |
| SC-009 | Confirm completion-report.md validates (SC-001) and this testing.md validates (SC-002), and both land under phase-tagged commits (SC-006); note SC-009's full closure is self-referential — it completes only once this document itself is validated and `testing(<id>)`-committed after this session returns. | Key results — explicit "This report is itself the evidence for SC-001 and (with testing.md) SC-009 — the M4 exit". | report-claimed | covered |
| SC-010 | Inspect the per-wave `verify-gate` audit trail (waves 2–6) against the drifted tasks.md SHA sequence (bound `9601bcd`; checkbox advances 7→8→11→17→18) and confirm the pre-flight REFUSE-until-T008 → RELEASE → wave-2+ PASS-unassisted narrative holds end to end. | Integration status — "SC-010 proven live on this run (the headline)", the report's single most detailed and load-bearing claim. | report-claimed | covered |

## Verified by reading vs. would-execute in v2

All 26 `covered` rows rest on `report-claimed` evidence — `completion-report.md`'s prose alone — and none is `log-verified`: nothing in the report read as thin, generic, or too convenient enough to warrant the lazy `implement.log.md` cross-check (R1-S05), so that file was never opened this session. A v2, test-executing tester would replace nearly every row's *reading* with a *running*: it would invoke `sh extensions/testing/test/run.sh` and `sh extensions/git/test/run.sh` itself and check live exit codes/counts rather than trust the report's stated 43/0 and 46/0; run `git log` directly to observe the `complete(<id>)`/`testing(<id>)` tags rather than read the claim they exist; re-drive an actual wave-2+ implement pass against a freshly-drifted `tasks.md` and watch the `verify-gate workforce` freshness check pass live rather than read the narrated S09 audit-line description (SC-010/FR-014); invoke the SC/FR coverage validator against this very file and the git-ext reinstall + foreign-extension-reinstall regressions rather than assume they'd pass by resemblance to the described golden fixtures; and grep the live environment for `ANTHROPIC_API_KEY` rather than read the Observability paragraph's assertion that it's unset.

One real gap: **FR-017** ("the fix design is plan-level and MUST be council-reviewed as gate-integrity") is marked `**GAP**`, not `covered`. Neither `completion-report.md` nor `spec.md` evidences that the council review actually occurred or what it concluded — `spec.md`'s D68 note only records the *ruling that it should happen*, and the completion report (scoped to `tasks.md` + `implement.log.md`, i.e. the implement phase) is silent on council activity throughout. That evidence, if it exists, lives in `specs/004-testing-completion/council/`, which this session's context-in boundary correctly excludes — so honesty requires flagging the gap rather than inferring coverage from an out-of-scope phase. Separately, FR-013 stays `covered` but carries a disclosed nuance (F3): the git-ext testing-phase seam is reinstall-survival-tested on its exercised auto-merge path, but the report itself flags a latent, booked (I-23) staleness in `install.sh`'s untested PyYAML-less manual-fallback path — worth a reader's attention even though it doesn't unseat the requirement as evidenced.
