# Suggestions — Round 1

**Feature**: `004-testing-completion`
**Prepared by**: Session B — chairman (Opus, xhigh, D18)
**Reads**: `round-1/opinions/{A..E}.md`, `round-1/opinions/peer/consolidated.md` (chairman-only inputs; never quoted verbatim below)

> Format: markdown v1 (D15). This is the **compression boundary** (`data-model.md`) — the only file under `council/` the main thread reads (FR-007), and the sole input to `/speckit-council-triage`. Written once as `round-1/suggestions.md` and never overwritten. Standard-tier round: stage-2 input is one consolidated peer critique; its endorsements/challenges are folded into the rows below by member letter, and its one genuinely new point is sourced `peer`.

---

**Verdict:** 1 blocking · 12 strong · 9 consider

---

## Suggestions

| ID | Class | Suggestion | Sources | Target |
|----|-------|-----------|---------|--------|
| R1-S01 | blocking | Add a mechanical mechanism that pins the I-17 fix (verify-gate.sh edit + git-ext reinstall + survival regression) to wave 1 / pre-wave — encode it as a zero-inbound-dependency wave-1 node in `/speckit-tasks-graph`'s Execution Waves DAG that `implement-parallel` consumes, or run it outside the tasks.md wave loop entirely — rather than asserting FR-016/SC-010 as prose the not-yet-generated tasks.md may violate. All five lenses converged independently; the graph confirms the wave-DAG generator has no signal to auto-place it (verify-gate.sh degree 4, no path to implement-parallel), so a misordered DAG reproduces the exact wave-2 hard-block the fix removes. | A, B, C, D, E | plan.md §2.3 |
| R1-S02 | strong | Have `/speckit-complete` explicitly re-read tasks.md and implement.log.md from disk on every invocation instead of assuming they are "already in main's context — no dispatch"; the context-retention assumption trades away the resumability principle (every phase resumable from artifacts alone) and can silently break across a long multi-wave run, a compaction, or a resumed session. | A | plan.md §1.2 |
| R1-S03 | strong | Add a mechanical SC/FR-coverage completeness check — grep every `SC-\d+`/`FR-\d+` ID from spec.md and assert each appears in testing.md's coverage map, failing contract validation on any gap — so SC-004's "100% mapped" is code-enforced, not prompt-enforced. Precedented by 003's categorization-completeness validator. | A, B, C, D | plan.md §1.3 |
| R1-S04 | strong | Add a regression fixture for the "task added/removed" diff shape (unpaired insertion/deletion) that §2.2's own prose names as a BLOCK case — the named suite tests only two same-line-count fixtures (checkbox flip, edited line), leaving this structurally distinct shape untested before the classifier is trusted fail-closed on it. | A | plan.md §2.3 |
| R1-S05 | strong | Name (and, if possible, mitigate) the tester's second-hand-evidence failure mode: its context-in is completion-report.md + spec.md only — never implement.log.md or source — so a report that over-claims or mistranscribes an implement-time result propagates into testing.md's coverage map with no independent cross-check. Distinct from the SC-004 ID-completeness gap; this concerns the factual fidelity of the one artifact the tester trusts. | B | plan.md §1.3 |
| R1-S06 | strong | Reconcile risk-register R2's mitigation ("the one-trace-record structure makes a violation auditable after the fact") with the actual trace fields (role/model/agent_id/skills/elevated_grants — no context-in/files-read field): either add a context-in field to the tester's trace so a context-hygiene violation is detectable after the fact, or soften R2's claim. | B | plan.md §1.3 |
| R1-S07 | strong | Specify the failure handling of the two new commit hooks (after_complete/after_testing firing commit.sh with a new phase tag): make an errored or empty invocation phase-halting, symmetric with complete's own "malformed/absent report leaves complete INCOMPLETE" guarantee, so SC-006/SC-009's "both phases leave a commit" cannot read as satisfied on a silent no-op. | B | plan.md §1.4 |
| R1-S08 | strong | Spell out the SC-008 testing-seam reinstall-survival regression's concrete fixture and assertions at the same detail as the I-17/SC-010 regression (currently only "extensions/git/test/run.sh §3 model", L134) — e.g. dispatch fixture `/speckit-complete` + `/speckit-testing`, assert two phase-tagged commits exist, then re-assert after a git-ext reinstall and after a foreign-extension reinstall. SC-006 and FR-013 both ride on this test. (The "§3 model" citation is itself traceable to a real named section — inline the assertions; the reference is not broken.) | D | plan.md §1.4 |
| R1-S09 | strong | Emit a durable audit signal — a log/trace line fired specifically when the new checkbox-delta classification branch (not the pre-existing exact-match fast path) produces the workforce-gate PASS — so SC-010's "zero hand assistance" (a process claim a successful wave 2+ alone cannot distinguish from a human working around the gate) leaves independently-auditable evidence after the M4 dogfood run. | D | plan.md §2.2 |
| R1-S10 | strong | Commit both an appendix-bearing and an appendix-free completion-report.md to the golden set, each independently contract-validated, so SC-005's with/without-appendix claim has a genuine two-branch test — the Testing field (L24) currently names only a single golden report. | D | plan.md Technical Context (Testing) |
| R1-S11 | strong | Add to §2.3 the one-sentence argument that makes the wave-1 requirement achievable rather than circular: verify-gate.sh's first firing (before wave 1) checks a pristine, just-bound tasks.md and passes under the pre-fix script regardless, so the staleness defect is triggered only by wave 1's own commit — leaving all of wave 1 as a safe build/install/prove window. The plan asserts the wave-1 MUST but never states why it is satisfiable. | E | plan.md §2.3 |
| R1-S12 | strong | Resolve the standing "wave 1 (or pre-wave)" hedge to one concrete mechanism, preferring "pre-wave" — a step run outside the tasks.md wave loop (e.g. folded into implement-parallel's bootstrap ahead of before_implement) — which sidesteps the wave-DAG-authoring risk of R1-S01 entirely and is strictly safer than "wave 1," an ordinary task exposed to the same scheduling risk as any other. | E | plan.md §2.3 |
| R1-S13 | strong | State explicitly that Concern 1's git-ext edits (the commit.sh testing-enum entry + the two extension.yml hooks) land in the same wave-1/pre-wave git-ext reinstall as Concern 2's fix — they currently carry no stated wave placement despite sharing git-ext's single install.sh, and bundling them costs nothing and avoids reinstalling the extension twice. | E | plan.md §2.3 |
| R1-S14 | consider | Confirm the checkbox-flip direction asymmetry is intentional — a reverse flip (`[x]`→`[ ]`, e.g. a human correcting a wrongly-marked-done task) BLOCKs like any content edit — and name the recovery path (currently only a full re-gate, since the I-16 bootstrap-escape was ruled out). This direction invariant is load-bearing (see R1-S18's contest). | A | plan.md §2.2 |
| R1-S15 | consider | Ensure the I-17 rule is restated direction-explicitly and with the "foreign-extension reinstall" clause wherever standard-tier readers trust the compressed text alone; plan.md itself (L148 "advanced from unchecked to checked"; L162 "git-ext reinstall AND a foreign-extension reinstall") is already precise, so this is a wording-parity confirmation, not a design change. (Peer contest: the "direction-agnostic" reading was an overread — the arrow encodes direction; only the dropped foreign-extension clause is the valid part.) | B | plan.md §2.2 |
| R1-S16 | consider | Ratify the plan's two-separate-contract-files position (completion-report.md, testing-doc.md) over merging: a combined file would be the first exception to the existing 1:1 schema-per-file convention across the seven docs/contracts/ siblings, and the pointer-based cross-reference (D46 rule 3) already gives cheap coupling without co-location. | C | plan.md §Project Structure (Contract-file count) |
| R1-S17 | consider | Ratify `/speckit-complete` as a real command over orchestrator-inline authorship: beyond the hook-boundary argument (after_complete/FR-012), every other pipeline phase is already its own `/speckit-*` command, so inline authorship would be the sole exception to a repo-wide one-command-per-phase convention. | C | plan.md §1.2 |
| R1-S18 | consider | Evaluate — but note the contest against — replacing the checkbox-delta "git diff + per-line regex" branch with a canonicalized-checkbox-hash comparison (normalize every marker to one placeholder, hash at bind and check time, PASS iff equal): it is simpler and reuses the existing SHA idiom, but peer review found a real correctness gap — normalizing both `[ ]` and `[x]` to one placeholder makes the hash direction-blind, so a reverse flip (`[x]`→`[ ]`) would PASS, violating plan.md L148's "advanced from unchecked to checked" invariant (see R1-S14). Any adopted hash variant must preserve the direction asymmetry. | C | plan.md §2.2 |
| R1-S19 | consider | Confirm that extensions/testing/test/run.sh's golden-fixture assertions are derived from docs/contracts/*.md's own section list rather than hand-maintained in parallel prose, so the contract docs and their validator cannot silently diverge — a compounding cost since 004 declares these contracts pipeline-wide (every future feature's complete/testing phase validates against them). | D | plan.md Technical Context (Testing) |
| R1-S20 | consider | Reconcile the two sequencing shapes the plan presents for the git-ext file pair: risk-register R4 reads staged/sequential ("the I-17 fix gets priority over the later-firing testing-seam hooks") while the §4 collision watch reads simultaneous/bundled ("redeployed together by one install.sh") — pick one so a reader cannot land on either. | E | plan.md §2.3 |
| R1-S21 | consider | Name a recovery path for a wave-DAG scheduling miss — the I-17-fix task landing in wave 2+ despite the guarantee — a failure mode distinct from the classifier's own fail-closed edge cases (R3): e.g. manually reorder tasks.md and resume. Mooted if R1-S01's mechanical enforcement is adopted. | E | plan.md §2.3 |
| R1-S22 | consider | Confirm the deliberate omission of docs/contracts/artifact-layout.md from the shared/mutable "collision watch" set, even though the Constitution Check (Principle I) and both Project Structure diagrams commit a real §6-ownership-table edit (two new writer rows) to it in the same build — likely low-risk additive rows, but it is a file the plan itself edits that is absent from the set framed as needing serialization. | peer | plan.md §Project Structure |

---

## Chairman's note

One blocking finding carries the round: all five lenses converged independently (5/5, peer-confirmed as the round's strongest signal) that the I-17 fix's FR-016/SC-010 "wave 1 (or pre-wave)" placement has no mechanical enforcement, so a misordered wave-DAG would reproduce the exact wave-2 hard-block the fix removes (R1-S01). Both concerns are otherwise structurally sound — the strong tier clusters on making load-bearing guarantees mechanically checkable (SC-004 coverage, SC-008 seam regression, SC-010 auditability, `/speckit-complete` resumability) and on the tester's second-hand evidentiary base, while the two POSITIONs the plan defends (`/speckit-complete`-as-command, two contract files) drew ratifications rather than rejections. Members split only on the simplicity proposal to swap the diff-classifier for a canonicalized hash, which peer review showed would silently erase the plan's check-direction invariant (R1-S18, contested).

### Chairman delta check — 2026-07-12T17:20:29Z

- R1-S01 — RESOLVED — §2.3 relocates the I-17 fix out of the `tasks.md` wave loop entirely — invoking the D-row invariant "machinery that gates the waves is never itself a wave" so it is no longer a wave-DAG node that could be misordered — and makes provisioning mechanical via `implement-parallel`'s pre-flight, which refuses to enter wave 1 until the single bundled git-ext change, reinstall, and survival-regression report are done, so the wave-2 hard-block the finding named cannot recur.

**Delta verdict:** all clear, ready for the gate
