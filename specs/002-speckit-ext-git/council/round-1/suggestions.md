# Suggestions — Round 1

**Feature**: `002-speckit-ext-git`
**Prepared by**: Chairman (Opus, xhigh — D18)
**Reads**: `round-1/opinions/*.md`, `round-1/opinions/peer/*.md` (chairman-only inputs; consolidated below, never quoted verbatim)

> This file is the compression boundary (`data-model.md`, principle 1): the only file under `council/` the main thread and `/speckit-council-triage` read. `opinions/` stays chairman-only for the life of the feature. Written once as `round-1/suggestions.md`, never overwritten.

---

**Verdict:** 6 blocking · 12 strong · 11 consider

---

## Suggestions

| ID | Class | Suggestion | Sources | Target |
|----|-------|-----------|---------|--------|
| R1-S01 | blocking | Set the branch/commit/gate-verify hooks to `optional: false` (not `optional: true` copied from graphify's schema): the stock dispatch prose only *announces* an `optional: true` hook, never auto-invokes it, so `after_specify` and the `before_tasks`/`before_implement` hard-blocks never fire unprompted — exactly the warn-and-override `spec.md` FR-009 rejected on 2026-07-09, falsifying SC-001 and FR-009. Round's convergent headline (raised independently on 3 lenses, all peers endorse). | A, B, C, D, E | plan.md §F |
| R1-S02 | blocking | Add an explicit "if the invoked hook exits non-zero, STOP — do not proceed" clause to the Pre-Execution Checks of `speckit-tasks`/`speckit-implement` (the shared dispatch prose says "wait for it to finish," never "abort on failure"). This is a *second, independent* hard-block failure from S01 — flipping the flag makes the hook run but still not block; both patches must land, or the second is silently dropped as "already covered." | A, D | plan.md §B |
| R1-S03 | blocking | Correct the false Project-Structure/Dependency-impact manifest ("the only existing file touched is `.specify/extensions.yml`… no source file mutated"): the design requires editing four existing skills — `speckit-council-approve` (SHA-record) and `speckit-implement-parallel` (a per-wave commit step that does not exist there today) for the R1 seam, plus `speckit-tasks`/`speckit-implement` for S02 — and regenerating `graphify-context.md`'s shared/mutable list. Since `/speckit-tasks` keys off this manifest, the plan's own top-flagged risk (R1) can ship with no task at all. | A, B, D, E | plan.md §Dependency/graph impact |
| R1-S04 | blocking | Resolve the cross-extension coupling the R1 seam actually creates: those two call sites live in council/graphify-owned skills whose installers `rm -rf` + `cp -R` unconditionally on every reinstall, so any edit to the installed copies is silently wiped and editing their source trees is an unnamed ownership violation. Commit to a reinstall-surviving wiring (e.g. register the SHA-record as an `after_council_approve` hook per council's own `before_council` pattern; note no stock per-wave hook vocabulary exists for `implement-parallel`) and document the coupling in R1. | B, C, D | plan.md §D |
| R1-S05 | blocking | Close the working-tree blind spot in the gate binding: `speckit.git.sha`/`verify-gate` compare committed-HEAD SHAs only, so a human who hand-edits an approved `plan.md` and runs `/speckit-tasks` *without committing* sails through the hard-block (false-pass) — the single most direct threat FR-009 exists to catch. This is independent of S01/S02 (all three must be fixed). Specify a commit-before-read ordering or make `verify-gate` working-tree-aware. | A, B | plan.md §D |
| R1-S06 | blocking | Fix the wave-commit ordering hole (own risk-register row, an ordering hazard distinct from R1's omission hazard): the shipped `implement-parallel` loop marks each task `[X]` in `tasks.md` *before* the proposed per-wave commit, so an interruption in that window leaves a resuming session unable to tell "wave done, commit missing" from "wave partial, discard" — breaking NON-NEGOTIABLE principle III and SC-006. Commit before marking `[X]`, or make the marker write and the commit atomic. | A, B, D, E | plan.md §F |
| R1-S07 | strong | Rewrite §F's composition story and pin real ordering: "the git commit hook runs after graphify's context hook at a shared boundary" describes a scenario that cannot occur (every git commit hook is `after_*`, graphify is exclusively `before_*`). The only real shared keys are `before_tasks`/`before_implement`, where git's `verify-gate` meets graphify's context-gen, and the `priority` field is dead code (written as a static `5`, never read) — so append order lands verify-gate *after* graphify's heavy context regeneration. Make the installer insert verify-gate ahead of graphify at those two keys. | A, B, C, D, E | plan.md §F |
| R1-S08 | strong | Name explicitly, at real severity in the Risk register and Testability Claim, that there is no code-level hook enforcement anywhere in the repo — the `HookExecutor` every skill defers to is referenced 11-13 times and implemented zero times — so even a corrected `optional: false` + stop-on-failure hook is enforced only by an LLM following prose, undercutting the deck's "falsifiable without a model call" claim at the mechanism level. (Members explicitly asked to elevate this above its original low confidence.) | A, B, C, D | plan.md §Testability claim |
| R1-S09 | strong | Fix `data-model.md`'s `GateSHABinding`: the `## Human Gate` section has no `@ <sha>` field today (its fields are reviewer/decision/reviewed) — the existing `@ <sha>` citations live in `## Round N`/R4, written by a different command at a different time and not satisfying FR-008. Add an explicit contract amendment to `## Human Gate` (parallel to FR-001's D51 amendment), or adopt a git-ext-owned binding (see S20); the plan currently reads as reusing a field that does not exist. | A, C | plan.md §D |
| R1-S10 | strong | Require `verify-gate.sh` to fail *closed* as an FR-009 acceptance criterion: its behavior when it cannot parse the `<artifact> @ <sha>` line out of the gate section is unspecified, and an unparseable-but-present section that silently passes is a textbook fail-open that compounds S01/S05. Add a format-version guard against future gate-table edits. | B, E | plan.md §D |
| R1-S11 | strong | Pin `commit.sh`'s staging scope explicitly — the contract's "stages the feature's changed paths" is ambiguous between "everything currently dirty" (needed for wave commits) and "only this feature's known paths" (needed so a phase commit's diff is trustworthy, SC-003). Every `after_*` commit fires unattended, so a repo-wide `git add -A` would sweep a stray dirty `.env`/credential file into permanent history before anyone looks — never repo-wide; record as a named risk. | A, B, C | plan.md §B |
| R1-S12 | strong | Close the un-backstopped `after_specify` hole: it is the *only* hook that creates the branch, so if it fails to fire every later phase commit lands silently on the base branch (the SC-002 failure the design exists to prevent), with no detection. Fold the idempotent ensure-branch call (already specified for `branch.sh`) into `commit.sh` itself — one self-healing entry point — rather than duplicating a guard in every `after_*` hook. | A, C | plan.md §C |
| R1-S13 | strong | Add atomic/locked NNN allocation and correct R3's analysis: two concurrent `/speckit-specify` runs race the unlocked directory scan (in the skill's inline Outline step 3 — which has no existence check before `mkdir -p`, more exposed than `create-new-feature.sh`), and `branch.sh`'s create-if-absent mitigation then *converts a loud collision into a silent shared-branch merge* rather than preventing it. "Fail loudly if the branch exists" only catches the same-slug sub-case; the likelier same-NNN-different-slug case needs locked allocation. Predates git-ext but git-ext sharpens it; add a concurrency test. | A, B, C, D, E | plan.md §Risk register (R3) |
| R1-S14 | strong | Add `after_analyze` to the hook table — `speckit-analyze` already exists with a working `before_analyze`/`after_analyze` pair, is exercised in this repo's M1 history, and can revise `tasks.md`, yet FR-003 lists `analyze` and the plan omits its commit hook (weakening SC-003). Also state explicitly why `categorize`/`agent-assign`/`complete` are omitted (no skill until M3/M4) rather than silently dropping three of ten FR-003 boundaries. | A | plan.md §B |
| R1-S15 | strong | Budget the installer's hook-merge as its own Phase-1 task with an explicit size/complexity estimate: graphify needed a 193-line embedded-Python YAML merge with a 5-way interpreter fallback and a documented degrade-to-manual-paste failure just to merge 3 uniform entries; git-ext must merge 7 entries across 3 distinct shapes into 5 new keys plus 2 append targets — very likely the largest, most failure-prone file, currently invisible inside the "~5 short POSIX sh scripts" framing. | C, D | plan.md §Cost/complexity estimate |
| R1-S16 | strong | Add "correct the stale `before_specify` assertions" to this feature's scope: `speckit-specify/SKILL.md`'s closing NOTE ("Branch creation is handled by the `before_specify` hook") and `graphify-context.md` line 13 ("002 needs… `before_specify`") both flatly contradict the ratified `after_specify`/D51 design, leaving two authoritative-looking documents pointing a future implementer at the rejected approach. | A, B, D, E | plan.md §Project Structure |
| R1-S17 | strong | Ship a scripted, CI-able test harness (`extensions/git/test/run.sh`) so a "mechanical, zero-AI, deterministic" extension is not validated by manual eyeballing: include isolated unit tests for `branch.sh`'s create-if-absent logic (scratch repo + fabricated `feature.json`, no model call) and a regression case that reinstalls council + graphify and asserts the R1 seam call sites survived — the one failure class (S04) a human quickstart run would never catch. | C, D | plan.md §Testability claim |
| R1-S18 | strong | Make `quickstart.md` actually prove its SCs: add the missing SC-006 interrupt/resume scenario (the second P1 story has zero coverage — kill after wave 2, resume at wave 3, assert git HEAD and `tasks.md` `[X]` markers agree, nothing duplicated/lost); and fix three verification methods that do not test their claim — SC-007's zero-`role:"git"`-traces grep is vacuous (no build-phase session writes traces today), SC-008 checks for the *absence of a `worktree` reference* (and names no file that exists) instead of removing the spike and re-running 1–4, and SC-005 matches commit *counts* rather than asserting per-SHA `merge-base --is-ancestor`. | D | plan.md §Testability claim |
| R1-S19 | consider | Add one line to Rejected Alternatives acknowledging the purpose-built `before_specify` slot in `speckit-specify` step 2 is deliberately left unused, not missed (its own contract already tolerates the branch/dir-name mismatch cited as the rejection reason). Note: the "or switch to `before_specify`" alternative one member floated was rebutted by two others as resurrecting the NNN+slug duplication the plan correctly rejected, D51 having closed the question same-day — `after_specify` stands; triage to formally dispose. | A, C, E | plan.md §Rejected Alternatives |
| R1-S20 | consider | Consider decoupling the gate-SHA binding from a foreign artifact — either a narrow, explicit principle-I exception for recording a purely mechanical derived value (a SHA), or a git-ext-owned gate-bindings artifact written by `speckit.git.commit` as a side effect of a call the gate command already makes — which would dissolve S09 without adding a SHA column to `decision-record.md`'s shape, and shrink R1's gate-recording half. Name explicitly that D-R3's supply/record split is what manufactures that half of R1 (Constitution-adjacent; flagged for triage, not decided here). | C | plan.md §D |
| R1-S21 | consider | Descope proposal (contested — surface for triage disposition): ship gate-SHA binding as "record-and-disclose only" and defer the enforced hard-block to a later version. Rebutted by two peers as reproducing the warn-and-override pattern `spec.md` FR-009 rejected by name on 2026-07-09 — adopting it would require a spec-delta re-litigating that clarification, not a plan-level simplification, and would leave FR-009/SC-004 with nothing to hard-block-test. | B, C, D | plan.md §D |
| R1-S22 | consider | Label the deck §3 / Dependency-impact "graph-grounded" claims about the installer pattern and `.specify/extensions.yml`'s "every phase command" blast radius as engineer assertion, not graph fact: `graphify explain`/`path` return "No node matching" for `.sh`/`.yml` files, so the graph cannot substantiate them (they may still be true — re-derive by reading files). Distinct from S16's stale-*content* finding: this is a tool-coverage gap, not a document bug. | A, C, E | plan.md §Dependency/graph impact |
| R1-S23 | consider | Consider re-verifying gate freshness at each wave boundary for `implement` only: R2's entry-only check has its widest blind spot on precisely the longest, most multi-wave, most interruption-prone phase, so a mid-run edit to `tasks.md`/`assignment.md` rides unchecked for every remaining wave. The plan already scopes continuous re-verification out of v1 — weigh the cost asymmetry (a stale multi-hour run vs. one cheap SHA check per wave). | B | plan.md §Risk register (R2) |
| R1-S24 | consider | In the Phase-4 spike write-up, distinguish git-ext's proposed per-*wave*, shell-scripted `git worktree` from the pre-existing per-*story* `Agent isolation: "worktree"` guardrail already documented in `speckit-implement-parallel` — both are "worktree" in one file/phase, so give the spike a distinct name to keep `implement.log.md`'s recorded outcome from being misattributed to the pre-existing mechanism. | B, C | plan.md §G |
| R1-S25 | consider | Give FR-002's branch-name regex (`^[0-9]{3}-[a-z0-9]+…`) a carve-out for the already-supported `feature_numbering: "timestamp"` mode (`YYYYMMDD-HHMMSS-slug`): dormant in this repo (sequential) but it will break on first install into a timestamp-numbered repo, undermining the "install like graphify anywhere" packaging goal. | B | plan.md §Project Structure |
| R1-S26 | consider | Installer/uninstaller robustness: (a) order `uninstall.sh` so hook-deregistration happens (or is verified) *before* payload removal, or fail hard rather than warn-and-continue when no PyYAML is available — otherwise `extensions.yml` is left referencing deleted commands, and a dangling `optional: false` hook would hard-block every phase; (b) a `flock` on the `extensions.yml` merge was proposed but rebutted by a peer as duplicative of the existing serialize-in-orchestrator-glue discipline — triage to dispose. | B, C | plan.md §A |
| R1-S27 | consider | Consider a lightweight `complete/<spec-id>` tag at the completion commit as the M5 anchor instead of hard-locking `merge.policy` to `no-ff` in `git-config.yml` for a consumer (`platform/`) that does not exist until M5 — a tag serves the same anchor purpose without forcing merge topology and stays cheaply revisable. (Challenges the ratified D51, so lower priority.) | C | plan.md §E |
| R1-S28 | consider | Document two bootstrap/wording self-references: (a) grandfather `002`'s own `## Human Gate` as unbound (it is recorded by `speckit-council-approve` before this very feature adds the SHA-record call), as M1's ff-merge is grandfathered; and (b) disambiguate D51's "as soon as the spec ID is assigned" wording — `after_specify` actually fires at the *end* of specify (after drafting, up-to-3 checklist iterations, and a possible clarify Q&A), not at ID-assignment time. | E | plan.md §C |
| R1-S29 | consider | Testability polish + drift lint: soften SC-002/SC-004's "100%" language to an existence-proof (a single manual run cannot establish it); give quickstart Scenario 2's injection step a literal copy-pasteable command (the only step lacking one); add an `optional:`-value check to the Setup grep, not just a registration check; and add a post-build `before_specify` lint over `speckit-specify/SKILL.md` and any committed `graphify-context.md`, wired into the M2 conformance checker (D50), so the S16 drift class is caught mechanically next time. | A, C, D | plan.md §Testability claim |

---

## Chairman's note

The plan's mechanical-git spine — the branch and commit graph *are* the state, no state file — is sound and well-defended, but the round converged hard (raised independently on three lenses, endorsed by all five peers) on a mechanism-level defect that guts its central guarantees: the hooks that must be non-negotiable are registered `optional: true`, which the dispatch layer only announces rather than auto-invokes, and even when invoked carry no stop-on-failure path — so FR-009's hard-block and SC-001's zero-manual-git are prose-only today, precisely the warn-and-override the spec rejected on 2026-07-09, and this compounds with a verifiably false blast-radius claim (the R1 seam requires editing two other extensions' skills their installers silently overwrite on reinstall). The two live disagreements — reopening `before_specify` (rebutted: D51 closed it same-day, the fix is documentation-only) and descoping the enforced hard-block to record-and-disclose (rebutted: it reopens a ratified spec clarification) — are surfaced for triage to dispose, not pre-empted here. Grounding was full this round (`graph.json` present, though every member independently found it low-signal for the `.sh`/`.yml` files this plan turns on), so no reduced-grounding banner applies.
