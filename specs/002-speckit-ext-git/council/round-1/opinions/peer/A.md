---
lens: correctness
---

I re-verified the load-bearing factual claims in all four opinions directly against the repo (not just against each other) before ranking them: `diff`-ing installed vs. source skill files, `grep`-ing the actual hook-dispatch prose in `speckit-tasks/SKILL.md` and 15 other skills, reading `create-new-feature.sh` and `speckit-specify/SKILL.md`'s inline Outline step 3 side by side, grepping `priority` and `HookExecutor` repo-wide, reading graphify's live `extension.yml`, and re-reading `quickstart.md` in full myself. Every specific, checkable claim I tested from B, C, D, and E came back **true**. That materially changes how I weight this round: this isn't four plausible-sounding reviews to arbitrate on tone, it's four reviews where the disagreements are mostly about framing and priority, not about facts.

**Ranking.** D and E are the strongest opinions this round, and not narrowly. Both independently — Stage 1, no visibility into each other — converged on the identical, mechanism-level defect: the hooks this plan needs to be non-negotiable (`after_specify`, `verify-gate` at `before_tasks`/`before_implement`) are registered `optional: true`, and under the actual, already-shipped hook-dispatch prose, `optional: true` means *announced only*; only `optional: false` gets "you MUST actually invoke the hook and wait for it to finish before continuing" (confirmed verbatim in `speckit-tasks/SKILL.md` L33-45, and identically in `speckit-specify`, `speckit-plan`, `speckit-clarify`, `speckit-implement`, etc.). Two reviewers landing on the same bug from different lenses (testability, sequencing), independently, is about as strong a correctness signal as this council format can produce — it should be treated as fact-checked twice, not once. I also checked graphify's own live `extension.yml`: its three hooks are `optional: true` too — and correctly so, since skipping them only degrades grounding. That contrast is worth stating explicitly: git-ext appears to have copied graphify's `optional: true` value wholesale (plan.md §F says exactly this: "graphify's schema... so they compose") without re-deriving whether the *semantics* graphify earned that value with actually apply to a hard-block gate. They don't.

B is comparably strong on a different axis: the single most novel finding of the round (installers unconditionally `rm -rf` + overwrite skill directories on every reinstall) is verified — I `diff`'d both pairs myself and both installed skills are byte-identical to their extension-source counterparts, so B's "silently reverted integration point" concern is a real mechanism, not a hypothetical. B's TOCTOU race in feature-number allocation also checks out against `create-new-feature.sh` (see below, and see New-tagged suggestion — I extended this one).

C is the softest opinion of the four, not because anything in it is factually wrong (I checked C's `graphify explain` claims and they hold — `.specify/extensions.yml` really does return a single degree-1 README-mention node, not a rich blast-radius edge set), but because most of C's suggestions are cost/scope judgment calls appropriate to a simplicity lens (installer complexity undercosted, SHA-enforcement possibly gold-plating, `--no-ff` lock-in) rather than "the plan asserts something false" claims. C's two genuinely correctness-shaped findings — the graph-grounding gap and the hook-composition contradiction — are exactly the two that E also reaches independently, which is itself informative: where C and E agree, it's solid; C's lower-confidence, more editorial calls (gold-plating, D-R3 tradeoff-obscured) are reasonable opinions but shouldn't be weighted the same as a verified fact.

## Suggestions

- [correctness] Endorse D and E: the `optional: true` hook-dispatch finding is this round's single highest-priority item. I independently re-verified it against `speckit-tasks/SKILL.md` (and confirmed the identical mandatory/optional dispatch split repo-wide) — under `optional: true`, none of `after_specify`'s branch/commit, `before_tasks`/`before_implement`'s hard-block, or the `after_*` phase commits are actually guaranteed to run; they're merely printed as a suggestion. This directly falsifies SC-001 ("no manual git needed") and FR-009 (hard-block). Every hook in `contracts/commands.md`'s table needs `optional: false` except possibly none — there's no hook in this extension whose whole purpose isn't to be guaranteed. (confidence: high)
- [correctness] Endorse B, Endorse E: plan.md's Dependency/graph impact claim — "The **only** existing file touched is `.specify/extensions.yml`... No source file is mutated" — is false, confirmed two independent ways: B's file-diff shows the two skills needing edits (`speckit-council-approve`, `speckit-implement-parallel`) are owned by other extensions with unconditional reinstall-overwrite; E's grep shows `plan.md` itself never mentions either file (only `contracts/commands.md`'s "R1 seam" section does). These are complementary evidence for the same false claim, not duplicate findings — the chairman should fold both into one triage item citing both letters, since together they're stronger than either alone. (confidence: high)
- [correctness] New: B's TOCTOU suggestion (the `create-new-feature.sh` race on `get_highest_from_specs()`) targets the wrong file, or at least an incomplete one. E's own citation-accuracy finding is that `speckit-specify/SKILL.md` never actually calls that script — I checked, and it's right: the live path is inline prose (Outline step 3, L88-97): "next available 3-digit number after scanning existing directories in `specs/`" → `mkdir -p SPECIFY_FEATURE_DIRECTORY`, with **no existence check or error at all** before the `mkdir -p` (unlike the shell script, which at least errors "already exists" unless `--allow-existing-branch`). The live path has the same TOCTOU shape B found, but is *more* exposed: no collision error, and a wider race window because an LLM session — not a fast shell script — sits between the scan and the `mkdir`. B's fix needs to target the inline Outline-step-3 prose (or wherever the real numbering logic ends up), not the legacy script, or the fix will land on a codepath nothing exercises. (confidence: high)
- [correctness] Endorse B: the wave-commit/`tasks.md`-marking ordering hazard is real, not speculative — I read `speckit-implement-parallel/SKILL.md`'s wave loop directly. Step 3 marks each completed task `[X]` in `tasks.md` as results come back; step 4 (Integrate) and step 5 (Log) come after; the proposed per-wave `speckit.git.commit` call (per D-R7) can only land after step 4 at the earliest. So the ordering gap B describes — `tasks.md` asserting a wave is done while git hasn't committed it — exists regardless of exactly which step the commit call is wired to. (confidence: med-high)
- [correctness] New: B's and E's `before_specify` findings are complementary, not duplicate — both should reach triage as separate items. B is right that `speckit-specify/SKILL.md`'s closing NOTE ("Branch creation is handled by the `before_specify` hook") is a flatly false, stale statement given the ratified `after_specify` design (D51) — that's a documentation-correctness bug, independent of whether `after_specify` is the right call. E is right, on a different point, that the plan's rejected-alternatives table argues against a generic strawman rather than the concrete, already-tolerant `before_specify` contract that exists at Outline step 2 (L76-80: "the branch name does **not** dictate the spec directory name") — I read that passage; it does already tolerate the exact mismatch risk the rejection cites, which is a fairness-of-argument point, not a factual error. `after_specify` still looks correct on the merits either way, but conflating B's "fix this false NOTE" with E's "acknowledge the dormant slot" would lose one of the two. (confidence: med)
- [correctness] Endorse C, Endorse E: the `priority` field is inert. I grepped `priority` across every stock skill's hook-processing section, `common.sh`, and `extensions/graphify/install.sh` myself — it's written as a static `5` by graphify's installer and never read anywhere to order hook execution; stock skills process a hook key's YAML list in as-written order. `contracts/commands.md`'s "Hooks compose... by `priority`" (FR-013) describes a mechanism that doesn't exist in this codebase today. C and E reached this independently (simplicity and sequencing lenses); that convergence plus my own grep makes this high-confidence, not a judgment call. (confidence: high)
- [correctness] Endorse C, Endorse E: the hook-composition claim in `contracts/commands.md` ("the git commit hook runs after graphify's context hook at a shared boundary") describes a scenario that cannot occur under the plan's own design. I checked graphify's live `extension.yml` directly: it registers exactly three hooks, all `before_*` (`before_plan`, `before_tasks`, `before_implement`), zero `after_*`. Every git-ext commit hook is `after_*`. There is no key where a git commit hook and a graphify hook could ever coexist — the only two real shared keys (`before_tasks`, `before_implement`) host git's `verify-gate` (a hard-block, not a commit) alongside graphify's context-gen. This needs a rewrite, not a footnote: the actual open question is whether `verify-gate` should run before or after graphify's (heavier) context regeneration at those two keys, and nothing in Phase 1 answers it. (confidence: high)
- [testability] New, but directly reinforcing D: I read `quickstart.md` myself in full (37 lines) and every one of D's specific claims checks out letter-for-letter — no interrupt/resume step for SC-006 anywhere; Scenario 2 step 2 ("Edit `plan.md`; commit") is the one step with no literal command; Scenario 5's "outside the spike task" names no file that exists anywhere in `plan.md`'s Project Structure; and Setup's `grep -A2 'after_specify\|before_tasks' .specify/extensions.yml` confirms only that the hooks are *registered*, never that they're `optional: false`. That last point compounds the round's headline finding: even after the `optional: true` fix (see above) lands, quickstart's own Setup step still wouldn't catch a regression back to `optional: true`, because it never inspects that field. Quickstart needs a line for it. (confidence: high)
- [correctness] Challenge D: don't leave the "no code-level enforcement / `HookExecutor` undefined" observation at `confidence: low`. I grepped `HookExecutor` repo-wide myself — it's named in 15+ skill files' boilerplate and implemented nowhere (no class, script, or binary, anywhere). Combined with the `optional: true` finding above, this means FR-009's "hard-block" and SC-001's "no manual git" aren't just mis-flagged in config — there is *no* code path in this repo, today, that could enforce either one even with the flag corrected; enforcement is 100% LLM-prose compliance. That's a fact about the plan's actual guarantee strength, not a systemic aside — it belongs in the risk register at real severity, not a footnote in a testability opinion. (confidence: med)
- [correctness] Endorse C: `commit.sh`'s "stages the feature's changed paths" is a genuine, unresolved ambiguity between "everything currently dirty" (needed for wave commits, per M1's own trail) and "only this feature's known paths" (needed for phase-boundary commits, per SC-003's trustworthy-diff claim) — and it compounds the TOCTOU finding above: if two `/speckit-specify` races ever collide on one `NNN` directory (New-tagged suggestion, above), an "everything dirty" wave commit later in the pipeline could silently sweep one feature's stray uncommitted files into the other feature's phase-tagged commit, which is a strictly worse outcome than either bug alone. Worth resolving both in the same pass rather than as two unrelated Phase-1 line items. (confidence: med)
