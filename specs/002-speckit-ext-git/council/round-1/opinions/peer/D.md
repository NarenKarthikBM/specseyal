---
lens: testability
---

`graphify-out/graph.json` exists, but every peer independently found it low-signal for this feature (degree-1 README nodes, "No node matching" on `.sh` files, no edge type for hook-dispatch or cross-file procedure coupling) — consistent with what I found in Stage 1. I didn't re-run graph queries for this pass; instead I re-verified the specific file-level claims below directly (`diff`, `grep`, direct reads of `spec.md`/`quickstart.md`/`decision-record.md`), the same methodology all four peers used to do their best work this round.

**Merit ranking.** A, B, and E sit at the same top tier: every load-bearing claim is checked against the actual file — exhaustive greps across all 16 skill files (A, Finding 2), byte-`diff` confirmation of cross-extension file identity (B, §1, which I re-ran and confirmed: both `speckit-council-approve/SKILL.md` and `speckit-implement-parallel/SKILL.md` are byte-identical to their source-extension trees), and mtime-ordered staleness proofs (E). C is a genuine half-step behind on falsifiability specifically, though not on value: its concrete findings (install.sh line counts, `troubleshooting.md` citations, `graphify explain` returning nothing for two claims) are as well-grounded as anyone's, but its two highest-leverage suggestions — "gold-plating for v1," "very likely to be the largest, most failure-prone file" — are engineering judgment calls without an attached falsification method, which is a meaningfully different (and weaker) claim-type than A/B/E's mechanism-level findings. See Suggestions below for where this matters concretely.

**The triple convergence is the strongest signal in the round.** A (Finding 1), B (§5), and E (the "strawman" finding) each independently — via correctness, risk, and sequencing lenses respectively, none citing the others — landed on the identical two facts: `speckit-specify/SKILL.md`'s live `before_specify` hook contract already tolerates the branch/directory-name mismatch the plan cites as its rejection reason, and this feature's own `graphify-context.md` still asserts the rejected `before_specify` design. Independent convergence from three different entry points on the same two facts is a stronger evidentiary signal than any one member's stated confidence, and I think the chairman should weight it accordingly. From my lens, though, none of the three proposes anything that would have caught this mechanically instead of requiring three people to read raw files by hand — see my new suggestion below.

**The hard-block "mechanism gap" is two stacked bugs, not one, and the peers split it across two opinions without cross-referencing each other.** E found that git-ext's hooks are specified `optional: true` uniformly, and that the stock dispatch prose only auto-invokes `optional: false` hooks — an `optional: true` hook is merely announced, never run unprompted. A separately found that even the hooks that *are* `optional: false` and do get auto-invoked have no "stop on non-zero exit" instruction anywhere across all 16 skill files — the prose says "wait for it to finish," never "and abort if it fails." These are different failure layers of the same FR-009 claim, and fixing one does not fix the other: flip the flag to `optional: false` (E's fix) and the hook now runs, but nothing yet tells the calling skill to stop when `verify-gate.sh` exits non-zero (A's gap, still open); add stop-on-failure prose (A's fix) without also fixing the flag, and it's moot because the hook is never invoked to begin with. Neither opinion flags the other's half as a precondition, and I'd flag to the chairman that these need to be tracked and verified as two separate patches, not collapsed into one "fix the hard-block" item where the second half quietly falls off.

**Testability chain on SC-004 checks out under direct verification.** I read `quickstart.md` Scenario 2 directly: step 2 is exactly "Edit `plan.md`; commit (`<S2>`)" — confirming A's claim (only the committed-edit case is exercised) precisely as stated, no hedging needed. I also independently confirmed A's Finding 4 against `docs/contracts/decision-record.md` §2/§5 directly: the `## Human Gate` section's fields are exactly `reviewer | decision | reviewed`, no SHA field — Finding 4 holds exactly as described.

**B's ownership-coupling finding has an under-examined testability angle.** Beyond B's framing (a silent-overwrite risk on reinstall), the `diff`-confirmed fact that the R1 seam has to be hand-written into files owned by other extensions means a regression there (the seam getting wiped by a council/graphify reinstall) is exactly the kind of thing that ships undetected, because nothing in `quickstart.md` re-verifies Scenario 1 after a reinstall — it's a one-shot check, not a regression guard.

**On the spec-clarification check:** I read `spec.md` directly rather than trust paraphrase, since this bears on a Challenge below. FR-009 states verbatim: "a warn-and-override was rejected as defeating the binding (clarified 2026-07-09)." That's a specific, dated, already-ratified position, not an open question — relevant to one of C's suggestions (see Suggestions).

## Suggestions

- [testability] New: A's Finding 2 (mandatory hooks have no stop-on-failure instruction) and E's finding (hard-block hooks are registered `optional: true`, and the stock dispatch prose only auto-invokes `optional: false` hooks) are two independent, stacking failures of FR-009's hard-block, not the same finding twice. Fixing only E's half (flip the flag) makes the hook run but not block on failure; fixing only A's half (add stop-on-failure prose) is moot while the hook is never auto-invoked. Track and verify both as separate required patches, or the second is likely to get silently dropped as "already covered" by the first. (confidence: high)
- [testability] Endorse A and B jointly: re-verified with `diff` that `speckit-council-approve/SKILL.md` and `speckit-implement-parallel/SKILL.md` are byte-identical to their source-extension trees, and confirmed `speckit-implement-parallel/SKILL.md`'s "Execute waves" loop has no commit step anywhere (only an unrelated worktree-isolation guardrail line). Beyond B's ownership-coupling/risk framing, this is a testability gap in its own right: nothing in `quickstart.md` re-runs Scenario 1 after a council/graphify reinstall, so a regression where the R1 seam gets silently wiped (B's failure mode (a)) would ship undetected by the plan's own test suite. Add a reinstall-then-rerun step to Scenario 1. (confidence: high)
- [testability] Endorse A: independently checked `quickstart.md` Scenario 2 directly — step 2 is exactly "Edit `plan.md`; commit (`<S2>`)," confirming the committed-edit-only gap precisely as A describes. Since the underlying bug (Finding 3, SHA-blindness to uncommitted drift) is itself rated high confidence and the missing test case is a direct, mechanical consequence of it rather than a softer inference, I'd raise this specific testability suggestion from A's stated med to high. (confidence: high)
- [testability] Endorse B: the wave-interruption ordering hole (per-task `[X]` marking in `tasks.md` landing before the proposed per-wave commit call) is a concrete, identified failure mode sitting inside a documented blind spot — `quickstart.md` has no interrupt/resume scenario anywhere for SC-006/US2, the pipeline's second P1 story. The untested territory isn't hypothetical; it already contains a specific bug a resume-safety test would need to assert against (git HEAD and `tasks.md`'s `[X]` markers must agree on which waves are actually committed after a simulated mid-wave kill). (confidence: high)
- [testability] New: B's two-feature race (TOCTOU on `create-new-feature.sh`'s directory-scan `NNN` allocation) is the single highest-value new defect surfaced this round, and I confirmed no falsification path exists anywhere for it — not in B's own suggestion (which targets the fix, not a test), not in `quickstart.md`, whose 5 scenarios plus `spec.md`/`plan.md`'s testing sections contain zero concurrency case (grepped for concurrent/race/simultaneous: no hits outside the unrelated worktree-isolation spike). Add a Scenario 6: race two `/speckit-specify` invocations (or script two `create-new-feature.sh` calls against one shared `specs/` listing) and assert either two distinct `NNN`s or a loud, detected failure — never a silent shared-branch merge. (confidence: high)
- [risk] Challenge C: shipping gate-SHA binding as "record-and-disclose only" (deferring the enforced hard-block) conflicts with an already-ratified spec clarification, not just a design preference — `spec.md` FR-009 states verbatim that "a warn-and-override was rejected as defeating the binding (clarified 2026-07-09)," confirmed by direct read. C's proposal is functionally that rejected pattern. If the council wants this simplification, route it as a spec-delta re-litigating that clarification, not a plan-level scope cut — as worded it would leave FR-009/SC-004 with nothing left to hard-block-test against. (confidence: high)
- [simplicity] Endorse E over C: C's hook-composition/priority finding ("mechanical git hides ambiguity," item 2) and E's "priority ordering is inert" finding describe the same bug at different depths. E is strictly more rigorous — it traces whether `priority` is consumed anywhere (grepped: never; hardcoded static `5` by graphify's own installer) and derives the concrete consequence (append-only merge means git-ext's verify-gate lands after graphify's context-gen at the two real shared keys, so a stale gate is discovered only after context-regeneration already ran). The chairman should fold these into one suggestion sourced to both, credited to E for the deeper mechanism trace. (confidence: med)
- [correctness] Endorse A, B, and E jointly: all three independently, via correctness/risk/sequencing lenses respectively, verified that `speckit-specify/SKILL.md`'s live `before_specify` hook contract already tolerates the exact branch/directory-name mismatch the plan cites as its rejection reason, and that this feature's own `graphify-context.md` still asserts the rejected `before_specify` design. Three-lens independent convergence on the same two facts is the strongest-signal item in this round — weight it above any single member's individually stated confidence. (confidence: high)
- [testability] New: that three independent members had to manually re-derive the same stale-doc-drift finding (previous item) by reading raw skill/context files by hand is itself evidence of a missing automated check, not just a one-off doc bug. Recommend a lint step — grep `speckit-specify/SKILL.md` and any committed `graphify-context.md` for `before_specify` post-build, wired into M2's conformance checker (D50) — so this exact class of drift is caught mechanically next time rather than by council attrition. (confidence: med)
- [testability] Challenge C (minor): the installer-undercosting claim ("very likely to be the largest, most failure-prone file") is argued by analogy to graphify's simpler 3-entry merge rather than from any estimate specific to git-ext's actual 7-entry/3-shape merge — plausible, and consistent with E's independent finding that the installer's append-only ordering already causes a real sequencing bug (see the priority item above), but as worded it's a prediction with no acceptance threshold attached. If accepted, convert it into a concrete Phase-1 task with an explicit size/complexity budget rather than leaving it a qualitative flag. (confidence: low)
