# Decision Record — 007-oss-docs

> Append-only. Rounds are appended, never edited. Rejection requires reasoning (D13.5).

## Metadata

| Field | Value |
|---|---|
| feature | `007-oss-docs` |
| spec-id | `007-oss-docs` |
| profile | `gates.council=human, gates.workforce=human` |
| rounds-run | 1 |
| schema-version | `1.0` |

---

## Round 1 — 2026-07-16T12:43:59Z

**Verdict:** 3 blocking · 8 strong · 11 consider
**Deck reviewed:** `council/defense-deck/technical.md` @ `3d144d5`
**Plan reviewed:** `plan.md` @ `926b402`

| ID | Class | Suggestion | Disposition | Rationale | Plan delta |
|---|---|---|---|---|---|
| R1-S01 | blocking | Write the P2∧P3 pass-branch fixture (`full_auto: true` with both gates `auto` ⇒ VALID) into the Project Structure fixture list now under an explicit name (e.g. `full-auto-satisfied.yaml`). `tasks.md` is generated mechanically from that named list, so a gap left unnamed here never becomes a task and SC-007's "both branches of every targeted class" coverage claim stays silently unmet; this feature's own `profile.yaml` is not an instance of the strict-pass case, so no existing profile can stand in. | accepted | Unanimous (5/5 + peer); an unnamed fixture never becomes a `tasks.md` task, so SC-007's both-branch claim would silently overstate. Named `full-auto-satisfied.yaml` (P2∧P3 VALID) in the Project Structure fixture list. Delta-check: RESOLVED. | `plan.md §Project Structure` @ `a147b4d` |
| R1-S02 | blocking | Correct the "live `council_tier: standrad` silent-degrade defect" framing: `git log --all -p -- '**/profile.yaml'` has zero matches across all committed history — `standrad` has never been a committed profile value, only prose/synthetic-fixture value. Restate it as I-27/D79(2)'s illustrative example and the SC-009 fixture, not a present-tense live defect, so the motivating narrative isn't grounded on a claim the repo's own history contradicts. | accepted | Peer-reproduced against git history; the "live defect" framing is disproven by the repo's own log. Summary reframed as the I-27/D79(2) illustrative class reproduced as the SC-009 out-of-enum fixture, explicitly not present-tense, noting no committed `standrad` value exists. Delta-check: RESOLVED. | `plan.md §Summary` @ `a147b4d` |
| R1-S03 | blocking | The recommended fail-closed `before_plan` hook is a single point-in-time check on a file that stays mutable for the feature's life and is never SHA-pinned into `gates.yml`, so the "no gate ever reasons about an invalid profile" claim fails the instant a profile is edited after the check — before `/speckit-council` reads `council_tier` or `before_implement` acts on `gates.workforce`. Weigh mirroring the repo's existing priority-1 `verify-gate` pattern (re-validate at `before_tasks`/`before_implement`, the actual consumption points) alongside the current `before_plan`-only vs standalone-only options before locking the enforcement point. | accepted | Peer-verified real defect: a `before_plan`-only check cannot uphold its central safety claim on a mutable, un-SHA-pinned profile. Enforcement point moved to re-validation at the actual consumption points (`before_plan`, `before_tasks`, `before_implement`) at priority 1, mirroring `verify-gate`; recorded in Complexity Tracking + Project Structure. Delta-check: RESOLVED. | `plan.md §Complexity Tracking` @ `a147b4d` |
| R1-S04 | strong | Decide and state whether the validator checks `schema_version`'s value (against the version it implements, e.g. exact-match `1.2`) or only its presence/type — a type-only check lets a wrong/typo'd version (`2.0`, `1.O`) silently pass, the exact silent-failure class this feature exists to close, and at the next schema bump surfaces as a pile of unrelated unknown-key errors instead of one clear version-mismatch message. Add a `schema_version`-mismatch fixture either way. | accepted | Closes the very silent-drift class the feature targets, one level up. D-c now specifies an exact-match `1.2` value check + a `schema-version-mismatch.yaml` fixture. | `plan.md §Phase 0 (D-c)` @ `a147b4d` |
| R1-S05 | strong | Add a one-time pre-flight task to run `validate-profile.py` against every currently-committed `specs/*/profile.yaml` before the fail-closed FR-019 hook is wired live, catching any retroactive hard-block of an in-flight feature's `plan` phase on adoption day — the Scale/Scope claim that all `specs/00{0..7}` "must validate correctly" is asserted plan-time, not executed. | accepted | Real adoption-day risk: the hook could retroactively hard-block an in-flight feature. Added a one-time pre-flight running the validator over every committed profile before the hook goes live, executing (not asserting) the "all must validate" claim. | `plan.md §Complexity Tracking` @ `a147b4d` |
| R1-S06 | strong | The interpreter ladder's final `uv run --with pyyaml python` rung can attempt a network fetch and hang in an offline/restricted environment rather than failing loud — contradicting both the "loud non-zero failure" guarantee and SC-010's <2s bound. Add an explicit timeout (or drop the rung) so an unreachable-package case fails fast and non-zero. | accepted | A hang would violate both the loud-failure guarantee and SC-010's <2s bound. The `uv` rung is now bounded by an explicit short timeout, fail-fast non-zero on timeout. | `plan.md §Technical Context (Primary Dependencies)` @ `a147b4d` |
| R1-S07 | strong | Make one explicit decision on the FR-018 `deck_render` subsumption mechanism (D-d) rather than leaving it open at implement time: the council split — one member wants a pinned module constant guarded by a committed equivalence test, another wants a runtime cross-extension import that removes the need for that test (citing the flat, package-less script-directory layout as making the import fragile). Pick one and record the reason. | accepted | The round's one genuine split (chairman-flagged). Resolved to a **pinned local `DECK_RENDER_ENUM` + committed equivalence test**, not a runtime cross-extension import: the two scripts sit in different package-less `scripts/` trees, so a runtime import needs fragile `sys.path` surgery while the equivalence test gives the same SSOT guarantee (drift fails loudly). Reason recorded at D-d + Summary. | `plan.md §Phase 0 (D-d)` @ `a147b4d` |
| R1-S08 | strong | Name, as one Risk Register row, the compound blast-radius/escape-hatch risk of the fail-closed FR-019 hook: it promotes `profile_key.py`'s interpreter probe from gating one optional feature (deck-render) to gating every feature's mandatory `plan` phase, with no override — so a validator bug false-positiving on an untested-but-legitimate profile, or an environment with no reachable PyYAML-capable interpreter, becomes a repo-wide planning outage indistinguishable at the exit-code level from a genuinely malformed profile. State whether a bypass exists or explicitly accept there is none and why. | accepted | The blast-radius promotion is real and load-bearing for the enforcement decision. Named in Complexity Tracking with an explicit decision: **no silent bypass, by design** (a fail-open hatch reintroduces the silent mis-config this feature closes); residual bounded by S06 timeout + S10 stderr legibility + S05 pre-flight. | `plan.md §Complexity Tracking` @ `a147b4d` |
| R1-S09 | strong | Name the mechanism by which the "no PyYAML-capable interpreter reachable ⇒ loud non-zero failure" branch is actually exercised — it is an environment property, not a file property, so it can't be fixtured like a malformed YAML file and needs interpreter-mocking or PATH manipulation, and no such precedent exists in the repo's harnesses (`test_categorize.sh`, `test_skill_builder.sh`, `test_frontmatter.py`). Without a named mechanism this branch is claimed-tested but never forced. | accepted | A claimed-tested-but-never-forced branch. Testing now names the mechanism: PATH/interpreter shadowing in the harness (a shim dir prepended to `PATH`), explicitly noting no existing harness sets the precedent. | `plan.md §Technical Context (Testing)` @ `a147b4d` |
| R1-S10 | strong | Require at least one fixture to assert on stderr message *content* (names the offending key; never a raw parser traceback), not only on exit code — an exit-code-only check cannot distinguish a clean rejection from a regressed traceback that also exits non-zero, which is exactly the guarantee the message-shape rule promises. | accepted | An exit-code-only test can't tell a clean rejection from a regressed traceback that also exits non-zero. Testing now requires ≥1 fixture asserting stderr content names the offending key with no raw traceback. | `plan.md §Technical Context (Testing)` @ `a147b4d` |
| R1-S11 | strong | Give the FR-019 hook-wiring task an explicit dependency edge on the validator plus its full `test_profile.sh` suite passing (not just an informal "separable" framing), so a parallel implementation wave can't register the fail-closed hook before the script is hardened and hard-block `plan` on the validator's own bugs rather than on real profile defects. | accepted | Without the edge a parallel wave could wire the fail-closed hook before the validator is hardened, hard-blocking `plan` on validator bugs. Project Structure now gives the wiring task an explicit dependency on `validate-profile.py` + a green `test_profile.sh`. | `plan.md §Project Structure` @ `a147b4d` |
| R1-S12 | consider | Distinguish `reopen_tier` from `max_rounds` when characterizing the deferred schema keys: `profile-schema.md` gives `reopen_tier` live triage behavior today (`auto` ⇒ triage proposes/human confirms; `delta`/`full` pin the tier and skip the proposal) while `max_rounds` is genuinely inert (validators reject `>1`) — a "Low impact, dead key" framing that bundles them understates `reopen_tier`'s real behavioral stakes, even though the enum check catches a typo in either regardless. | accepted | Cheap correctness fix (and self-confirming — triage exercised `reopen_tier`'s live behavior this run). D-e now distinguishes the live `reopen_tier` from the inert `max_rounds` rather than framing both as dead. | `plan.md §Phase 0 (D-e)` @ `a147b4d` |
| R1-S13 | consider | Add a housekeeping task to refresh `graphify-out/graph.json` after this feature lands, so FR-018's "no divergent copy" claim and this feature's new coupling become independently graph-checkable going forward rather than resting on the stale graph plus the equivalence test alone. | accepted | Cheap and keeps FR-018's new cross-extension coupling verifiable. Booked as a post-merge `graphify` refresh housekeeping task in Testing, so `/speckit-tasks` mints it. | `plan.md §Technical Context (Testing)` @ `a147b4d` |
| R1-S14 | consider | Make the session-count/cost estimate foot to its own listed components — the named items total at least 7, not "~6-8", so the ballpark should track the arithmetic if it is meant to be load-bearing for council/human review. | rejected | plan.md §Scale/Scope enumerates concrete artifacts and carries **no** `~6-8` session/cost ballpark — the figure this note critiques appears only in the technical deck's rendering, not in the plan triage revises. There is no in-plan estimate to foot; the component enumeration already stands on its own, and inventing a synthetic session count to satisfy the note would manufacture precision the plan does not claim. (The deck is round-scoped and superseded; no plan edit is warranted.) | — |
| R1-S15 | consider | State how the validator's source and its tracked install mirror (`.specify/extensions/workforce/scripts/`) are kept in sync — if an existing repo-wide install mechanism guarantees it, say so; otherwise a drifted install copy is an unguarded failure mode with no named fixture, unlike the `deck_render` enum which does get an equivalence test. | accepted | A drifted install copy would be an unguarded failure mode. Structure Decision now names the guarantee: the repo's existing extension installer copies source into `.specify/…`, and the drift check rides existing install-idempotency coverage — no second sync path. | `plan.md §Project Structure` @ `a147b4d` |
| R1-S16 | consider | Note the follow-on drift risk between `profile-schema.md`'s prose and `validate-profile.py`'s hardcoded rules (required keys, enums, the P1–P5 handshake): only the single `deck_render` field gets an equivalence guard, and D-e already anticipates a future schema amendment, so nothing would catch the validator's rules and the contract drifting apart — the same prose-vs-enforcement gap this feature closes for `profile.yaml`, reintroduced one level up. | accepted | Legitimate one-level-up recursion of the gap this feature closes. Phase 1 now notes the residual prose-vs-validator drift and explicitly **accepts** it (a full schema-conformance test is out of scope), flagging it for the D-e-anticipated schema amendment rather than leaving it silent. | `plan.md §Phase 1 (Design & Contracts)` @ `a147b4d` |
| R1-S17 | consider | Track Arm A and Arm B as separable task sequences within this feature so a stall in one arm's review/implementation doesn't block the other — they share no code and have disjoint testability profiles. (Sequencing recommendation only; the peer review flags that re-opening it as a *scope* split re-litigates D82, which already folded I-27 into 007.) | accepted | Applied as a **sequencing** note only (per the peer caveat — not a scope re-split; D82 keeps 007 one feature). Structure Decision now records Arm A / Arm B as two independent task sequences so a stall in one can't block the other. | `plan.md §Project Structure` @ `a147b4d` |
| R1-S18 | consider | Close SC-010's "Partial" status by adding a committed wall-clock <2s assertion in the harness rather than leaving the timing bound as a quickstart-only, non-committed check. | accepted | Fully mechanizable and closes SC-010's "Partial". Testing now commits a wall-clock <2s assertion in `test_profile.sh`. | `plan.md §Technical Context (Testing)` @ `a147b4d` |
| R1-S19 | consider | Confirm the unknown-key fixtures cover both a top-level unknown key and one nested inside a mapping (e.g. `gates.council`), since a recursive-validation bug could reject one depth and silently pass the other. | accepted | A recursive-validation bug could pass one depth silently. Fixtures now include both a top-level unknown key and a nested one under `gates.council`. | `plan.md §Technical Context (Testing)` @ `a147b4d` |
| R1-S20 | consider | Commit Arm A's FR-010/FR-011 reference-resolution + leakage grep as a standalone re-runnable script now (no CI wiring, matching the feature's scope boundary) so a future feature can re-verify SC-003/SC-004 as the front door's paths/commands drift, instead of re-inventing the check. | accepted | Cheap durability win within scope. Testing now commits the docs reference-resolution + leakage grep as a standalone re-runnable script (no CI), keeping SC-003/SC-004 re-verifiable. | `plan.md §Technical Context (Testing)` @ `a147b4d` |
| R1-S21 | consider | Pin the new mandatory `before_plan` validate-profile hook to priority 1 explicitly (the two existing fail-closed `verify-gate` hooks run at priority 1; the current `before_plan` graphify hook sits at priority 5), so its fail-closed check runs deterministically rather than depending on hook-list ordering. | accepted | Correct and consistent with the two existing priority-1 `verify-gate` hooks. Project Structure now pins the fail-closed validate-profile hook to priority 1 (ahead of the priority-5 graphify hook). | `plan.md §Project Structure` @ `a147b4d` |
| R1-S22 | consider | Confirm at task-writing time that the fixture set exercises the absent-optional-key defaulting path (omitting `council_tier` ⇒ default `full`; omitting `max_rounds`/`reopen_tier` ⇒ default `1`/`auto`) as distinct from a profile stating a *wrong* value for those keys, since `conformant.yaml`'s exact shape isn't specified anywhere plan-time. | accepted | The default path is a distinct behavior from a wrong-value rejection. Added a `defaults-omitted.yaml` fixture exercising the absent-optional-key defaulting, separate from wrong-value fixtures. | `plan.md §Technical Context (Testing)` @ `a147b4d` |

### Chairman delta check — 2026-07-16T12:40:26Z

- R1-S01 — RESOLVED — plan.md's Project Structure fixture list now names `full-auto-satisfied.yaml` as the `full_auto: true` + both-gates-`auto` ⇒ VALID pass-branch fixture, so `/speckit-tasks` will mint the strict-pass task and SC-007's both-branch coverage claim is backed.
- R1-S02 — RESOLVED — the Summary now casts `council_tier: standrad` as the I-27/D79(2) illustrative example reproduced as the SC-009 out-of-enum fixture, explicitly not a present-tense live defect and noting the repo's committed history carries no `standrad` profile value.
- R1-S03 — RESOLVED — the enforcement point is moved off `before_plan`-only to re-validation at the actual consumption points (`before_plan`, `before_tasks`, and `before_implement`) at priority 1, mirroring the existing `verify-gate` pattern, so a post-check profile edit can no longer reach a gate unvalidated.

**Delta verdict:** all clear, ready for the gate.

---

## Human Gate — 2026-07-16T16:10:13Z

| Field | Value |
|---|---|
| reviewer | Naren Karthik B M |
| decision | `approved` |
| reviewed | `defense-deck/overview.md`, `round-1/suggestions.md`, this record |

**Notes:** none.

**Overrides:** none.

**Binding:** plan↔SHA binding recorded at `specs/007-oss-docs/gates.yml` (git-ext-owned; FR-008/D55).

---

## Carried Constraints

*Rebuilt from every round's accepted rows; `/speckit-tasks` reads this section and nothing else from this file.*

- `R1-S01` — Name `full-auto-satisfied.yaml` (`full_auto: true` + both gates `auto` ⇒ VALID, the P2∧P3 strict-pass branch) in the profile fixture set, so tasks generation mints its coverage task and SC-007's both-branch claim holds.
- `R1-S02` — Frame `council_tier: standrad` throughout as the I-27/D79(2) illustrative class and the SC-009 out-of-enum fixture — never a present-tense live/committed defect.
- `R1-S03` — Wire the fail-closed validate-profile check at the actual consumption points (`before_plan`, `before_tasks`, `before_implement`) mirroring the priority-1 `verify-gate` pattern, not `before_plan` alone.
- `R1-S04` — The validator checks `schema_version` by **value** (exact-match `1.2`), not presence/type only; commit a `schema-version-mismatch.yaml` fixture.
- `R1-S05` — Include a one-time pre-flight task running `validate-profile.py` over every committed `specs/*/profile.yaml` before the FR-019 hook is wired live.
- `R1-S06` — Bound the `uv run --with pyyaml` interpreter-ladder rung with an explicit short timeout, fail-fast non-zero, so an offline host can't hang (loud-failure + SC-010 <2s).
- `R1-S07` — Implement FR-018 subsumption as a pinned local `DECK_RENDER_ENUM` constant guarded by a committed equivalence test against `profile_key.DECK_RENDER_ENUM` — not a runtime cross-extension import.
- `R1-S08` — Keep the Complexity-Tracking row naming the fail-closed hook's compound blast-radius risk and its explicit "no silent bypass, by design" decision with the bounding mitigations.
- `R1-S09` — Force the no-reachable-interpreter branch via PATH/interpreter shadowing in the harness (a shim dir on `PATH`); it is an environment property, not a file fixture.
- `R1-S10` — At least one fixture asserts stderr **content** (names the offending key, never a raw parser traceback), not exit code alone.
- `R1-S11` — The FR-019 hook-wiring task carries an explicit dependency edge on `validate-profile.py` + a green `test_profile.sh` suite before it may register the fail-closed hook.
- `R1-S12` — Treat `reopen_tier` as a live key (real triage behavior) and `max_rounds` as inert; enforce both as written, but do not frame both as "dead".
- `R1-S13` — Include a post-merge `graphify` graph-refresh housekeeping task so FR-018's cross-extension coupling stays graph-checkable.
- `R1-S15` — State the source↔install-mirror sync guarantee (the repo installer copies source into `.specify/…`); no second sync path, drift rides install-idempotency coverage.
- `R1-S16` — Note (and explicitly accept, flagging for the future schema amendment) the residual prose-vs-`validate-profile.py` drift beyond the `deck_render` equivalence guard.
- `R1-S17` — Sequence Arm A (docs) and Arm B (validator) as two independent task sequences within this one feature (a stall in one must not block the other) — sequencing only, not a scope split.
- `R1-S18` — Commit a wall-clock <2s assertion in `test_profile.sh` (close SC-010's "Partial").
- `R1-S19` — Cover both a top-level unknown key and a nested one (`gates.council`) in the unknown-key fixtures.
- `R1-S20` — Commit Arm A's FR-010/FR-011 reference-resolution + leakage grep as a standalone re-runnable script (no CI), keeping SC-003/SC-004 re-verifiable.
- `R1-S21` — Pin the fail-closed validate-profile hook to priority 1 explicitly (ahead of the priority-5 graphify hook).
- `R1-S22` — Include a `defaults-omitted.yaml` fixture exercising the absent-optional-key defaulting path, distinct from wrong-value fixtures.
