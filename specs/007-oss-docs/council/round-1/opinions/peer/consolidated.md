---
lens: consolidated
---

All five stage-1 opinions are well-grounded and none is padded — this is a strong round. Ranked as a set, with independent verification of the load-bearing empirical claims (git history, `.specify/extensions.yml`, the repo's existing test harnesses, and `profile-schema.md`'s exact wording):

**Strongest: B and E.** B's central finding — that the deck's §1 "not hypothetical" claim about a real committed `council_tier: standrad` typo does not hold up — is the single highest-value catch in the round. I independently reproduced it: `git log --all -p -- '**/profile.yaml'` across the entire repo returns zero matches for "standrad," and a full-repo grep shows every occurrence is prose (006-deck-render's illustrative D79(2) narrative, docs/90, and 007's own spec/plan/quickstart/contracts using it as a synthetic fixture value) — never a committed profile. This corrects the deck's own motivating narrative, which the chairman should not let stand uncorrected. E's finding — that `before_plan` is a single point-in-time check on a file (`profile.yaml`) that stays mutable for the rest of the feature's life, with no re-validation before `/speckit-council` reads `council_tier` or before `before_implement` acts on `gates.workforce` — is the most architecturally substantive finding in the round. I verified it directly: `.specify/extensions.yml` shows the existing `before_tasks`/`before_implement` `verify-gate` hooks both run at **priority 1**, while the current `before_plan` slot sits at **priority 5** — confirming both E's precedent argument (this repo already re-checks mutable gate-adjacent artifacts at the point of consumption, not just at the earliest checkpoint) and its separate priority-ordering suggestion.

**Solid: A and D.** A's `schema_version` exact-match-vs-type-only gap is well-grounded in `profile-schema.md`'s own language ("bump on any breaking change" implies an expected exact value, not an arbitrary string) and names a distinct silent-failure class from the P2/P3 gap everyone else converges on. D's testability critique is the sharpest on mechanism: I checked `extensions/workforce/test/` (`test_categorize.sh`, `test_skill_builder.sh`, `test_frontmatter.py`) and found no existing PATH-manipulation or interpreter-mocking precedent anywhere in this repo's harnesses — so D's doubt that the "no PyYAML-capable interpreter reachable" branch is actually forceable, rather than merely claimed-tested, is not speculative.

**Weakest / needs reconciliation: C.** C's bundling argument (Arm A/Arm B should be tracked separately) re-opens ground D82 already settled — the owner explicitly folded I-27 into 007 for scope/deadline reasons — so it's better read as a task-sequencing recommendation (which C's own suggestion bullet already states) than a scope challenge. More importantly, **C and B give directly conflicting concrete recommendations** on the `deck_render` subsumption mechanism (research.md D-d) without either acknowledging the other: B wants a pinned module constant guarded by a committed equivalence test; C wants a runtime import specifically because it eliminates the need for that test. Neither is unreasonable, but the chairman must pick one, not both — C's cross-extension-import fragility point (flat script directories, no package structure per plan.md §4) is real, and so is B's point that pinned constants can silently drift without the test.

**A repeated finding worth flagging as one, not four.** A, C, D, and E all independently name the same gap: no fixture is confirmed to exercise the P2/P3 pass branch (`full_auto: true` + both gates `auto`). This is the deck's own self-flagged gap (§7), and the convergence across four of five members should read as strong confirmation it's real and must be closed — but E's version is the actionable one: it explains the mechanism (tasks.md is generated mechanically from plan.md's named fixture list, so a gap not written back into plan.md now has no later mechanical path to resurface). Verified: `specs/007-oss-docs/profile.yaml` (`council_tier: standard`, both gates `human`) is not itself an instance of the strict-pass case, so there's no existing example in this repo to point to either — the fixture genuinely doesn't exist yet anywhere.

## Suggestions

- [risk] Endorse B: the `council_tier: standrad` "not hypothetical" claim is unsupported by the repo's git history — independently reproduced (zero matches across all committed `profile.yaml` history; every occurrence elsewhere is prose/fixture-value, not a real committed defect). Correct the deck's §1 framing before the chairman treats it as live evidence for urgency. (confidence: high)
- [sequencing] Endorse E: the `before_plan`-only enforcement gap is the round's most substantive architectural finding — independently verified against `.specify/extensions.yml` that the existing `verify-gate` hooks at `before_tasks`/`before_implement` run at priority 1 while today's `before_plan` slot sits at priority 5, confirming both the re-validation-at-consumption-point argument and the priority-pinning suggestion. Should become a required plan.md revision. (confidence: high)
- [sequencing] Endorse E over A/C/D on the P2/P3 pass-branch gap: all four raise it, but only E names the actual mechanism (mechanical generation of tasks.md from plan.md's named fixture list) explaining why deferral past deck-review is unsafe. Chairman should treat this as one finding sourced from four members, not four independent suggestions, and require it be written into plan.md's Project Structure now. (confidence: high)
- [risk] Reconcile B vs. C: they give opposite concrete recommendations for the `deck_render` subsumption mechanism (D-d) — B wants a pinned module constant + committed equivalence test, C wants a runtime import to eliminate the need for that test — and neither acknowledges the other's option exists. Both have real supporting arguments (C: cross-extension import fragility given the flat, package-less script-directory layout; B: pinned constants can drift silently without the test). This needs one explicit plan.md decision, not two unreconciled Stage-1 recommendations. (confidence: med)
- [correctness] Endorse A: the `schema_version` exact-match-vs-type-only gap is well-grounded in `profile-schema.md`'s own wording and is a distinct silent-failure class from the P2/P3 gap — deserves its own fixture regardless of how P2/P3 is resolved. (confidence: med)
- [testability] Endorse D: the interpreter-ladder mocking concern is real, not speculative — verified no existing PATH-manipulation/interpreter-mocking precedent exists anywhere in this repo's test harnesses (`test_categorize.sh`, `test_skill_builder.sh`, `test_frontmatter.py`). If the committed fixture set can't actually force the "no PyYAML-capable interpreter reachable" branch, this guarantee is claimed-tested but not actually exercised. (confidence: med)
- [risk] Endorse B: the pre-flight smoke test (run `validate-profile.py` against every currently-committed `specs/*/profile.yaml` before the fail-closed hook goes live) is cheap and concrete — should be a named task regardless of which enforcement point is ultimately chosen. (confidence: high)
- [risk] Combine D's and E's escape-hatch findings into one Risk Register row: D notes FR-019 has no override if the validator itself has a bug; E notes the same interpreter probe is being promoted from gating one optional feature to gating every feature's mandatory plan phase. Together, a validator defect or an unreachable interpreter becomes a total, hookless planning outage indistinguishable at the exit-code level from a genuinely malformed profile — this compound risk is currently scattered across two members' opinions rather than named once. (confidence: med)
- [simplicity] Challenge C (partial): the Arm A/Arm B "should be separable" argument re-opens scope ground D82 already settled (owner explicitly folded I-27 into 007). Read it as C's own fallback framing already suggests — a task-sequencing recommendation within one feature, not an unresolved scope question. (confidence: low)
- [testability] New: none of the five checked whether the fixture set ("1 conformant + 1-per-malformed-class," plan.md §4) exercises the absent-optional-key defaulting path (e.g. a profile omitting `council_tier` should resolve to `full` per T1; omitting `gates.council.max_rounds`/`reopen_tier` should default to `1`/`auto` per profile-schema.md §3) as distinct from a profile stating a wrong value for those keys. `conformant.yaml`'s exact shape isn't specified anywhere plan-time (C confirmed this directly against plan.md), so it's unknown whether default-substitution is exercised at all. Lower-stakes than the P2/P3 gap but worth a one-line confirmation at task-writing time. (confidence: low)
- [testability] Endorse D on message-content vs. exit-code: this is a sharp, easy-to-miss distinction — step 6's guarantee is explicitly about stderr content ("names the offending key," never a raw traceback), and an exit-code-only fixture assertion can't distinguish a clean rejection from a regressed traceback. Elevate alongside the P2/P3 fix as a required task-writing-time item. (confidence: med)
- [correctness] Endorse A on the `reopen_tier`/`max_rounds` risk-register mischaracterization — grounded directly in `profile-schema.md`'s own language distinguishing a live triage-behavior consumer (`reopen_tier`) from a genuinely inert key (`max_rounds`); bundling them as one "Low impact, dead key" row understates the former's real behavioral stakes. (confidence: med)
