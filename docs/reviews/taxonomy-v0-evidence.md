# Taxonomy v0 → v1 — Evidence Dossier

> **What this is.** The v0→v1 review trigger (`taxonomy-v0.md` §8: *"three real dogfood features
> categorized"*) is **met** — `001-council-extension`, `002-speckit-ext-git`, `003-workforce` have all
> been categorized and assembled. This file bundles **every booked item** carried to that review, each with
> its evidence pointer and its status after three features. It is **input to the review, not the review**:
> it takes **no positions** and proposes **no resolutions** — every "→ for the review" is a question left
> open for the owner. Adopting anything here is a D-row per `taxonomy-v0.md` §8 (a value add/remove/rename,
> or a cap change), authored at review time — not here.
>
> **Prior review:** `docs/reviews/2026-07-09-taxonomy-v0-review.md` (the M0 *blessing*, 8×11, cap adopted).
> **Consumers if the enum changes:** `agent-library-schema.md` §3 (matching) + §6 (rule 5 validation), and
> a **major** version bump on every library entry using a removed/renamed value (§8 rule 2).

## A. Taxonomy structure — the axes and their exercise

| # | Booked item | Evidence after 001/002/003 | → for the review |
|---|---|---|---|
| A1 | **OQ4 — the `test × qa-automation` vs `test × backend-service` boundary** is decided by test *scope*, not by any graphify signal (`2026-07-09` memo §2; taxonomy §8 #1). | Exercised both sides: `test × qa-automation` — 002 ×3, 003 (T013/T020/T021/T026/T028/T029/T032); `test × backend-service` — the §5 worked example (T010/T020). No categorizer confusion recorded in 001/002/003, but every call was a **human** hand-categorization (the tool didn't exist for its own builds). | Does a scope-based boundary hold once the *categorizer session* (not a human) draws it? First machine test is M4+. |
| A2 | **`endpoint` vs `service` body boundary** — flagged when 001's 3 command skills were `endpoint × ai-agents` but their bodies did service-like work (docs/90 M1 workforce gate, 2026-07-09: *"endpoint/service body boundary booked as v0→v1 evidence"*). | 001 categorization (`endpoint × ai-agents` ×3, `orchestration` tag added to all three). 003 has 3 `endpoint` tasks (T012/T016/T017 command authoring) — same pattern: the deliverable is a command surface, the body orchestrates. | Is `endpoint`-vs-`service` a real type split or a body-content distinction skills should carry? |
| A3 | **Prompt-asset question** — a prompt/deck template is mechanically `docs` (`*.md` rule) but is implementation work; D48 guard holds the Sonnet floor. The underlying type question (a `prompt-asset` type vs a runtime-consumed `docs` carve-out) is **deferred to this review** (D48 rationale; raised at 001 Call 2). | D48 exercised in code in 003: the **3 `prompt`-tagged tasks** (T010 categorizer-prompt, T022 skill-builder-prompt, T030 deck-prep) are `docs × ai-agents`, and the D48 guard routes them to `agt_ai_agents` (Sonnet); the guard's error branch has a committed test (SC-006, `test_assemble.sh` non-Sonnet fixture). 001 booked it; 002 had none (zero-AI ext). | Does `docs` keep hosting prompt-assets behind the D48 guard, or does a `prompt-asset` type earn its place now that 3 real prompt-tagged tasks exist? |
| A4 | **`preserves_behavior: true` is unexercised** — §5's worked example is all `false`; the `refactor-discipline` auto-injection has never fired on real output (taxonomy §8 #4). | **Still unexercised after three features.** 001/002/003 are all greenfield *builds*: 003's categorization is **32/32 `preserves_behavior: false`** (`specs/003-workforce/categorization.md`). The modifier and its auto-injection remain a hypothesis three features in. | The one taxonomy mechanism with **zero** real exercise across the whole v0→v1 window — does it need a refactor-heavy feature before v1, or ship on argument? |
| A5 | **Assembly cap of 3 — does it bind, and what gets dropped?** (taxonomy §8 #5). | 003: the cap was **reached** on T008 (`install.sh`: `installer_hygiene` + `shell_scripting` + `yaml_hooks` = exactly 3 injected, 0 dropped — `agents/assignment.md`), never exceeded on the real roster. The **trim** path is exercised only by the SC-004 golden fixture (>3 candidates → 3 injected + drop logged, `test_assemble.sh`), not by real dogfood work. | Real features clustered at ≤3; is the cap load-bearing yet, or is the trim path still theoretical? |
| A6 | **OQ6 — do `ai-agents` / `devtools-cli` survive a repo that is not SpecSeyal?** (memo §2; taxonomy §8 #2). | **Untested by construction:** all three dogfood features ARE SpecSeyal's own tooling, so `devtools-cli` (12/32 in 003) + `ai-agents` (11/32) dominate exactly as the memo predicted. Per-repo library (D17) contains overfit; no non-SpecSeyal repo has been categorized. | The overfit risk the memo flagged is unfalsified — needs a non-SpecSeyal feature, which the v0→v1 window (all dogfood) never provided. |

## B. The type axis's mechanical-derivation claim (graph coverage)

| # | Booked item | Evidence | → for the review |
|---|---|---|---|
| B1 | **I-13 — graphify emits no nodes for `.sh` / `.yml`**, so a plan turning on installer/hook wiring can't graph-ground its blast-radius/`[P]` claims (docs/90 I-13; 002 R1-S22). | 002 + 003 are almost entirely `.sh`/`.yml`/`.py` + `.md`. In 003, `[P]` fell back to **file-disjointness** (I-13 fallback) for the whole DAG — noted in `tasks.md` (*"all-new code, no graph nodes"*). The D59 baseline (`graph-baseline.json`) types code+doc+concept nodes but still returns "no node" for shell/yaml anchors. | Extend the extractor to `.sh`/`.yml`, or accept file-disjointness as the standing `[P]` basis for plumbing features? |
| B2 | **Type-not-graph-derivable** — taxonomy §2 claims `type` is *mechanical* (keyed off graphify `files=`/`deps=`/phase), but for artifacts graphify doesn't cover (`.sh`/`.yml`, and here `.md` prompt/template files) the derivation rule has **no signal to key off**. | Every 002/003 `type` was assigned by **file-path convention + human judgment**, not graph derivation (e.g. 003 `agt_*.md`/`SKILL.md` → `docs` by the `*.md`-outside-`specs/` rule; `install.sh` → `scaffold` by convention; `assemble.py` → `service`). The "mechanical type" property held in graphify's *own* example (§5) but not on the three dogfood features built since. | Is `type` mechanical-in-principle-only outside the graph, and does that undercut the axis's acceptance test (taxonomy §2: *"if you can't write its derivation rule against graphify signals, it's not a type"*)? |

## C. Seed library & matching evidence (M3-specific)

| # | Booked item | Evidence (`003`) | → for the review |
|---|---|---|---|
| C1 | **`docs × devtools-cli` empty lane** — no seed base accepts `docs` in the `devtools-cli` lane; only `agt_ai_agents` accepts `docs` (docs/90 **I-15**). | 003 T031 (README, `docs × devtools-cli`) matched no lane → `agt_generic` + a **reported** empty lane (FR-016, `agents/assignment.md`). Exactly one task's worth of evidence. | Seed a `docs`-capable `devtools-cli` base, widen `agt_devtools_cli`'s accepted types to include `docs`, or leave `agt_generic` as the honest fallback? |
| C2 | **Provisional bases unexercised** — `agt_backend_service` / `agt_data_persistence` carry `provisional: true` (seeded on the worked example only, not dogfood evidence — S11). | 003 exercised **5 of 7** bases; the two `provisional` bases went **unexercised** (no `data-model`/persistence work in a pipeline-extension build) — `agents/assignment.md` notes. So the two thinnest lanes still have **zero** dogfood evidence three features in. | Keep both `provisional`, promote on the worked-example basis, or drop until a real `data-model` feature appears? |
| C3 | **The 14-gap flywheel signal** — a first feature against a thin seed library produces many ∅-match gaps. | `assemble.py` on 003's real `categorization.md` flags **14 ∅-match gaps** (tasks whose tags miss the 5 narrow seed skills) — quantified, reproducible (`specs/003-workforce/completion-report.md` Key results). The **hand roster the human signed is gap-free** (Sonnet bases judged sufficient); the tool's 14 gaps are the *steady-state* answer the flywheel would act on. **Two levers govern the build volume:** (i) **seed breadth** — more/broader seed skills → fewer gaps; (ii) **gap-batching** — the connected-components dedup already bounds builds to *distinct tag-clusters* (SC-007), so 14 gapped tasks ≠ 14 skills. | How aggressively should the flywheel fire on early features — tune seed breadth, the dedup granularity, or a per-feature build budget? Neither lever has a real steady-state datapoint yet (003's own build was hand-rostered gap-free). |

## D. Cap edge & contract drift

| # | Booked item | Evidence | → for the review |
|---|---|---|---|
| D1 | **20% `general` cap — small-feature edge** (D44; taxonomy §8 #3). The literal cap admits **zero** `general` for features with <5 tasks (`0.20·n < 1`). Unadopted v1 candidate: `count(general) ≤ max(1, ⌊0.2·n⌋)`. | No small feature has stressed it: 001/002/003 are all ≥20 tasks (003 = 32, cap 6, `general` **0/32** — `categorization.md`). `validate-categorization.py` enforces the literal `>` cap in code (integer inequality; a boundary test proves `>` not `≥`, `test_categorize.sh`). The small-feature edge remains **decided-in-the-abstract**, never against real small-feature data. | Adopt the one-task floor, or keep the literal cap until a real <5-task feature forces the call? |
| D2 | **I-19 — spec↔skill drift on workforce auto-mode.** `spec.md` FR-020 + `contracts/commands.md` say workforce auto is *"only within `full_auto`"*; `profile-schema.md` **P4** says `gates.workforce.mode: auto` is valid **standalone** (economic guard, unlike the council gate's correctness handshake). | The shipped `/speckit-workforce-approve` skill (003 T017) followed **P4** — so the drift is now embedded in installed behavior, not just prose. Moot for 003 itself (`gates.workforce: human`). Booked docs/90 **I-19**. | Ratify P4 (standalone economic-guard auto) or restore the `full_auto` restriction — a D-row either way; the skill currently ships the P4 reading. |

## Coverage note

Booked items **not** re-listed above because they were resolved before this window: OQ1/OQ2/OQ3/OQ5 (memo §2, all applied at blessing); I-12 (workforce-gate format → D49); the `web_search` grant question (→ D60, and its gate-vs-dispatch nuance → **D63**, already ruled). The A-2 grant model itself got its **first real exercise** in 003 (the builder's `web_search`, and the D63 correction that it attaches to the *role/dispatch*, never to generated modules) — recorded as shipped behavior, not a v0→v1 open question.
