# Decisions & Ideas — Working Log

> The scratchpad. Jot here first; promote to specs when ripe.
> Convention: **D** = decision made · **Q** = open question · **I** = idea in the parking lot.

---

## Decisions made

| # | Date | Decision | Notes |
|---|---|---|---|
| D1 | 2026-07-08 | GUI is **Agent SDK-native** — it drives sessions, not a wrapper over CLI | Resolves the "Agent-SDK or UI interfaces" note. GUI = control plane. |
| D2 | 2026-07-08 | Agent creator is **hybrid**: specialist library + generated gaps | Persist good generated agents back → library flywheel. |
| D3 | 2026-07-08 | Council v1 uses **llm-council methodology** as-is; evolves to development-oriented critics in v2; **human review always happens** | Human gate is final arbiter. |
| D4 | 2026-07-08 | **Artifacts are the contract** between pipeline layer and platform layer | *Proposed by Claude, tacitly accepted — flag if you disagree.* Enables CLI-first build with zero rework. |
| D5 | 2026-07-08 | Build order: council → git ext → categorize+agents → testing → platform → wizard | Per priority page + D4 rationale. |
| D6 | 2026-07-08 | Principles expanded: + **Resumability**, **Observability**, **Non-technical UX**, **Self-evolving setup**. Cost-awareness deliberately NOT a principle | Cost lives inside observability as a tracked metric. Round 1. |
| D7 | 2026-07-08 | Audience: **Me → work teams + open source, simultaneously**. Central manager tracks local changes across devices/users | Supersedes I-7's "decide later". Forces multi-user auth (Q10). |
| D8 | 2026-07-08 | Automation posture: **configurable per feature** — autonomy profiles declare which gates are human vs auto | Tension with D3 ("human review always happens" at council) — resolved in Round 2. |
| D9 | 2026-07-08 | v1 gate-capable checkpoints: **council gate + workforce gate** (post-tasks + agent assignment). Council gate default-on in every profile; skipping requires explicit full-auto profile | Resolves D3/D8 tension. Spec/clarify and post-implement: not gate-capable in v1. |
| D10 | 2026-07-08 | Council reviews the **plan**, but members hold spec read-access + **graphify query tool** for on-demand codebase grounding | "Reviewers that can check receipts." Session B needs graphify as a tool, not just files. |
| D11 | 2026-07-08 | Remediation is **severity-based**: routine findings patch tasks.md, severe findings reopen the plan | Reopened post-council plan → delta-review question (Q12). |
| D12 | 2026-07-08 | Council v1 is **Claude-only** (mixed Opus/Sonnet/Haiku subagent bench); multi-provider later, Spec Kit-style agent-agnostic | Keep member interface thin so the backend swap is mechanical. |
| D13 | 2026-07-08 | Convergence, v1-simple: **one full round**, chairman classifies (`blocking`/`strong`/`consider`); blocking → one revision + chairman-only delta check → human gate | Claude's recommendation, adopted. Safe because the gate is default-on (D9). |
| D14 | 2026-07-08 | Reopened plans: **tiered** — delta review by default, full rerun if the patch changes approach/architecture; human can override the tier | |
| D15 | 2026-07-08 | Defense deck format: **markdown v1**; presentational rendering deferred to GUI | Adopted by default, no objection raised. |
| D16 | 2026-07-08 | Categorization taxonomy: **hybrid** — fixed core (type × specialization enums) + free tags | Fixed core = deterministic library matching; tags = nuance for gap generator. |
| D17 | 2026-07-08 | Agent library: **per-repo now** (`.claude/agents/`), **central library later via central-library MCP** (I-10) | Entries carry stable IDs + version metadata from day one → central migration is lift-and-shift. |
| D18 | 2026-07-08 | Model policy, two-plane: **Sonnet default for implementation agents; Opus xhigh effort for main thread.** Judgment roles (chairman, analyze/triage) → Opus; mechanical roles (deck prep, categorizer, members) → Sonnet; Haiku unused v1 | Role map **confirmed by default** (review requested Round 4, no corrections). Amendable anytime. Graph-scoring deferred to observability data. |
| D19 | 2026-07-08 | MCP sync: **phase-completion events carry full artifact + status + trace**; mid-phase = status heartbeats only (no artifact bodies); on-demand pull for GUI freshness | Claude's recommendation per Babu's delegation ("phase completion for sure; artifact writes if sustainable"). Sustainable because artifacts are small markdown pushed at boundaries only. |
| D20 | 2026-07-08 | Central manager auth: **single-user token v1, GitHub OAuth later** | OAuth chosen as the designed target — one mechanism serves personal, team, and OSS self-hosters. |
| D21 | 2026-07-08 | GUI MVP: **observe + approve gates first** — read-only tracking + the two D9 gates in the browser; drive capability later | Principle 8 made real: council gate approvable from a phone via the non-technical deck. |
| D22 | 2026-07-08 | Central manager hosting: **claude.narenwebworks.in EC2 (t4g.large)** initially; revisit on load/work adoption | Adopted by default, no objection raised. |
| D23 | 2026-07-08 | Setup wizard: **full scope** — init + graphify/spec-kit + dev server + agent library bootstrap + MCP registration + autonomy profiles | Ships last in build order; packages everything. |
| D24 | 2026-07-08 | Self-evolving setup, year-one boundary: **agent library only** (the flywheel) | Self-tuning config and self-regenerating wizard out of scope until the flywheel proves the pattern. |
| D25 | 2026-07-08 | Git extension v1: **branch-before-plan + per-feature lifecycle** (branch naming from spec ID, phase commit conventions, cleanup). Worktrees-per-wave (I-4) = timeboxed spike, not committed scope | Adopted by default, no objection raised. |
| D26 | 2026-07-08 | The system lives in a **brand-new repo** (A1 revised). **Migration confirmed:** graphify moves into `extensions/graphify/` (monorepo: `extensions/` + `platform/` + `docs/`); `speckit-graphifyy` archived with pointer **at checkpoint α** (D29) | Naming (Q4) needed for repo creation; working name OK, GitHub redirects renames. |
| D27 | 2026-07-08 | License: **MIT** confirmed (A2) | |
| D28 | 2026-07-08 | Billing/auth: **subscription-only through M7.** M0–M5 = interactive Claude Code (plan usage) + no-AI manager service; M6+ programmatic sessions on the **Agent SDK monthly credit** (Pro/Max, covers Agent SDK + `claude -p`, separate from interactive limits) — no API keys | Keep `ANTHROPIC_API_KEY` unset on build machines (it overrides subscription auth). API keys only if work-team production automation demands them later. |
| D29 | 2026-07-08 | New repo visibility: **private until checkpoint α**, public when the full CLI pipeline demonstrably runs (end of M4) | Best-first-impression launch. If migrating (D26), old repo archival waits for α too — archiving earlier would remove graphify's only public home. |
| D30 | 2026-07-08 | Name: **SpecSeyal** (செயல் *seyal* = action; spec → action) | Babu's own candidate. GitHub: zero repos; npm: free — fully virgin namespace, verified 2026-07-08. |
| D31 | 2026-07-09 | Graphify migration by **`git subtree add` (no `--squash`)**, not plain copy or filter-repo | Upstream had exactly 1 commit, so history preservation was free and `git-filter-repo` unnecessary. Extension keeps its own `install.sh`/`uninstall.sh` and installs from `extensions/graphify/`. Verified: install → idempotent re-install → clean uninstall. Behavior unmodified (M0 constraint). |
| D32 | 2026-07-09 | **Phase state is inferred from artifacts. No state file, ever.** A phase is complete iff its artifact-out exists *and* validates | Principle 3 made mechanical. Two consequences: (a) gate decisions are **artifacts, not events** — a human approval is a section appended to `decision-record.md` / `assignment.md`, never a row in a database; (b) `branch` is the sole phase whose state lives outside `specs/` (the git ref itself). |
| D33 | 2026-07-09 | Autonomy profile: **absent `profile.yaml` ⇒ both gates `human`**. `gates.council.mode: auto` is invalid without `full_auto: true`, and `full_auto: true` is invalid unless both gates are `auto` | D9's "explicit full-auto profile" encoded as a two-key handshake. `full_auto` is redundant *as data* and load-bearing *as ceremony* — it makes the diff that disables adversarial review impossible to read as an accident. Missing file = safest profile, never fastest. |
| D34 | 2026-07-09 | Agent library entries are **Claude Code agent files** carrying SpecSeyal metadata under a single `specseyal:` frontmatter key; the central-sync join key is the immutable `id`, never `name` | Ordering is the design: the file must dispatch as a subagent today and sync to the central library (I-10) tomorrow with no conversion step. Joining on `name` would orphan a specialist's stats on first rename — and those stats *are* the flywheel (D24). |
| D35 | 2026-07-09 | Traces: **append-only JSONL** at `specs/NNN-feature/traces.jsonl`; **no message bodies**; `cost_usd` is `null` under subscription billing — **tokens are the unit of account** | No-bodies serves context hygiene *and* makes traces safe to sync to a multi-tenant manager without redaction review (D7/D20). Synthesizing `cost_usd` from public API rates was rejected: a number that looks like money but was never charged gets summed, charted, and eventually believed. D19's phase-event envelopes are defined in the same contract, so M5 is plumbing, not design. |
| D36 | 2026-07-09 | Taxonomy v0 (**DRAFT — awaiting Babu**): **9 types × 10 specializations + `general`**. No `integration` type. `type` is derivable from graphify's emitted signals; `specialization` is not | **Superseded same day by D42** (OQ1 overruled → 8 types; cardinality now 8 × 11). The draft-state record is kept as history. `integration` rejected because `speckit-implement-parallel` step 4 assigns shared-manifest wiring to the *orchestrator*, never a subagent — a type no task can hold is dead schema. The derivability asymmetry is what justifies two axes at all, and is why a Sonnet categorizer suffices (D18). The six OQs it logged are all resolved in D42. |
| D37 | 2026-07-09 | The categorize extension writes **`categorization.md`** and does not mutate `tasks.md` | Principle 1 (one artifact out). Keeps phases independently resumable and keeps `/speckit-analyze`'s input stable. `tasks.md` already has two writers (spec-kit + graphify's wave appender); a third would make it the pipeline's shared-mutable file — the exact hazard graphify exists to detect. |
| D38 | 2026-07-09 | The council deck is **not round-scoped**: `council/defense-deck/` is overwritten in place on revision. Git on the feature branch (D25) is the deck's version store | Round-scoped artifacts stay under `round-N/`. Copying the deck per round duplicates what git already holds. `opinions/` holds stage 1 (`A.md`) and stage 2 (`peer/A.md`) alike, per docs/10 §3. |
| D39 | 2026-07-09 | Two directories named `contracts/` — `docs/contracts/` (SpecSeyal schemas) vs `specs/NNN/contracts/` (spec-kit API contracts) — **documented, not renamed** | The collision is inherited from spec-kit. Renaming either breaks a convention someone else owns. Always qualify the path. |
| D40 | 2026-07-09 | **Composability (Amendment A-1). The library's unit of storage is the SKILL, not the agent.** An agent is an assembly at dispatch: `base (Sonnet, D18) + base specialist config selected by (type, specialization) + skill modules selected by tags + tool grants aggregated from those skills` | Blessed by Babu, memo `docs/reviews/2026-07-09-taxonomy-v0-review.md` §3. Consequences, all normative: (1) free tags gain a **third job** — skill selection — beside ranking and gap-briefing; (2) **the gap generator becomes a skill builder** — on ∅-match it authors a `SKILL.md`, not a whole agent; the flywheel (D24) persists skills; (3) I-10 is reframed as a **skill registry**; (4) guardrails: **assembly cap of 3** injected skills per dispatch (trim by tag-rank, never exceed), and **additive-only** — skills extend the base, never override it; (5) `preserves_behavior: true` auto-injects the `refactor-discipline` skill, closing OQ1's "differently-disciplined agent" requirement without a type. New contract: `docs/contracts/skill-module.md`. |
| D41 | 2026-07-09 | **Tool grants, including web search (Amendment A-2).** Tool access is a **per-skill declared grant**, not an agent-level default. The assembled agent's grant set = the union of its skills' declarations | Blessed by Babu, memo §4. **The workforce-gate roster (D9) MUST display each assembled agent's grant set — approving the roster is approving network access.** Binding on M3 (roster artifact) and M5 (gate view). Example: a `scaffold` dependency-version-lookup skill declares `web_search`; a `data-model` skill declares nothing. *Applying-session interpretation, flagged: bases keep an immutable core toolset (`Read, Write, Edit, Bash, Glob, Grep`); only elevated access is skill-declared.* |
| D42 | 2026-07-09 | **Taxonomy v0 BLESSED with amendments.** Six OQ verdicts: **OQ1 overruled** (`refactor` is a modifier, not a type → 8 types + boolean `preserves_behavior`); OQ2 upheld (exactly one specialization — cross-cutting signal recovered by tag→skill injection, not `secondary[]`); OQ3 upheld **with a cap** (`general` ≤ 20% of a feature's tasks; over-cap categorization **fails**); OQ4 upheld (`T030` is `qa-automation`); OQ5 upheld (`scaffold`/`infra` stay separate — composability removed the cost of near-empty lanes); OQ6 upheld (`ai-agents`, `devtools-cli` stay). **Resulting cardinality: 8 types × 11 specializations.** | Memo §2. `docs/contracts/taxonomy-v0.md` status → **v0 BLESSED (normative)**; the M0 exit gate closes. v0→v1 (three real dogfood features) re-examines OQ4, OQ6, the small-`n` behavior of the `general` cap, the unexercised `preserves_behavior: true` branch, and whether the assembly cap binds in practice. |
| D43 | 2026-07-09 | **Trace record carries the assembly (authorized amendment to `trace-schema.md`, now 1.1).** Each record gains (a) `skills: [{id, version}]` — every skill injected into the dispatch, `[]` when none; (b) `elevated_grants: [...]` — the union of skill-declared elevated grants active for the dispatch. `agent_id` redefined as the assembled **base**. Skill `stats` aggregation (`agent-library-schema.md` §5) rekeyed on **skill `id@version`**; the BLOCKED flag removed | Unblocks the flywheel, which D40 had inadvertently stranded — the pre-amendment record named no injected skills, so skill-level attribution was uncomputable. **Rationale:** flywheel stats must segment per skill *version* (D17/D24) — a regressed 1.1 must not coast on 1.0's record; and grants-in-traces completes the D41/A-2 audit chain — the gate-approved elevated-grant set is now recorded on the dispatch that used it. Attribution is combinatorial (a skill is credited only alongside its ≤3 assembly-mates), stated honestly, sufficient for a promotion bar. |
| D44 | 2026-07-09 | **Flag adjudications from the taxonomy-amendment application.** **Flag 2:** the literal 20% `general` cap **stands for v0**; the small-feature edge (n<5 ⇒ zero `general`) goes to the taxonomy §8 v0→v1 checklist with `max(1, ⌊0.2n⌋)` noted as an **unadopted** candidate. **Flag 3:** two-kind storage **confirmed** — base specialists are **curated-static** (human + D-row only; they do not learn), **skills alone evolve** via the flywheel. **Flag 4:** **core toolset** (`{Read,Write,Edit,Bash,Glob,Grep}`, assumed, never listed) vs **elevated grant** (anything beyond it, skill-declared, gate-visible) codified as **defined terms**; A-2's "not an agent-level default" clarified to govern **elevated access only** | Closes the four items flagged rather than improvised during the D40–D42 application. Each adjudication is written where it was flagged: cap edge in `taxonomy-v0.md` §4/§8; storage split in `agent-library-schema.md` (assembly preamble); toolset terms in `agent-library-schema.md` §4.1. |
| D45 | 2026-07-09 | **M1 tooling bootstrap — real spec-kit + graphify installed into specseyal itself for dogfooding.** `specify init --here --integration claude` (v0.12.5, offline/bundled) installs `.specify/` + the `/speckit-*` commands as `.claude/skills/`; graphify's `install.sh` adds its three skills and registers the `before_plan` / `before_tasks` / `before_implement` hooks. The graph (`/graphify`) is built lazily, before the plan phase that first consumes it | Makes the docs/05 dogfooding rule concrete ("M1 onward the workflow builds itself; M0 was the only plain-session milestone"): M0 deferred spec-kit init (graphify was verified against a *synthetic* target), so M1 installs the real pipeline in-repo. No pre-existing file modified — only `.claude/` and `.specify/` added. This spec-kit resolves the active feature from `.specify/feature.json` (**branch-agnostic** — git branch and feature-tracking are decoupled), which is **gitignored** as transient session state: orthogonal to SpecSeyal's contract surface and consistent with D32 (durable phase state lives only under `specs/NNN/`). Tooling committed on the feature branch (first feature; reaches `main` when `001` merges at M1 completion). Council extension's on-disk home: `extensions/council/` (mirrors `extensions/graphify/`). Constitution left as the stock template — seeded from docs/00 §2 only if the plan phase requires it |
| D46 | 2026-07-09 | **M1 spec-review adjudications (`001-council-extension`).** (1) **FR-017 split at the trigger:** v1 ships the reopen *interface* only — `/speckit-council` accepts a **manually-invoked** reopen (`delta` = plan diff + triggering finding as the context package; `full` = full rerun), appending a `## Reopen` record naming the tier (delta default; proposed at triage, human-overridable). **Automated severity routing from `/speckit-analyze` (D11) is OUT of v1 scope** and ships with analyze integration. (2) **Build-phase tracing = option (a):** this feature's interactive build sessions predate the tracing capability, so they write no `traces.jsonl` and are not backfilled — a bootstrap fact, not a conformance gap; the first real traces are the council's first *live* run (M2), which is SC-002's first measurement and M1's exit test. (3) **Spec-template framing approved as adapted, with a standing rule:** every `Constraints & Assumptions` entry MUST cite a D-row; anything that cannot is a design choice and belongs in the plan, not the spec. (4) **Reduced-grounding must be visible:** a no-graph (deck-only) council run surfaces an explicit reduced-grounding flag in `suggestions.md` **and** the decision record, visible at the human gate — never presented as fully grounded. | Spec **APPROVED**. Refines docs/10 §5 for v1 (reopen is a manual interface; the automated `/speckit-analyze` trigger is deferred to analyze integration); adds/rewrites spec FR-017, adds FR-019 + SC-008. Rule (3) is **general spec-hygiene policy**, not feature-scoped — it binds every future feature spec's Constraints & Assumptions section. Applied to `001-council-extension/spec.md` in the same commit as this row. |
| D47 | 2026-07-09 | **Trace token capture is exact-or-null, with a recorded method (`trace-schema.md` → 1.2).** Raised as **S1** by the bootstrap council at the `001-council-extension` plan gate. `tokens` may now be **`null`** (the whole object), and every record carries **`capture_method ∈ {sdk, transcript, unavailable}`** naming how the count was obtained — `sdk` (Agent SDK/API usage metadata, exact, the M6 path), `transcript` (parsed from the Claude Code transcript JSONL, exact, the interactive path), or `unavailable` (`tokens: null`). **Never a fabricated estimate** — the same ethos as `cost_usd: null` (D35). Spend rollups skip null-token records and MUST report their count so completeness is visible. | Resolves plan risk **R1** without blocking the M1 exit: SC-002 measures council spend *with the capture method recorded* — best-effort exact interactive, guaranteed exact under the SDK (M6). A **timeboxed Phase-4 spike** attempts `transcript`-based capture for subagent sessions; spike failure = `capture_method: unavailable` per this row, **not a blocker**. Fixture `specs/000-sample/traces.jsonl` updated in the same commit (contract-change discipline). |
| D48 | 2026-07-09 | **M3 assignment guard: `prompt`-tagged tasks keep the D18 Sonnet floor.** Raised at the `001-council-extension` bootstrap **workforce** gate (Call 2). A prompt/deck template is mechanically typed `docs` (taxonomy §2 `*.md` rule), and `agent-library-schema.md` §4 / `taxonomy-v0.md` §3 let a **`docs`-only specialist take any model D18 permits** — so, absent a guard, a `prompt`-tagged task could route to a non-Sonnet docs specialist and escape the implementation floor. **Guard:** when M3 builds the assignment step, a task carrying the `prompt` tag MUST assemble onto an implementation specialist under the **D18 Sonnet floor**, never onto a docs-exempt one. | Prompt authoring is `ai-agents` *implementation* work wearing a `docs` type — the taxonomy friction booked in `001`'s `categorization.md` (Call 2). The guard lives in M3's assigner; recorded now so it is not lost. The underlying type question (a `prompt-asset` type vs a runtime-consumed `docs` carve-out) is deferred to the taxonomy §8 three-feature review. |
| D49 | 2026-07-09 | **Workforce-gate record format defined — `## Workforce Gate` (resolving I-12; `artifact-layout.md` → 1.1, new §8).** The gate exists (D9) and its home was already fixed at `agents/assignment.md` (artifact-layout §2 + D32), but its *format* was undefined — unlike the council gate's `## Human Gate` (decision-record.md) — so `001` had to record it as a flagged addendum in `council/decision-record.md`, a contracts bug. **Now §8:** reviewer · decision (`approved`\|`approved-with-notes`\|`rejected`) · reviewed · a **Roster** table (one row per assembled agent, D40) carrying **Model** (D18/D48 Sonnet-floor auditable) and a **mandatory `Elevated grants`** column (D41/D44 — approving the roster is approving network/tool access). | Chose "add a gate-record section" over "extend decision-record.md": the workforce gate is not a council artifact and decision-record.md is council-owned (§6) — homing it there breaks ownership. artifact-layout.md already owns the phase table, the `assignment.md` entry, and the D32 gate-placement rule, so the format completes an existing responsibility. `001`'s addendum is a superseded **bootstrap** exception, not retrofitted (append-only). M3's `assignment.md` contract may relocate §8 into itself. |
| D50 | 2026-07-09 | **Conformance rule-5 meta-feature carve-out (resolving I-11; `artifact-layout.md` §7).** Rule 5 ("no file outside `council/` mentions `opinions/`") false-positives on a feature whose subject *is* the council — it must *describe* `opinions/` to specify it (flagged at `001`'s M1 close). **Amended:** a feature is **exempt iff its `spec.md` carries a fixed marker** — `> **Rule-5 exempt (meta-feature):** <reason>. See <D-row>.` — detected by grep (`^>\s*\*\*Rule-5 exempt \(meta-feature\):`); if present, rule 5 is skipped for that feature alone. `001` marked inaugural-exempt (marker added to its `spec.md` in this commit — contract-change discipline). | Keeps rule 5's ethos — mechanical, CI-runnable, no judgment in the grep — by pushing the judgment into a **one-time, reasoned, human-authored marker** that can't be applied silently and stays **local** to the feature. Exempt-feature opinion hygiene falls back to the skill-enforced runtime invariant (§4) + the human gate. Defines the carve-out only; **building** the checker (I-11's main body) stays open. A central spec-ID allowlist was rejected as non-local. |
| D51 | 2026-07-09 | **M2 spec-review adjudications (`002-speckit-ext-git`).** Four rulings on the git-extension spec, applied in one commit (the D46 pattern). **(1) FR-001 RATIFIED — branch born at `specify`:** the feature branch is created co-incident with `specify` (as soon as the spec ID is assigned, before `spec.md` is committed), so every feature artifact lands on the branch and the base stays clean. **`artifact-layout.md` §2/§3 amended** as an authorized contract change (branch row co-incident with specify; context-in = spec ID; → 1.2). **(2) FR-011 STRENGTHENED — `--no-ff` unconditional:** the linear fast-forward exception is removed; integration is *always* a merge-commit because that commit is the feature's **completion anchor** — the D19/M5 phase-event binding target — and preserves first-parent feature enumeration; an anchor must exist unconditionally. M1's `main` ff-merge is **grandfathered** as pre-policy and never rewritten. **(3) FR-008/009 AMENDED:** re-approval after a stale-approval hard-block follows the artifact's **gate type** — council-approved artifacts route through D14's reopen tiers (`delta` default, `full` if the approach changed) via the manual reopen interface (`001`-FR-017); workforce-gate artifacts re-run the gate. **(4) Ratified as scoped:** I-4 worktrees firewalling (FR-015, P3), the stale-approval hard-block, and explicit-invoked cleanup. | Spec **APPROVED**. Mirrors D46 (spec-review adjudications as a D-row). The §2 amendment resolves the branch-birth spec-vs-contract mismatch `002`-FR-001 flagged rather than silently reconciled. FR-011's anchor rationale ties merge policy to a downstream binding target (D19/M5) and makes "preserve the phase trail" (D25) concrete: `git log --first-parent` enumerates features. Applied to `002/spec.md` + `artifact-layout.md` §2/§3 in the same commit as this row. |
| D52 | 2026-07-09 | **FR-011 completion anchor becomes a `complete/<spec-id>` tag; `ff`-permitted (partial supersession of D51, owner-authorized at the `002` council gate).** D51's merge clause required an **unconditional `--no-ff` merge** as the anchor. Superseded: the anchor is now a **mandatory annotated tag `complete/<spec-id>`** created at cleanup — immutable, enumerable via `git for-each-ref refs/tags/complete/*` — serving the D19/M5 binding + per-feature enumeration purpose **independent of merge topology**, so merge policy becomes **`ff`-permitted** (merge-commit only when the base diverged). | Raised as `002`-round-1 **R1-S27** (member C; adopted beyond its own framing — mandatory, not optional). **Supersedes D51's merge clause ONLY** — D51's branch-at-`specify` (FR-001) and the FR-008/009 stale-approval reopen routing **stand unchanged**. M1's `main` ff-merge grandfather is now **moot** (kept as history — ff is permitted, so no exception is needed). A tag anchor also lets solo/linear features stay fast-forward-clean. Spec delta to FR-011 authorized by Babu at this gate; applied to `002/spec.md` + reconciled in plan/data-model/contracts. |
| D53 | 2026-07-09 | **Hook enforcement is prose-level in v1; a mechanical `HookExecutor` is deferred to M6 (`002`-round-1 R1-S08).** The first live council found the repo's hook system has **no code-level enforcement** — the `HookExecutor` every stock skill defers to is referenced ~11–13× and implemented nowhere — so even a corrected `optional: false` + stop-on-nonzero hook (R1-S01/S02) is enforced only by an LLM following the skill's prose. **v1 accepts this honestly:** the git-ext plan names enforcement as prose-level and downgrades the "falsifiable without a model call" claim; a **mechanical** `HookExecutor` is **deferred to M6**, where the Agent SDK orchestrator gives programmatic enforcement. | The M2 conformance-checker drift lint (R1-S29, wired into D50) partially compensates meanwhile by catching that specific drift class mechanically. Recorded so the deferral is a **decision, not an omission** — the guarantee gap is known, bounded, and scheduled. |
| D54 | 2026-07-09 | **Wave-worktree spike (`002`-FR-015/T018) hard-capped at ≤2h; a null result is a success (owner-confirmed at the `002` workforce gate).** The spec/plan said only "timeboxed"; `002`'s `/speckit-analyze` flagged the magnitude as unquantified (M2 finding). **Ruling (Babu, workforce-gate note 1):** T018 is hard-capped at **≤2h** of build time, **1 attempt**; the outcome is recorded in `implement.log.md` + an I-row **either way** — a spike concluding "worktree-per-wave doesn't pay" is a **success, not a failure** (D25 scopes it as a knowledge deliverable, not committed behavior). | Fixes the one ambiguity a bounded experiment cannot carry — an unbounded "timebox" is untestable (SC-008 asks it conclude "within its timebox"). Booked at gate-close per the owner's note-4 hygiene rule (this D-row lands in the `gate(002)` boundary, not deferred). Firewall unchanged (FR-015): no v1 hook/command depends on the outcome. **→ RESOLVED at M2 close (spike ran wave 10 → ABANDON; resolves I-4).** |
| D55 | 2026-07-10 | **FR-008 gate-section reference = well-known-path convention; pointer-ownership inverted to the gate command (`002` M2 close, Finding-2).** The git ext records the SHA binding in its own `specs/NNN/gates.yml` (D-R3 / R1-S09/S20) and — **verified at build** (`on-council-approve.sh` leaves `council/decision-record.md`'s checksum unchanged) — writes **nothing** into the council-owned gate artifact. FR-008's "the gate section carries a one-line reference to `gates.yml`" is satisfied by **convention** (the well-known path `specs/NNN/gates.yml`), not a git-ext co-write. **Ownership inverted:** adding the pointer line into the `## Human Gate` section is the **gate command's** job (`speckit-council-approve`, which owns `decision-record.md`), applied on its **next touch** (the M3 update to `001`'s council flow). | Keeps principle I clean (no second writer of a council artifact) and dissolves the last remnant of the D-R3 supply/record seam. `verify-gate` reads `gates.yml` directly (the source of truth), so the section reference is human-discoverability only — correctly owned by that artifact's writer, not the git ext. Adjudicated by Babu at the M2 close-out. |
| D56 | 2026-07-10 | **Council ceremony tiers + lazy context grounding (M3 cost review — lands the M2 5.25M finding).** The first live council (`002`) cost **5,249,858 billable tokens** for one round; the transcript sum ran ~3.4× the Agent-return aggregate because each member's 25–38 graphify tool-call turns churned `cache_creation` (2026-07-09 log). A **`council_tier`** knob (`profile.yaml`, `profile-schema.md` §7 → 1.1; per-feature, default resolved from `council-config.yml`) selects the ceremony: **`full`** (default) = the M2 baseline — 1 deck + 5 stage-1 opinions + **5** per-member stage-2 peer + 1 chairman = **12 sessions**; **eager** context (every member reads deck+plan+spec up front); no output cap — byte-for-byte the pre-D56 behavior. **`standard`** (M3 opts in) = 1 deck + 5 stage-1 + **ONE consolidated peer-critique session** (`opinions/peer/consolidated.md`) + 1 chairman = **8 sessions**; **lazy** context (the technical deck is the sole up-front read; plan/spec/graph resolved on demand only to verify a specific deck claim); a **member output cap** (≤6 suggestions/stage-1 member, ≤15 consolidated). The four levers are additive and independent; the chairman reads all stage-1 opinions + whatever peer files exist (10 files under `full`, 6 under `standard`). | Records the tiers decision the M2 finding pointed at but never wrote (the "weigh before the trim-member-count lever" note; `council_tier: standard` is now the **first** cost lever, `member_count` the second). **Default = `full`** by deliberate choice — it matches profile-schema's "absent ⇒ safest/most-thorough" ethos (P1/T1), so no future feature silently loses review rigor; `standard` is **opt-in and measured before adopted** (docs/05 "tune only after SC-002"). M3's Phase-2 council runs `standard` and reports `council_spend` per-stage (deck/stage-1/consolidated-peer/chairman) + per-member `plan.md` read-rate against the 5.25M `full` baseline; a later D-row may flip the default once the evidence lands. Tier changes **ceremony only** — never the D18 model map (Sonnet members, Opus chairman, both tiers) nor who signs (that is `gates.council.mode`/`full_auto`). **Landed on `main` as M3 Phase-0 infrastructure**, additively: contract touch `profile-schema.md` → 1.1 (`council_tier` optional, default `full`; fixture `000-sample/profile.yaml` gains `council_tier: full`); impl = `council-config.yml` `tiers` block + `/speckit-council` orchestrator tier branch + `chairman-prompt`/`member-prompt` generalization (consolidated peer). Open sub-choices flagged for the owner (trivially reversible): cap = 6/member (tunable like `member_count`); consolidated peer traces as `council-member`. |
| D57 | 2026-07-10 | **Cross-extension seam convention — hook points over source edits (promotes I-14 to normative; `artifact-layout.md` §9 → 1.3).** Before M3 wires categorize/agent-assign coupling into the pipeline, the I-14 rule is made a contract: **cross-extension coupling attaches at a hook point** (`extension.yml` `after_*`/`before_*`), **never a source edit to another extension's installer-overwritten file** — every `install.sh` does `rm -rf` + `cp -R` of its own installed tree, silently wiping a foreign edit (the M2 live bug: graphify's reinstall wiped git-ext's per-wave edit at implement wave 12 until it was relocated into graphify **source**, R1-S17). **Rule:** prefer a hook; if a source edit into another extension is genuinely unavoidable, it lives in the **owning** extension's `extensions/<name>/` **source** (so that extension's own reinstall carries it) and is **reinstall-survival-tested** (`extensions/git/test/run.sh` §3 is the model). | Extends §6's artifact-ownership ethos ("no writer touches another's artifact") to the **code layer** ("no extension patches another's installed source"). Homed in `artifact-layout.md` beside §6 rather than a new contract: the two boundary rules are one principle, and `on-council-approve.sh` already cites §6 for its reinstall-survival design, so a developer reasoning about seams lands in one file. `graphify` shipping the `002`-edited `speckit-implement-parallel` (per-wave commit) is the standing precedent. Resolves the "establish before M3 wires in" clause of I-14. |
| D58 | 2026-07-10 | **M3 spec-review adjudications (`003-workforce`).** Babu ratified the workforce-pair spec with tightenings (D46/D51 precedent). **(1) Pipeline order RATIFIED + `artifact-layout.md` §2 reconciliation authorized:** the operative order is `tasks → analyze → categorize → assign → workforce-gate` — `analyze` runs **before** `categorize` because `analyze` may patch `tasks.md` (D11 remediation), so classifying first would categorize tasks that can still change; **classification follows stabilization**. `artifact-layout.md` §2 (which listed `categorize` before `analyze`) is **amended → 1.4** (cites this row) during this feature. Flagging rather than silently fixing was correct — again. **(2) Clarify resolution RATIFIED + tightened:** determinism = assembly-only against a fixed library snapshot (gap-free runs byte-reproducible; skill-builder runs excluded, LLM authorship). **Tightening:** the boundary must be **checkable from the artifact** — the roster records, per injected skill, `library` (present at run start) vs `built` (authored this run), so "gap-free" is a verifiable property (zero `built`), not an assertion (new spec **FR-022**; SC-005 refined). **(3) Phase-0 judgment calls RATIFIED as logged** (D56 `full`-default + cap-6; D55 triage/approve consistency). **(4) Smalls accepted:** slug `003-workforce`; `categorization.md` file *format* is plan-level (D46 rule 3, a design choice). **(5) Plan-time verification (report, do NOT re-open the spec):** (a) the 20% `general` cap is an FR with failure behavior (FR-004 / SC-002), not prose; (b) the D48 guard has an SC proving enforcement by code (FR-014 / SC-006); (c) assembly cap ≤3 + additive-only are testable constraints (FR-007 / FR-011 / SC-004); (d) the `web_search` question reaches the council with **both options costed** — what the builder loses without it vs the grant machinery it exercises with it. | Spec **APPROVED**. Mirrors D46/D51 (spec-review adjudications as a D-row). §2's amendment resolves the pipeline-order spec-vs-contract mismatch `003` flagged: analyze-before-categorize is now normative in both the spec and the phase table (the §3 resume walk runs it in that order). The provenance-checkability tightening turns SC-005 into an artifact property. Applied to `003/spec.md` (FR-022, SC-005, pipeline constraint, clarifications note) + `artifact-layout.md` §2 (→1.4) in the same commit as this row. Next: `/speckit-plan` (incl. the proposed seed set) → the first live `standard`-tier council (D56). |
| D59 | 2026-07-10 | **M3 tiered-council baseline graph is snapshotted + committed — a scoped, one-time exception to D45's "never committed" working graph (`003` Phase-2 step 0).** The first `standard`-tier council run is a **baseline measurement** (D56) whose grounding deserves exact reproducibility. After the `.gitignore`/D45 "never committed" convention — and the fact that the 5.25M `full` baseline (M2) was recorded via **stats only**, not a committed graph — was surfaced, the owner chose (informed) to **commit a graph snapshot**: at Phase-2 **step 0**, the regenerated graph is copied to **`specs/003-workforce/graph-baseline.json`** (a committed feature artifact, *outside* the gitignored `graphify-out/`, capturing the exact graph the council reviews). The **live** `graphify-out/graph.json` **stays gitignored** — D45's transient-working-graph principle is unchanged; only this per-feature baseline snapshot is committed, so there is no ongoing diff-noise. | Scoped exception, **not** a repeal of D45. Asymmetry with the M2 full baseline (stats-only record) is accepted for exact `standard`-tier reproducibility. **Rationale (owner):** stale receipts masquerade as ground truth — worse than the honest "engineer-assertion" labeling absent nodes get (S22/I-13) — so the baseline earns a fresh graph, and a committed snapshot makes the measured run reproducible. **Executed at Phase-2 step 0 (fresh session):** `/graphify . --update` (incremental **semantic** re-extraction of the changed `.md`/`.yml`/`.sh` — a subagent-using pipeline, hence run in the fresh session, not this session's tail) → `cp graphify-out/graph.json specs/003-workforce/graph-baseline.json` → commit (refs this row) + log the fresh node/edge stats in the session log (as M1/M2 did) → then `/speckit-plan`. |
| D60 | 2026-07-10 | **`web_search` GRANTED to the skill-builder role — the system's FIRST elevated grant (`003` round-1 triage, S20/owner).** Resolves the spec's OPEN item; the plan had recommended Option A (no grant). The builder declares `grants: [web_search]` in its skill-module frontmatter (**A-2 — a skill-declared grant, not a role-level ambient default**); it surfaces on every roster whose assembly includes the builder path — **`003`'s own workforce gate displays it, the first elevated grant a human approves** (D41, `artifact-layout.md` §8 W2) — and is recorded in `traces.elevated_grants` (D43) on any builder dispatch that searched. **S17's stale-knowledge flag ships as a complement:** authoring a module for a framework past the model's training cutoff *without* searching stamps `provenance.stale_risk: true`. | **Rationale (owner):** a builder authoring skills for post-cutoff frameworks without search produces **confidently-stale** modules — the exact failure A-2's grant machinery exists to make visible and governable; granting search + flagging non-search is the honest resolution. First real network reach in the system; the audit chain (skill-declared → gate-visible → trace-recorded) stays intact. Supersedes the plan's Option-A recommendation. Applied to `plan.md` § web_search + `workforce-config.yml` (`skill_builder.web_search: true`) in the triage commit. |
| D61 | 2026-07-10 | **`council_tier` default flips to `standard` — D56 amended (`003` round-1 triage, owner).** The M3 measurement landed: the first live `standard`-tier council cost **2,830,593 billable tok vs the 5.25M `full` baseline (−46.1%)**, 8 sessions vs 12, and **rigor held** — 2 real blocking defects (an unsorted-`set` determinism break S01; an unreachable workforce gate-write path S02), receipts-checked corrections (the "degree 15" claim corrected to **degree 1** via `graphify explain`, S06), no opinion below bar. So `standard` becomes the **default** `council_tier`; **`full` stays opt-in**, recommended (docs/05 sizing guidance) for **L-sized or architecture-changing features**. | **Amends D56's "default = `full`" clause ONLY** — D56's four levers + tier mechanics are unchanged; who signs is unchanged. **Honest caveat: N=1**, so a **revisit clause**: if any `standard` round misses a defect a `full` round would plausibly have caught, reopen this default. Phase-0's `full`-default was correct *until measured*; flipping on evidence is the observability principle (D6) cashing out — **the first tuning decision this system has made on its own data.** Applied to `council-config.yml` (`council_tier: standard`) in the triage commit; reinstalled + reinstall-survival re-checked. |
| D62 | 2026-07-10 | **Deck-prep improvement — mine council read-rate to enrich the deck (`003` round-1 triage, owner).** The `standard`-tier lazy-context run produced a per-member `plan.md` read-rate of **4/5** (spec 1/5, graph 3/5): four of five members opened `plan.md` because the deck under-served them. **Task added to `003`'s build scope:** mine this round's five member transcripts for *which* `plan.md` sections the readers actually pulled, then **enrich the deck-prep template** (`extensions/council/` **own source**, D57 S2; reinstall after) so those high-demand sections ride in the technical deck by default. Also: the consolidated-peer prompt gains a **firmer status-only clause** (the round's one process nit — this round's consolidated reviewer returned process detail beyond its one line; a D56 tier-mechanics patch). | **After-metric:** the `plan.md` read-rate at the *next* council run — target: the deck trusted by a **majority** of the bench (fewer members needing `plan.md`). A concrete, measurable council-quality improvement driven by the D56 read-rate telemetry the tier was built to produce. Booked as a task in `003`'s revision (build-time); the member-prompt status-only patch lands with the D61 council reinstall. |
| D63 | 2026-07-11 | **Capability authorization ≠ dispatch approval — a workforce gate approves *dispatches* with the grants their *work* needs (`003` workforce gate, approved-with-notes, owner).** The `003` by-hand build roster hand-attached `web_search` to the two builder-path rows (**T022** authoring `skill-builder-prompt.md`, which *declares* `grants: [web_search]`; **T025** wiring the gap-dispatch) as a "display convention." **Owner correction at the gate:** an A-2 grant derives **only** from a skill declaration *injected into an assembly* (FR-013 — "nothing else grants anything"). Neither T022 (writes a prompt file) nor T025 (wires a handoff) injects a `web_search`-declaring skill, and neither task's *work* reaches the network → both read **`elevated: none`** (merged into their honest `agt_ai_agents` assemblies). **D60 stands untouched** as *capability authorization*: the skill-builder role is authorized to hold `web_search` (the declaration T022 authors). That grant reaches a **roster** — and its **first human signature** — only at a **genuine builder dispatch**: a real gap event, or the **S08 integration test** that runs the real skill-builder on a gap fixture, where the builder's assembly injects the declaring module and the **A-2 union surfaces it honestly** — the mechanism working, not a hand-attachment. | **The distinction:** *capability authorization* (D60 — the system may grant X to role R) and *dispatch approval* (a gate — this dispatch runs with the grants its work needs) are **distinct acts**. Keeps the audit invariant **exact**: network reach on any roster = ⋃ injected-skill grants, never a curated display. Consequence: the honest reading of "the first elevated grant a human approves" (D41) is that it lands at a real dispatch that genuinely reaches the network (a gap event / the S08 test), **not** a build task that doesn't. Corrects the `003` hand-roster; `agents/assignment.md` ★ + the gate override record it. First correction the *gate itself* (not the council) has made to an assembly. |
| D64 | 2026-07-12 | **Meta-feature gate-machinery handling — grandfather the checks it can't apply to itself; fix the bugs that are its own deliverable (`003`, owner Option A).** `003` *builds* the workforce gate-write machinery (`on-gate-approve.sh` / `after_workforce_approve` / `verify-gate workforce`), so that machinery cannot govern `003`'s **own** implement — the circularity S28 first hit (a feature's own gate is unbound). **Extends S28:** not just the binding *record* is grandfathered but the **live** `before_implement` / per-wave `verify-gate workforce` **checks** — `003`'s 10-wave implement ran **grandfathered N/A** on the hand-signed gate (owner chose **Option A** at the hard-block, over fix-first or bind-by-hand). The per-wave **commit** half of the machinery was still used (note 4). **Defects the machinery revealed were triaged, never papered over:** the two pre-existing/general ones were **booked** (**I-16** no bootstrap binding path; **I-17** `[X]`-marking stales the workforce binding — hits every feature), the one that is `003`'s **own deliverable** was **fixed** (**I-18** `gates.sh` workforce path — shipping the S02 gate-write broken would defeat the council's blocking finding). | The honest split: a meta-feature can't be governed by the guard it is building (grandfather the *checks*, extending S28's grandfathered *record*), but it MUST NOT ship that guard broken (fix its own bugs). "Don't compensate silently" (note 4) = surface every machinery gap and let the owner rule (Option A here), never hand-wave past a hard-block or hand-edit `gates.yml`. A precedent for any future feature that builds pipeline-governance machinery. Applied at the `003` Phase-4 pre-flight + wave 7. |
| D65 | 2026-07-12 | **Taxonomy v0 → v1 BLESSED (the three-feature dogfood review; verdicts 1–10).** After 001/002/003 categorized (the taxonomy §8 trigger), Babu blessed the taxonomy at v1 per `docs/reviews/2026-07-12-taxonomy-v1-review.md`. Ten evidence-based verdicts, applied throughout `docs/contracts/taxonomy.md`: **(1)** OQ4 test-scope boundary **upheld unchanged** (applied ×3; first *machine*-drawn boundary is M4+). **(2)** OQ6 `ai-agents`/`devtools-cli` overfit **retained, caveat carried** — dogfood can't acquit (all three features ARE SpecSeyal), untested until the first non-SpecSeyal install. **(3)** `endpoint`↔`service` body fusion **upheld** (type = deliverable; slash commands are `endpoint`, body flavor rides in tags — categorizer note). **(4)** `preserves_behavior` **retained as a marked hypothesis** — still 0 exercise at 3 features (003 is 32/32 `false`). **(5)** assembly cap ≤3 **upheld** (reached once, 003 T008; never exceeded). **(6)** §1 derivability **amended for honesty** — `type` is graph-derived *where the graph covers the file*, with a documented **path-convention fallback** otherwise (three features' actual practice); graph coverage of `.sh`/`.yml`/`.md` stays a graphify enhancement (I-13), not a taxonomy patch. **(7)** `docs × devtools-cli` empty lane → **widen the base**: `agt_devtools_cli` accepts `docs` (v1.0.0→**2.0.0** major bump, agent-library-schema.md §2; **resolves I-15**) — this D-row is the curated-static base edit's authorization (D17/Flag-3). **(8)** provisional bases **kept provisional** (M5's manager backend is their scheduled exercise). **(9)** small-feature `general`-cap → **floor adopted**: `count(general) ≤ max(1, ⌊0.2·n⌋)` (§4) — v0's candidate, adopted on evidence (001 0/23, 003 0/32; inert for n≥5, deletes the n<5 "zero-general" absurdity before M4). **(10)** prompt-asset → **modifier**: promote the `prompt` tag to the `runtime_consumed` boolean modifier (§2.4; §6's promotion rule, OQ1 precedent) — the D48 Sonnet-floor guard is re-homed on the modifier, the tag-convention check retired. **Application:** `git mv taxonomy-v0.md → taxonomy.md` (header **v1.0 — BLESSED 2026-07-12**), live references swept (contracts, workforce ext source, docs/00; frozen history — the 2026-07-09 memo, `taxonomy-v0-evidence.md`, completed-spec artifacts, prior D-rows, installed `.specify/` — preserved as written); workforce ext code + tests (runtime_consumed 6th column + guard, floor'd cap, fixtures/golden regenerated, `test/run.sh` 12/0). Verdicts 11–12 are their own rows (**D66**, **D67**). **v1→v2 trigger:** first non-SpecSeyal repo categorized, or M5 close, whichever first. | Blesses v1; unblocks M4. Mirrors D42 (the v0 blessing). The whole v0→v1 window was dogfood, so OQ6/OQ4's real tests are deferred to v2 (a non-SpecSeyal repo / M5) — honestly labeled, not papered over. The evidence dossier (`docs/reviews/taxonomy-v0-evidence.md`) is consumed by this review. |
| D66 | 2026-07-12 | **Flywheel economics — gap-batching primary + evidence-backed seeding only (taxonomy v1 review, verdict 11, owner-ruled).** How aggressively should the skill-builder flywheel fire on early features (003 flagged 14 ∅-match gaps against the thin seed library)? **Ruling:** the builder **clusters ∅-matches by tag similarity and authors one skill per cluster** (003's 14 gaps ≈ 3–4 dispatches), never one-per-gap-task. Speculative **mass-seeding rejected** (violates evidence-based curation — a skill with no real task behind it is a guess in the library); **pure 1:1 firing rejected** (a network-session fan-out on every sparse feature). **Application (this makes the batching the assembler's own deterministic output, not command prose):** `assemble.py` gains `compute_gap_clusters` — connected components over gap tasks by tag intersection, total-ordered (S01) — emitting a `GAP_CLUSTERS: [T005,T012] [T020]` stdout line and recording the clusters in the roster's `**Gap clusters …**` notes; `/speckit-agent-assign` §2a now **consumes** those clusters (one builder dispatch per bracket) instead of re-deriving connected components by hand. Tested (`test_assemble.sh` §6: 3 gap tasks, 2 sharing a tag → 2 clusters, deterministic across `PYTHONHASHSEED`). | Owner ruling at the v1 review. Turns the SC-007/FR-006 dedup (which already existed as agent-assign prose) into a code-computed, roster-recorded artifact — the two levers the evidence dossier named (seed breadth; gap-batching granularity) are now both visible and one of them is mechanical. Neither lever has a steady-state datapoint yet (003's own build was hand-rostered gap-free); revisit when the flywheel first fires for real. |
| D67 | 2026-07-12 | **Workforce auto-mode is valid standalone, WITH a grant tripwire (taxonomy v1 review, verdict 12, owner-ruled; resolves I-19).** The I-19 drift — `spec.md` FR-020 + `contracts/commands.md` gate workforce auto behind `full_auto`, but `profile-schema.md` **P4** says `gates.workforce.mode: auto` is valid **standalone** (economic guard, unlike the council's protected correctness handshake), and the shipped T017 skill followed P4 — is resolved **in P4's favour**: standalone workforce-auto is ratified (P4 wins over FR-020's coupling; D8/D9 never coupled the gates). **WITH a tripwire (A-2's display duty, D41):** **any elevated grant in the assembled roster forces that gate to a human regardless of profile** — `auto` never approves a grant-bearing roster, because approving it is approving network/tool access. **Application:** FR-020 amended (S16-class, D-row pointer); `assemble.py` stamps a `GRANT_TRIPWIRE:` stdout line + a `**Grant tripwire (D67)**` roster notice (the roster-wide elevated-grant union, total-ordered); `/speckit-agent-assign` §3 auto-writes only when the tripwire is clear (else holds for a human); `/speckit-workforce-approve` forces `human` mode when the tripwire is engaged. Tested (`test_assemble.sh` §7: grant-bearing roster → ENGAGED, grant-free → clear). | Owner ruling at the v1 review; **resolves I-19**. The distinction is D63's again — *capability authorization* vs *dispatch approval*: `auto` may sign an economic roster, but a roster that reaches the network is a correctness/authority act a human must see. FR-020's original "only within `full_auto`" clause is superseded. |

## Open questions

| # | Question | Options | Current lean |
|---|---|---|---|
| Q1 | Council provider strategy | (a) multi-provider via API keys/OpenRouter — true diversity · (b) Claude-only Opus/Sonnet/Haiku subagents — no keys, cheaper, fits sessions | ✅ **Resolved → D12** |
| Q2 | Convergence rule details | Round limit (2?); who classifies suggestions as `blocking` — chairman or human? | ✅ **Resolved → D13** |
| Q3 | Defense deck format | markdown · HTML · pptx | ✅ **Resolved → D15** |
| Q4 | Naming the overall system | "SDD Orchestrator" is a placeholder | ✅ **Resolved → D30: SpecSeyal** |
| Q5 | MCP syncing rules granularity | Sync on every artifact write? Phase completion only? Batched? | ✅ **Resolved → D19** |
| Q6 | Model assignment heuristics | Graph fan-in/out, file count, task type weights — exact policy? | ✅ **Resolved → D18** (graph-scoring deferred; revisit with observability data) |
| Q7 | Setup wizard scope | Just init + graphify/spec-kit, or also agent library bootstrap + MCP registration? | ✅ **Resolved → D23** (full scope) |
| Q8 | Central manager hosting | claude.narenwebworks.in EC2 (t4g.large) vs elsewhere? Auth model for internal use? | ✅ **Resolved → D22** |
| Q9 | Illegible notebook items | (a) the scribbled word before "Assign/creating agents" on the Ideas page; (b) first sub-bullet under Setup wizard ("init with …?") | ✅ **Closed** — (a) strike-off, nothing meaningful; (b) "if needed" / superseded by D23's explicit wizard scope |
| Q10 | Central manager auth & multi-tenancy | Single-user token · team SSO · GitHub OAuth (OSS-friendly) | ✅ **Resolved → D20** |
| Q11 | "Self-evolving setup" concrete scope | Agent library only · + extension config · + wizard regenerating itself | ✅ **Resolved → D24** (library only, year one) |
| Q12 | A severe finding reopens a council-defended plan — what review does the patched plan get? | Delta council (focused single round) · full rerun · decision record + human gate only · severity-tiered | ✅ **Resolved → D14** |

## Idea parking lot

| # | Idea | Origin | Ripeness |
|---|---|---|---|
| I-1 | **Library flywheel**: generated agents that perform well get persisted into the specialist library with usage stats | Agent creator discussion | Ready to fold into `20-AGENT-CREATOR-SPEC.md` |
| I-2 | **Graph-driven complexity scoring** for model assignment (fan-in/out, file count as proxies) | Graphify already computes the graph | Needs Q6 |
| I-3 | **Testing agent v2**: actually run tests, not just produce the doc; feed failures back as remediation tasks | Notes: "for now, creates a testing doc" | Post-v1 |
| I-4 | **Git worktrees per implementation wave** — parallel subagents each get an isolated worktree, merged per wave | Some llm-council variants use worktrees per member | **Spike scheduled** M2 (D25). **→ RESOLVED M2 — ABANDON for v1** (the wave-worktree spike ran at `002` Phase-4 wave 10, T018/FR-015, timebox ≤2h/D54): worktree-per-wave adds setup/disk/merge cost for **zero** benefit on disjoint-file waves (all `[P]` permits) and is **actively worse** on shared/mutable files — it *reintroduces* the merge conflict that the DAG-level "serialize shared writers as orchestrator glue" rule (taxonomy §2.1) prevents by construction. Correct isolation layer = the DAG, already in place; `002`'s own 9 waves (up to 4 concurrent subagents, zero collisions, no worktrees) are the existence proof. A null result is a success (D25/D54); firewall held (no v1 hook/command references `worktree`; SC-008). Finding: `specs/002-speckit-ext-git/implement.log.md`. |
| I-5 | **Council reviews implementation too** — a post-implement council round on the completion report, not just the plan | Natural symmetry | Parked (council spec non-goal v1) |
| I-6 | **Non-technical deck doubles as stakeholder artifact** — reusable in work contexts (e.g., AI-First Delivery program demos) | Deck has technical/non-technical split anyway | Free byproduct |
| I-7 | **Open-source the orchestrator** — Agent SDK-native Spec Kit orchestration appears to be an unoccupied niche | Earlier research found no existing GUI orchestration layer on Spec Kit | **Promoted by D7** — OSS confirmed, simultaneous with work adoption. Remaining: licensing & packaging (Round 5/6) |
| I-8 | **Suggestion classification taxonomy** (`blocking`/`strong`/`consider`) could generalize to analyze/remediation findings too | Council spec §5 | Cheap consistency win — D11's severity routing could reuse it |
| I-9 | **Domain critics for work contexts** — e.g., a compliance/HIPAA critic auto-added to the council v2 roster for health-insurance repos; roster composition per repo/domain | Round 3 framing | Fits v2's prompt-config-only evolution |
| I-10 | **Central skill-registry MCP** — an MCP server exposing the shared **skill** bench; repos pull/contribute *skill modules* across devices and teams | Babu, Round 4; **reframed by D40** (memo §3.3) | Named future component; D17 preps for it via stable IDs. Reframed from "agent bench" to "skill registry": under D40 the skill is the unit of storage, so it is the unit that syncs. Base specialists are thin, hand-curated, and stay per-repo. |
| I-11 | **Conformance checker for `docs/contracts/`** — a script validating any `specs/NNN-feature/` against the contract schemas (seven as of 2026-07-09), run in CI so contract drift is caught by machine, not by review | M0 council fixture, `R1-S04` (deferred) | **Earliest home: M1.** A throwaway verifier (265 checks after the taxonomy amendments) already exists in the M0 scratchpad — it should be *rewritten* by someone working from `docs/contracts/` alone, since the existing one was written against the fixture and would certify its misreadings. It has twice caught real fixture bugs (a leaked `opinions/` path; a `: ` that broke YAML frontmatter). **v0→v1 refinement (M1):** it must exempt the council's *own* feature (`001`) from rule 5's `opinions/` grep — a council-building spec necessarily *describes* the path without leaking opinion content (evidence in `specs/001-council-extension/quickstart.md` §M1 conformance). **→ Carve-out resolved D50 (M2): self-declared `spec.md` marker; `001` marked. The checker *build* (rewrite from `docs/contracts/` alone) remains open.** |
| I-12 | **Workforce-gate record format is undefined in the contracts** — the gate exists (D9; `artifact-layout.md` §2 points its record at `agents/assignment.md`) but no contract defines that section's *format*, unlike the council gate's `## Human Gate` (specified in `decision-record.md`). `001`'s bootstrap workforce gate had to be recorded as a flagged addendum in `council/decision-record.md`, exceeding that contract's section set. | Bootstrap workforce gate, `001` | **A gate with no record format is a contracts bug.** Define the `## Workforce Gate` schema (reviewer, decision, roster, **elevated-grants approved** — D41) and its home (`assignment.md`, artifact-layout §2) when M3 ships the assigner, or at the v0→v1 contracts review. **→ Resolved D49 (M2): format = `artifact-layout.md` §8, homed in `agents/assignment.md`; brought forward from M3 so M2's own workforce gate can use it.** |
| I-13 | **graphify coverage for non-code artifacts (`.sh` / `.yml`).** `graphify explain`/`path` return "No node matching" for shell scripts and YAML, so a plan that turns on installer/hook wiring can't ground its blast-radius claims in the graph — they are engineer assertion, not graph fact. | `002`-round-1 **R1-S22**; the first live council found the graph low-signal for exactly the `.sh`/`.yml` files this feature is about. | Evidence booked: extend graphify's extractor to emit nodes/edges for shell + YAML (hook registrations, install copies) so pipeline-plumbing features get real receipts. Until then, such deck claims are **labeled assertion**, not graph-grounded. **→ v1 review (D65): reviewed and carried OUT of taxonomy scope — the taxonomy.md §1 derivability amendment (verdict 6) names the path-convention fallback as the standing basis until this lands; extending the extractor to `.sh`/`.yml`/`.md` stays on the graphify backlog, not a taxonomy patch.** |
| I-14 | **Cross-extension seam convention — hook points over source edits.** Cross-extension coupling should attach at a **hook point** (reinstall-surviving), never a source edit to another extension's installer-overwritten skill. `002`'s `after_council_approve` hook is the model; its `implement-parallel` per-wave commit is the exception (no per-wave hook vocabulary) and survives *only* because the edit was relocated into graphify's **source** — the installed-copy edit was silently wiped by a graphify reinstall until then. | `002` M2 close, **Finding-1**; the reinstall-survival regression (R1-S17) caught it live at implement wave 12. | Establish the convention **before M3 wires in** categorize/agent-assign coupling: prefer a hook; if a source edit is unavoidable, it lives in the owning extension's **source** and is reinstall-survival-tested. Standing consequence: `graphify` now ships the `002`-edited `speckit-implement-parallel` (per-wave commit + gate re-verify). |
| I-15 | **`docs × devtools-cli` lane demand — the seed library has no `docs`-accepting `devtools-cli` base.** `003`'s README task (**T031**, `docs × devtools-cli`) matched no lane and fell to `agt_generic` + a reported empty lane (FR-016); only `agt_ai_agents` accepts `docs` in the seed set. | `003` workforce gate (approved-with-notes), **note 2**. | Book for the taxonomy §8 **v0→v1** review — candidate resolutions: **seed the lane** (a `docs`-capable `devtools-cli` base) **or widen `agt_devtools_cli`'s accepted types to include `docs`**. The `agt_generic` fallback + empty-lane *reporting* behaved exactly as designed — a **demand signal, not a bug**. One task's evidence so far; the taxonomy §8 review weighs it against real v0→v1 usage. **→ Resolved D65 (v1 review, verdict 7): widen the base — `agt_devtools_cli` accepts `docs` (v1.0.0→2.0.0, major bump). The `agt_generic` fallback + empty-lane reporting behaved exactly as designed (a demand signal, not a bug); the lane now has a real owner.** |
| I-16 | **No bootstrap/grandfather workforce-binding path — `verify-gate workforce` fail-closes on the meta-feature that builds the binding hook.** `003`'s workforce gate is validly hand-signed but **unbound** (its binding hook `on-gate-approve.sh workforce` is exactly what `003` builds, T018–T020); the live `before_implement` `verify-gate workforce` hard-blocks on the missing binding (R1-S10). A circularity: the feature that builds the workforce-binding machinery can't bind its own workforce gate. | `003` Phase-4 pre-flight; owner chose **grandfather + proceed** (Option A). | **git-ext needs a documented bootstrap/grandfather escape** (a marker, or a `verify-gate` mode that treats a hand-signed-but-unbound gate on the binding-owning feature as N/A). For `003`, grandfathered-N/A by owner ruling; `003` implement proceeds without the workforce freshness check governing it (the hand-signed gate is the authority, as `002`'s own gate was unbound, S28). Book for a git-ext fix. **→ v1 review (D65): carried — a git-ext workforce-freshness follow-up, not a taxonomy concern; out of the v1 review's scope (memo §6).** |
| I-17 | **`[X]`-marking during implement stales the workforce binding — a GENERAL defect (every feature).** `implement-parallel` marks tasks `[X]` in `tasks.md` per wave; the workforce binding covers `tasks.md`; `verify-gate` is SHA + working-tree-aware, so the first `[X]` mark makes `tasks.md` stale → the per-wave re-verify hard-blocks wave 2+. `002`'s own implement predated `verify-gate`'s existence, so **`003` is the first implement run under the live workforce machinery** — this is where it surfaces (note-4 "first unassisted outing"). | `003` Phase-4 pre-flight; verified against `speckit-implement-parallel` (marks `[X]` per wave) + `verify-gate.sh` (working-tree-aware). | **git-ext must tolerate `[X]`-marking:** re-bind per wave after the commit, OR bind content excluding the checkbox column, OR exclude `tasks.md`'s check-state from the working-tree staleness check. Not 003-specific — fix in git-ext source, reinstall-survival-tested. For `003`, mooted by the I-16 grandfather (the check is N/A this run). **→ v1 review (D65): carried — a git-ext workforce-freshness follow-up (general defect, every feature), not a taxonomy concern; out of the v1 review's scope (memo §6).** |
| I-18 | **git-ext's workforce gate-write pointed at the wrong path — flat `specs/<id>/assignment.md`, not `specs/<id>/agents/assignment.md`** (**FIXED**). `gates.sh cmd_write` defaulted the workforce artifact list to a flat `tasks.md assignment.md` (resolved `specs/<id>/assignment.md`), but the roster+gate live at `specs/<id>/agents/assignment.md` (artifact-layout §8). Since `on-gate-approve.sh workforce` calls `gates.sh write workforce` with no override, the **first real `/speckit-workforce-approve` would `die`** ("artifact does not exist") — a latent `002` defect that would have broken the very workforce gate-write S02 built. | `003` T020 — the reinstall-survival subagent surfaced it (and left it visible rather than masking with the real path). T018 had *assumed* "gates.sh already knows the workforce artifact set — no change there"; the set was right, the path wasn't. | **FIXED in the S02 cluster (git own source, D57 S2):** `gates.sh` workforce default → `tasks.md agents/assignment.md` (+ header-doc shape); the T020 survival fixture now uses `agents/assignment.md`. Council path (`plan.md`) untouched. **Unlike I-16/I-17** (grandfathered-N/A for `003`), this one is *corrected* not deferred — `003` **builds** the workforce gate-write, and shipping it broken defeats the council's blocking S02 finding. |
| I-19 | **Contract inconsistency: workforce auto-mode scope.** `spec.md` FR-020 + `contracts/commands.md` gate the workforce auto-write behind **`full_auto`** ("only within `full_auto`"); `profile-schema.md` **P4** says `gates.workforce.mode: auto` is valid **standalone** (the workforce gate is an *economic* guard, unlike the council gate's protected correctness-guard handshake). The shipped `/speckit-workforce-approve` skill (T017) followed **P4** (more specific / more recently amended). | `003` T017 (subagent-flagged during the command build). | Reconcile at the v0→v1 contracts review — a D-row either **ratifies P4** (standalone-economic-guard, the more considered reading) or restores the `full_auto` restriction to P4. **Moot for `003`** (`gates.workforce: human`). Recorded so the spec↔profile-schema drift isn't silently embedded in the skill. **→ Resolved D67 (v1 review, verdict 12): ratifies P4 (standalone workforce-auto is valid), WITH a grant tripwire (any elevated grant forces the gate to human regardless of profile). FR-020 amended; the shipped skill's P4 reading stands.** |

## Session log

| Date | What happened |
|---|---|
| 2026-07-08 | Notebook pages transcribed & reconstructed into workflow. D1–D5 recorded. Doc set created (00, 10, 90). Council spec drafted to 0.1. |
| 2026-07-08 | **Brainstorm Round 1 (ideology):** D6–D8 recorded. Principles 6–9 added to doc 00 (resumability, observability, non-technical UX, self-evolving setup). Audience = personal → work + OSS simultaneously. Autonomy profiles introduced. Q10–Q11 opened. |
| 2026-07-08 | **Brainstorm Round 2 (workflow):** D9–D11 recorded. Workforce gate added to pipeline. Council gains graphify query tooling. Severity-based remediation routing. Q12 opened. |
| 2026-07-08 | **Brainstorm Round 3 (council):** D12–D15 recorded. Q1/Q2/Q3/Q12 all resolved. **Council spec promoted to 0.2 — buildable.** |
| 2026-07-08 | **Brainstorm Round 4 (tasks & agents):** D16–D18 recorded. Q6 resolved. Two-plane model policy (Sonnet workers / Opus-xhigh spine); role map pending confirmation. I-10 central-library MCP named. |
| 2026-07-08 | **Brainstorm Round 5 (platform):** D19–D22 recorded. Q5/Q8/Q10 resolved. Sync = phase events with artifacts + heartbeats. Token auth v1 → OAuth. GUI MVP = observe + approve. EC2 hosting. |
| 2026-07-08 | **Brainstorm Round 6 (edges):** D23–D25 recorded. Q7/Q11 resolved; Q4 assigned to Babu. D18 confirmed by default. **Brainstorm complete: 25 decisions, 10 of 12 questions closed.** Implementation plan created as doc 05. |
| 2026-07-08 | **Post-brainstorm:** D26 (new repo, monorepo default with graphify migration), D27 (MIT confirmed), D28 (subscription-only billing end to end — Agent SDK monthly credit covers M6+ programmatic sessions, no API keys). Plan doc updated. |
| 2026-07-08 | **Handoff:** CLAUDE.md seed + 95-M0-KICKOFF.md created (pre-flight checklist + paste-ready M0 prompt). Awaiting: working name, migration/public-repo default confirmations, Q9 disposition. |
| 2026-07-08 | **Final inputs closed:** Q9 closed (strike-off / superseded). D26 migration confirmed. D29 private-until-α. **D30: named SpecSeyal** (namespace verified clean). All 12 questions resolved, 30 decisions. Docs stamped. **Handoff complete — pre-flight is the only step left.** |
| 2026-07-08 → 07-09 | **Milestone 0 — Contracts & scaffolding.** First Claude Code session. Billing hygiene verified (`ANTHROPIC_API_KEY` unset, D28). Founding-docs seed commit made (pre-flight step 3 had been skipped — only `LICENSE` was committed). **Graphify migrated** into `extensions/graphify/` via `git subtree` with history preserved (D31); install → idempotent re-install → uninstall all verified against a synthetic spec-kit target. **Six contracts authored** under `docs/contracts/`. **`specs/000-sample/` committed** — every artifact, empty-but-valid, validated by a throwaway 240-check script (I-11). D31–D39 recorded; I-11 filed. **Taxonomy v0 is a DRAFT and blocks M3** — 6 open questions in `docs/contracts/taxonomy-v0.md` §7 await Babu. M1 not started. **M0 exit criteria met except "taxonomy blessed".** |
| 2026-07-09 | **Taxonomy v0 review applied.** Memo `docs/reviews/2026-07-09-taxonomy-v0-review.md` (Babu, blessed with amendments) applied mechanically per its §5. **D40** (composability — skill is the unit of storage; agent is an assembly), **D41** (per-skill tool grants; roster displays them), **D42** (six OQ verdicts; 8 types × 11 specializations; `general` capped at 20%) recorded. `refactor` deleted as a type → `preserves_behavior` modifier auto-injecting `refactor-discipline`. New contract `docs/contracts/skill-module.md`; `agent-library-schema.md` → 2.0; docs/00 §4.3 rewritten; I-10 reframed as a skill registry. `specs/000-sample/` revalidated against the amended contracts. **Taxonomy status → v0 BLESSED (normative); the M0 exit gate closes.** Two items flagged, not improvised: (a) `trace-schema.md` has no `skills[]` field, so skill-level stats attribution — and therefore the flywheel — is **BLOCKED** pending a D-row; (b) the `general` cap admits zero `general` tasks when a feature has fewer than 5. M1 not started. |
| 2026-07-09 | **Flag adjudications applied → M0 CLOSED.** Babu adjudicated all four flags from the taxonomy amendment. **D43** (authorized): `trace-schema.md` → 1.1 — records now carry `skills: [{id, version}]` + `elevated_grants: [...]`; `agent-library-schema.md` §5 stats rekeyed on skill `id@version`; **the flywheel BLOCK is lifted.** **D44** (adjudications): literal 20% `general` cap stands for v0 (small-`n` edge → §8 checklist, `max(1,⌊0.2n⌋)` unadopted); two-kind storage confirmed (bases curated-static, skills evolve); core-toolset vs elevated-grant codified as defined terms, A-2 clarified to elevated access only. Fixture verifier re-run green (**385 checks**) across all seven contracts. Committed as a single change (D40–D44). **All six M0 exit criteria now met; M0 is closed.** M1 not started. |
| 2026-07-09 | **Milestone 1 — `speckit-ext-council`, started.** Tooling bootstrap (D45): real spec-kit (`specify init`, v0.12.5) + the graphify extension installed into specseyal for dogfooding — `/speckit-*` and graphify skills and their hooks are now live in-repo. Feature branch `001-council-extension` created by hand (D25 emulated; the git extension is M2). `.specify/feature.json` gitignored as transient state. Next: `/speckit-specify` + `/speckit-clarify` for the council extension (source material: docs/10), then STOP for human review. |
| 2026-07-09 | **M1 · Phase 2 — spec + clarify (`001-council-extension`).** `/speckit-specify` authored `spec.md` for the council extension from docs/10 + the M0 contracts (5 prioritized user stories, FR-001–FR-018, 7 success criteria incl. the M1 exit metric SC-002 "council token spend is measured"); spec-quality checklist all-pass. `/speckit-clarify` ratified two open parameters into `## Clarifications`: **5 council members** + chairman (diverging from the fixture's illustrative 3), and **graceful degradation** when no graphify graph exists (council runs deck-only + flags reduced grounding). Held as spec-scoped clarifications, not system D-rows (durably captured in `spec.md`). STOP for human review before `/speckit-plan`. |
| 2026-07-09 | **M1 · Phase 2 review — spec APPROVED with adjudications (D46).** Babu approved the `001-council-extension` spec with four adjudications: FR-017 **split at the trigger** (v1 = manually-invoked reopen interface; automated `/speckit-analyze` routing deferred to analyze integration); build-phase tracing **option (a)** (no backfill; first real traces = the council's M2 live run); spec-template framing approved + a **standing rule** (every Constraints & Assumptions entry cites a D-row); and a **reduced-grounding flag** that must be visible at the gate (new FR-019 / SC-008). Spec amended, phase committed. Next: `/speckit-plan` — STOP at plan review; **the human is the council for this plan** (bootstrap, D9). |
| 2026-07-09 | **M1 · Phase 3 — plan (`001-council-extension`).** Built the graphify graph (`graphify update`, 638 nodes / 574 edges; deterministic, no LLM; gitignored) and ran the `before_plan` hook → `graphify-context.md` (honest: markdown-dominant repo, thin edges; grounding leans on the `extensions/graphify/` exemplar). Seeded `.specify/memory/constitution.md` v1.0.0 from docs/00 §2 — D45's flagged contingency, since the plan's Constitution Check needs real principles. Authored `plan.md` + Phase-0/1 companions (`research.md`, `data-model.md`, `contracts/commands.md`, `quickstart.md`): sibling-of-graphify packaging; three command skills; main-thread orchestrator dispatching deck-prep + 5 members ×2 stages + an Opus chairman (context hygiene = only `suggestions.md` returns as content); member differentiation resolved to **5 general-purpose reviewers + soft lens nudges** (the spec's deferred decision); the installed `graphify` CLI as the member receipts tool; orchestrator-serialized trace assembly. **Constitution Check PASSES** (no violations). **Open risk R1 flagged for the council:** per-session token capture in interactive CLI touches SC-002 / the M1 exit. STOP at plan review — **the human is the council for this plan** (bootstrap, D9). |
| 2026-07-09 | **M1 · Phase 3 gate — council APPROVED (bootstrap, no blocking); amendments applied.** The bench-of-one human council raised four suggestions, all accepted: **S1** (→ **D47**) trace `tokens` = exact-or-null + `capture_method ∈ {sdk,transcript,unavailable}` (`trace-schema.md` → 1.2; fixture `000-sample/traces.jsonl` updated — 20 records revalidated; `spec.md` SC-002 amended; a Phase-4 transcript-capture spike booked); **S2** explicit **status-only-returns** invariant in the plan + SC-005 extended to the orchestrator transcript; **S3** bench fixed at 5 + two-stage peer review for the first run (tune only after SC-002); **S4** each member's **lens** recorded in opinion metadata. Wrote `001`'s `profile.yaml` (both gates human) and — by hand — `council/decision-record.md`: the decision-record contract's **first real instance**, produced by its own bootstrap gate (no deck/opinions, since the human was the council). Next: **Phase 4** — `/speckit-tasks` (graph) → `/speckit-analyze` → categorize (taxonomy v0, manual). STOP at tasks + waves review. |
| 2026-07-09 | **M1 · Phase 4 — tasks + analyze + categorize (`001-council-extension`).** `/speckit-tasks-graph` → `tasks.md`: **20 tasks, 8 execution waves** (Setup → Foundational barrier → the 3 command skills cross-story parallel → provenance stubs → install-verify → validation); `[P]` verified by disjoint new-file sets (the graph has no council edges — all new code); the S1 token-capture spike + carried constraints (S2 status-only, S3 config-lever, S4 lens) booked in. `/speckit-analyze`: full FR/SC/US coverage, **no CRITICAL/HIGH, no constitution violations**; two **routine** findings remediated in place (D11) — the plan tree now shows the `extensions/council/skills/` **source** → `.claude/skills/` **destination**, and the spike is timeboxed. **Categorization** (manual; the categorizer is M3): 20 tasks tagged per taxonomy v0 — **0 `general` (cap 4) → PASS**; `ai-agents` ×10 + `devtools-cli` ×9 dominate. First **non-graphify** feature categorized → real v0→v1 evidence booked into `categorization.md` (OQ6 both lanes heavily exercised; prompt artifacts absorbed as `docs × ai-agents + tags`; `endpoint` ⊃ CLI-command with a service fusion; `preserves_behavior: true` still unexercised). STOP at tasks + waves review. |
| 2026-07-09 | **M1 · Phase 4 gate — workforce gate APPROVED (bootstrap, human).** Roster: Sonnet implementation agents (D18), **max 3 parallel, zero elevated grants**. Directives applied: **Call 1** upheld (`endpoint × ai-agents` for the 3 command skills; `orchestration` tag added to all three; endpoint/service body boundary booked as v0→v1 evidence). **Call 2** accepted for v0 (`docs × ai-agents + tags` for prompt/deck templates; two candidate resolutions booked for the §8 review; **D48** guard — `prompt`-tagged tasks keep the D18 Sonnet floor in M3). Gate recorded as a flagged `## Workforce Gate` addendum in `001/council/decision-record.md` — no contract defines a workforce-gate record format, filed as **I-12** (a gate without a record format is a contracts bug). Phase-5 discipline set: shared-file registration = orchestrator glue (never parallel subagents); commit per wave; wave boundaries = cold-resumable checkpoints. Next: `/speckit-implement-parallel` through all 8 waves; **STOP at completion, before the completion report.** |
| 2026-07-09 | **M1 · Phase 5 — implement (`001-council-extension`): 8 waves complete.** Built `extensions/council/` (19 files) via `/speckit-implement-parallel`: installer + uninstaller (mirror graphify, no hook-merge); manifest + `council-config.yml` (member_count 5, lenses, D18 model map) + 2 READMEs + 3 provenance stubs; 6 templates (trace-fragment, deck-technical, deck-overview, member-prompt `{{lens}}`, chairman-prompt synthesis+delta-check, suggestions); the 3 command skills (`/speckit-council` orchestrator — deck→5 opinions→5 peer→chairman, status-only/SC-005, serial trace assembly, `--reopen`; `-triage` — dispositions/D13.5, decision-record R1–R7, one-revision+delta-check, `## Reopen`, auto-mode gate write; `-approve` — `## Human Gate`, writes no trace, `disable-model-invocation: true`). **17 tasks via 13 Sonnet agents (max 3 parallel, zero elevated grants)** + 3 orchestrator-glue tasks; committed per wave (`c990682` … `6c65ba0`, + this). **Spike (R1/S1) SUCCEEDED:** interactive transcripts expose per-subagent usage (4 token fields + `isSidechain`) → `capture_method: transcript` is feasible, so SC-002 can be an **exact** interactive measurement. Installer verified (idempotent install / clean uninstall vs a synthetic target). Static conformance passes; **dynamic SCs (SC-001/002/004, live traces/reduced-grounding) are M2's exit test.** SC-005's rule-5 grep flags 001's own design docs (it *describes* `opinions/`) — a meta-feature false positive; the runtime invariant is skill-enforced; noted for I-11. **STOP before the completion report** for exit-posture review. |
| 2026-07-09 | **M1 CLOSED — `speckit-ext-council` built.** Exit posture approved; `completion-report.md` written (the `complete` phase). The council extension is built and verified-installable (19 files; 8 waves; 20/20 tasks; 13 Sonnet agents, max 3 parallel, zero elevated grants). **M1's exit *test* is M2**: the council's first *live* review is M2's plan (the dogfooding rule — the human was the council for 001's own bootstrap plan), producing the first measured `council_spend` (SC-001/002/004, live SC-006/008). One M2-setup step: `bash extensions/council/install.sh .`. Carried forward: **I-11** (rule-5 meta-feature carve-out), **I-12** (workforce-gate record format), **D48** (M3 `prompt`-tag Sonnet floor); taxonomy §8 gains its first non-graphify feature. **Next milestone: M2 `speckit-ext-git`**, whose plan is the council's first live run. |
| 2026-07-09 | **M2 — `speckit-ext-git` started; M1 landed on `main`.** Fast-forwarded `main` → `27f8b98` (all 17 of `001`'s phase-tagged commits + the D45 tooling bootstrap): M1's completion merge, done by hand since branch-lifecycle is the step M2 retires. **Phase 0(a):** installed the council extension (`extensions/council/install.sh .`) — `/speckit-council`, `-triage`, `-approve` resolve; extension → `.specify/extensions/council/`, skills → `.claude/skills/`, committed per the graphify precedent. **Phase 0(b):** two carried M1 contract fixes on `main` — **D49** (I-12): `## Workforce Gate` record format = new `artifact-layout.md` §8, homed in `agents/assignment.md`; **D50** (I-11): rule-5 meta-feature carve-out via a self-declared `spec.md` marker, `001` marked inaugural-exempt. `artifact-layout.md` → 1.1. Next: cut `002-speckit-ext-git` by hand (last time), `/speckit-specify` (scope from D25 + I-4) + `/speckit-clarify`, then STOP for spec review. |
| 2026-07-09 | **M2 · Phase 1 — spec + clarify (`002-speckit-ext-git`).** Branch cut by hand from `main` — the **last** hand-made feature branch (the step this feature retires). `/speckit-specify` authored `spec.md` (5 user stories, 15 FRs, 8 SCs) taking explicit **positions** on all four contested points: branch birth **at `specify`** (FR-001, refines `artifact-layout.md` §2 ordering), commit granularity **phase + per-wave** (FR-005/006), gate↔SHA **binding + stale-approval detection** (FR-008/009), cleanup **`--no-ff` trail-preserving merge** (FR-011); the I-4 worktrees experiment scoped as a firewalled **P3 spike** (FR-015, not committed scope — D25). Every Constraints entry cites a D-row (D46 standing rule). `/speckit-clarify` ratified two **spec-scoped** answers (not D-rows, per the `001` precedent): stale approval **hard-blocks** until re-approval; cleanup is an **explicit engineer-invoked** step. Two positions flagged for gate ratification (FR-001 §2-refinement; FR-011 merge policy). **STOP for spec review** before `/speckit-plan` — Phase 2's **first live council** run. |
| 2026-07-09 | **M2 · Phase 1 review — spec APPROVED with adjudications (D51).** Babu ratified the `002` spec with four rulings (→ **D51**): FR-001 branch-at-`specify` **ratified** + `artifact-layout.md` §2/§3 amended (branch co-incident with specify; contract → 1.2); FR-011 **strengthened** to unconditional `--no-ff` (the merge commit is the D19/M5 completion anchor; M1's ff grandfathered); FR-008/009 amended (stale-approval re-approval routes by gate type — council → D14 reopen tiers, workforce → re-run); spike firewalling + stale hard-block + explicit cleanup ratified as scoped. Spec + contract amended, phase committed. Next: `/speckit-plan` → the **first live `/speckit-council`** run; **STOP after `suggestions.md`, before triage** — present it raw + report `council_spend` with `capture_method` (SC-001/002/004 first measurements). |
| 2026-07-09 | **M2 · Phase 2 — plan (`002-speckit-ext-git`).** Rebuilt the graphify graph (1013 nodes / 917 edges, deterministic no-LLM) → `before_plan` hook wrote `graphify-context.md` (git-ext is new code; grounds on the graphify/council install exemplars; sole shared/mutable file = `.specify/extensions.yml`). Authored `plan.md` + Phase 0/1 companions: **hook-based design** — `after_*` commit hooks + a shared `commit`/`sha`/`verify-gate` primitive; **branch birth folded into the `after_specify` commit** (reads the spec ID from `feature.json`, no slug duplication); **gate↔SHA** = git ext *supplies* the SHA + guards freshness while the gate command *records* it (keeps principle I clean, no second writer); **`--no-ff` completion-anchor** cleanup command. **Constitution Check PASSES** (no violations). Flagged for the council: **R1** (no stock hook point for council/gate + per-wave commits → a command→primitive seam), **R2** (gate-verify only at gated-phase entry). Next: the **first live `/speckit-council`** on this plan; **STOP after `suggestions.md`, before triage**. |
| 2026-07-09 | **M2 · Phase 2 — FIRST LIVE COUNCIL (`002-speckit-ext-git`, round 1) — M1's exit test.** `/speckit-council` ran the real bench: 1 deck-prep + 5 members ×2 stages + 1 Opus chairman = **12 sessions**, full grounding (graph present), **status-only returns throughout (SC-005 held** — no opinion content entered the orchestrator). **Verdict: 6 blocking · 12 strong · 11 consider** (29 consolidated). It converged hard on a real mechanism defect the human backstop exists for: the git-ext hooks are specced `optional: true` (copied from graphify) which the stock dispatch layer only *announces*, so FR-009's hard-blocks never auto-fire (warn-and-override — the exact thing the spec rejected 2026-07-09); plus a **false blast-radius claim** (the R1 seam actually needs editing two *other* extensions' skills that their installers `rm -rf`-overwrite on reinstall). **SC-001 met** (deck→council→suggestions end-to-end, artifacts on-branch). **SC-002 measured: `council_spend` = 5,010,615 billable tokens** (`tokens_billable` = input+output+cache_creation, trace-schema §5; cache_read 29.4M excluded), **`capture_method: transcript`, 0 `unavailable`** — first exact interactive measurement, the T005 spike verified live. **Finding:** the §5 transcript sum (5.01M) is ~3.4× the Agent-return `subagent_tokens` aggregate (1.47M) — each member's 25–38 tool-call (graphify) turns churn cache, so `cache_creation` dominates; weigh before the trim-member-count lever (R2). Chairman hit one mid-write API error, resumed cleanly (analysis intact). Council-extension nit surfaced: its SKILL.md and trace-fragment.md disagree on `parent_trace_id` (used `null`, per the operative SKILL reasoning). **STOP before triage** — presenting `suggestions.md` raw for dispositions. |
| 2026-07-09 | **M2 · Phase 3 — triage + delta-check (`002-speckit-ext-git`, round 1).** `/speckit-council-triage` disposed all 29 suggestions (D13.5): **28 accepted, 1 rejected (S21** — descope the FR-009 hard-block; reproduces the warn-and-override the spec rejected *by name*, and no owner authorization exists, unlike the FR-011 delta). Owner preamble applied: (i) gate↔SHA binding home = git-ext-owned **`gates.yml`**; (ii) **D52** — completion anchor becomes a mandatory **`complete/<spec-id>` tag**, `ff`-permitted (supersedes D51's `--no-ff` clause only; branch-at-`specify` + FR-008/009 reopen routing stand; M1 ff-grandfather now moot). Applied the **FR-011 spec delta first** (`spec.md` @ `0dbb715`), then the FR-010 revision to `plan.md` + companions (@ `bec819e`) folding all 6 blocking (S01–S06) + the accepted strong/consider. **Chairman delta-check: 0 of 6 still blocking — all clear** (SC-004 measured: exactly one revision + one delta-check, no second round). D-rows: **D52** (D51 partial supersession), **D53** (S08 enforcement→M6), **I-13** (S22 graphify `.sh`/`.yml` coverage). Decision record written (R1–R7 conformant); triage + delta-check traces appended. **Round-1 `council_spend` = 5,249,858 billable tok** (the 5.01M synthesis round + the 239k delta-check chairman; `transcript`, 0 unavailable *in council scope*; the triage session is honestly `unavailable`/`tokens:null` — main-thread tokens aren't cleanly isolable, D47). **Gate PENDING (human)** — HOLD at `/speckit-council-approve`. |
| 2026-07-09 | **M2 · Phase 2 gate — council APPROVED; M1's exit test CLOSED (`002-speckit-ext-git`, first live run).** Human gate ran (no session): **`approved`** (reviewer Naren Karthik B M), recorded as `## Human Gate` in `002/council/decision-record.md` — **no `traces.jsonl` record** (the gate runs no session, artifact-layout §2 / trace-schema R9). `002`'s own gate is **unbound** — the FR-008 SHA-binding it designs doesn't apply retroactively (R1-S28 grandfather, like M1's ff). **This closes M1's exit test end-to-end:** the council's first *live* review ran deck → 5 members ×2 stages → chairman → triage → one-revision → delta-check → human gate. Datapoints booked: **SC-001** met (full loop, artifacts on-branch); **SC-002** = `council_spend` 5,249,858 tok, `capture_method: transcript`, 0 unavailable in scope; **SC-004** = one revision + one delta-check, no second round; **SC-005** held (status-only returns, no opinion leak); **SC-008** (reduced-grounding) N/A — full grounding. `/speckit-tasks` unlocked. **Next (original M2 Phase 3):** `/speckit-tasks` → analyze → categorize → **workforce gate (new I-12 `## Workforce Gate` format, D49)** → STOP; then Phase 4 implement (incl. the wave-worktree spike). |
| 2026-07-09 | **M2 · Phase 3 — tasks/analyze/categorize + workforce gate APPROVED-WITH-NOTES (`002-speckit-ext-git`).** `/speckit-tasks` (graph-aware) → **23 tasks** + a `## Execution Waves` DAG; the first live council's flagged omission is **verified absent** — the five R1-S03 existing-file edits are each their own task (**T006** `extensions.yml` merge, **T014** `after_council_approve`, **T010** per-wave commit, **T015** stop-on-nonzero, **T019** graphify-context regen) plus the R1-S15 **budgeted installer merge** (T006). `[P]` on the new `.sh`/`.yml` falls back to file-disjointness (no graph nodes — I-13). `/speckit-analyze`: **0 CRITICAL · 1 HIGH · 4 MEDIUM · 2 LOW**, constitution clean; routine findings remediated (H1 **FR-003 phase-commit coverage map** — council/triage/complete = orchestrator prose-level per D53, categorize/agents deferred M3/M4; M1 FR-010 auto-mode note; L1 spike re-sequenced `deps=T010`; L2 FR-002/013 citations). **Categorize** (manual taxonomy-v0): `general` **0/23** (≤20% cap PASS); `devtools-cli`×15, `security`×3 (gate-integrity trio), `qa-automation`×3, `ai-agents`×2; **D48 prompt-tag guard checked and inapplicable** (zero-AI ext, no LLM templates — contrast `001`). **Workforce gate** (I-12 `## Workforce Gate`, D49; homed in `agents/assignment.md`): **`approved-with-notes`** (Babu) — 10 Sonnet base specialists, **zero elevated grants**, **width-4** (no cap-down). Four binding notes: (1) spike ≤2h, null=success (**D54**); (2) width-4 rationale (I-13 disjoint-new-file fallback = the strong case); (3) bootstrap discipline — orchestrator does commit-before-`[X]` + wave-boundary gate-freshness **by hand** until T010/S23 land them; (4) gate-close hygiene (this row + D54) in the `gate(002)` commit. Phases committed `tasks/analyze/categorize/agents`; gate committed `gate(002)`. **Next: Phase 4 `/speckit-implement-parallel`** through the 14-wave DAG, width-4; **STOP after the final wave, before the completion report.** |
| 2026-07-10 | **M2 · Phase 4 — implement (`002-speckit-ext-git`): 14 waves, 23/23 tasks.** Built `extensions/git/` via `/speckit-implement-parallel` (width-4; Sonnet subagents for new-file/skill tasks + orchestrator glue for the shared registry, spike, doc reconciliation, tests; **zero elevated grants**). Bootstrap discipline **by hand** (note 3): commit-before-`[X]` (atomic) + per-wave gate-freshness — `plan.md@bec819e` / `assignment.md@1ffaf6c` never drifted across the phase. Delivered the 6 primitives (`branch/commit/sha/gates/verify-gate/cleanup.sh`) + `on-council-approve.sh`, `extension.yml` (9 hooks `optional:false`), the **S15 `install.sh`** hook-merge (verify-gate ahead of graphify, S07), `uninstall.sh` (deregister-first, **byte-identical** round-trip, FR-014), the cleanup skill, and `test/run.sh` (**15/15**). **US3 integration + quickstart SC-001–008 = 15/15 existence proofs.** Findings adjudicated at close: reinstall-survival test caught T010's per-wave edit was wiped by a graphify reinstall → relocated into graphify **source** (**Finding-1 → I-14**); `on-council-approve.sh` writes only `gates.yml`, never `decision-record.md` (**Finding-2 → D55**); wave-worktree spike → **ABANDON** (**D54**, resolves **I-4**); T005 subagent stalled → hand-authored (**Finding-5**, no phantom trace; runtime still zero-AI). Committed per wave (`e2533a1`…`b7bd56f`) + `complete(002)` report (`3e805f8`). |
| 2026-07-10 | **M2 CLOSED — `speckit-ext-git` built, merged, live.** `completion-report.md` written (the `complete` phase). **Merged `002` → `main` by hand (ff, D52)** — the final manual performance of the branch ritual `002` retires. **First live install** on `main` (`bash extensions/git/install.sh .`; committed as infra `ec3800f`, M1 phase-0 precedent): **9 hooks** registered, `verify-gate` ahead of graphify (S07), `installed:[graphify,git]`. **The extension's first live act:** `/speckit-git-cleanup` for `002` cut **`complete/002-speckit-ext-git`** — the **first annotated completion anchor** (D52), enumerable via `git for-each-ref refs/tags/complete/*` — and retired the `002` branch; all 28 M2 commits remain reachable from `main` (trail preserved, no squash — **SC-005 held live**). docs/05 M2 marked done. Recorded: **D55** (Finding-2), **I-14** (Finding-1); **D54** (spike). Carried to M3: FR-010 auto-trigger, D50 checker (I-11), `HookExecutor`→M6 (D53), M3/M4 commit paths. **M2 exit criterion met** (auto-created branch + phase-tagged commits). **Next: M3 (`speckit-ext-categorize` + agent creator) in a FRESH session.** |
| 2026-07-10 | **M3 started — Phase 0 (main, infrastructure) before `/speckit-specify`.** Three infra changes on `main` before the machine cuts the M3 feature branch. **(a) D56** — council ceremony tiers + lazy context, landing the M2 5.25M finding (never a D-row until now): `profile-schema.md` → 1.1 (`council_tier` {full\|standard}, per-feature, default `full` per P1/T1); `council-config.yml` `tiers` block; the `/speckit-council` orchestrator gains a `standard` branch — 1 consolidated peer session + lazy Stage-1 context + `member_output_cap` 6 → **8 sessions** vs `full`'s **12** (byte-for-byte unchanged); `chairman-prompt`/`member-prompt` generalized for the consolidated peer; per-stage + per-member `plan.md` read-rate reporting added. `standard` is **opt-in and measured** — M3's Phase-2 council runs it and reports `council_spend` per-stage (deck/stage-1/consolidated-peer/chairman) vs the 5.25M `full` baseline; a later D-row may flip the default. **(b) D57** — I-14 promoted to a normative convention: `artifact-layout.md` → 1.3 §9 (hook points over source edits; unavoidable edits live in the owning extension's source + reinstall-survival tested), §6's ownership ethos carried to the code layer, established before M3 wires categorize→assign coupling. **(c) D55 applied** (its scheduled "next touch"): the FR-008 `gates.yml` pointer added to `## Human Gate` — `decision-record.md` → 1.1 (§2 example + §3 `Binding` field, present-iff-git-ext), and the section's **two** writers — `/speckit-council-approve` (human) and `/speckit-council-triage` (auto) — both emit the identical well-known-path pointer (R6 mutual consistency), the git ext still writing only `gates.yml` (D55 ownership inversion, `on-council-approve.sh` unchanged). Council extension reinstalled; **reinstall-survival regression 15/15** (git-ext↔council seam + council-owned source edits both survive — D57/S3 in action). Commits: `d4345fb` (a+b), this commit (c). **Next: Phase 1 — `/speckit-specify` (first machine-born branch, confirm `after_specify` fired) + `/speckit-clarify`, then STOP for spec review.** |
| 2026-07-10 | **M3 · Phase 1 — spec + clarify (`003-workforce`), on the first machine-born branch.** `/speckit-specify` authored `spec.md` for the workforce pair — **`speckit-ext-categorize`** (categorizer) + **`speckit-ext-agents`** (skill builder + assembler) as **one feature** — encoding the owner rulings: categorizer/assembler PROPOSE + human signs (D9); flywheel v1 persists generated skills, promotion manual (D24); D48 Sonnet-floor guard as code in the assigner; `assignment.md` in the I-12/§8 `## Workforce Gate` format with per-agent skills + elevated grants (D41/D49); traces carry the assembly (D43); assemblies = base + ≤3 skills, additive-only (A-1/D40). Posed as an **open question for the plan/council**: does the skill-builder role declare `web_search` — the system's first elevated grant (D41). Seed library **deferred to plan**. 4 user stories (US1 categorize · US2 assemble+gate · US3 skill-builder/flywheel · US4 implement-consumes+traces), FR-001–021, SC-001–009; quality checklist all-pass. **The `after_specify` hook fired live** — `branch.sh` auto-created **`003-workforce`**, the **first *machine-born* feature branch** (001/002 were hand-cut), the git ext committed `spec(003-workforce)` onto it, `main` stayed clean (D51/FR-001 branch-born-clean held; verified `main` still `abd9402`). `/speckit-clarify` surfaced **one** genuine spec-scoped ambiguity — determinism (FR-015/SC-005) vs the LLM skill builder — resolved (owner) to **assembly-only against a fixed library snapshot**: gap-free runs are byte-reproducible, builder-invoking runs excluded (LLM authorship); encoded as a `## Clarifications` entry + FR-015/SC-005 refinements. `profile.yaml`: `council_tier: standard` (D56). Everything else Clear (contract-grounded) or Deferred-to-plan (seed set, `web_search` grant, `categorization.md` file *format* — a design choice per D46). **Reconciliation flagged:** owner's `analyze → categorize` order vs `artifact-layout.md` §2's current `categorize → analyze` row order. Commits `0f1d917` (spec, machine-born branch) + `9d50d2a` (clarify + profile). **STOP for spec review** before `/speckit-plan` (Phase 2 = plan + the first `standard`-tier live council). |
| 2026-07-10 | **M3 · Phase 1 review — spec APPROVED with adjudications (D58).** Babu ratified the `003-workforce` spec with five rulings (→ **D58**): pipeline order **ratified** (`analyze` before `categorize` — classification follows stabilization, D11) + `artifact-layout.md` §2 **amended → 1.4**; the determinism clarify resolution **ratified + tightened** (the roster now records `library` vs `built` per skill so "gap-free" is checkable — new **FR-022**, SC-005 refined); Phase-0 judgment calls **ratified** (D56 `full`-default / cap-6, D55 triage/approve consistency); smalls accepted (slug `003-workforce`; `categorization.md` file *format* plan-level); four **plan-time verification** items booked (general-cap FR, D48-guard SC, assembly-cap testability, `web_search` costed both ways). Spec + `artifact-layout.md` §2 amended, **D58** recorded — one commit (D46/D51 pattern). **Next: Phase 2 — `/speckit-plan` (incl. the proposed seed set) → first live `standard`-tier `/speckit-council` (D56); STOP after `suggestions.md`, before triage — present it raw + `council_spend` per-stage (deck/stage-1/consolidated-peer/chairman) + per-member `plan.md` read-rate vs the 5.25M `full` baseline.** |
| 2026-07-10 | **M3 · graph-recording decision (→ D59); Phase-2 step 0 defined.** Before Phase 2, the owner directed regenerating the stale (9 Jul, pre-Phase-0) graphify graph for clean baseline grounding. **Surfaced** (documented-convention conflict, before acting): the graph is `.gitignore`'d "never committed" (D45), and the 5.25M `full` baseline (M2) was recorded via **stats**, not a committed graph. Owner chose (informed) to **commit a graph snapshot** → **D59** — a scoped one-time D45 exception: snapshot to `specs/003-workforce/graph-baseline.json` (*outside* gitignored `graphify-out/`); the live graph stays gitignored. **Execution deferred to Phase-2 step 0 (fresh session):** `/graphify . --update` is an incremental **semantic** re-extraction (subagent-using, multi-step) — it fits the fresh session's context (the owner's context-hygiene choice), not this session's tail. **Phase 2 opens with step 0** (regen → snapshot + commit refs D59 + log node/edge stats) **then `/speckit-plan`**. This session closes at `f4dc94e` (003) + this D59 commit; `main` at `abd9402`. |
| 2026-07-10 | **M3 · Phase 2 step 0 — tiered-council baseline graph regenerated + snapshotted (D59), in a fresh session.** Ran `/graphify . --update`: `detect_incremental` found **202 changed files** (31 code `.sh`/`.json` + 171 docs `.md`/`.yml`, ~203k words), 0 deletions — effectively the whole 9 Jul corpus (**all 1013 prior nodes traced to changed files; zero legitimate carryover**, since the D56/D57 reinstalls + contract edits + git-install + D58/D59 doc work touched nearly everything). AST (deterministic, free) = 160 nodes/220 edges over the 31 code files; semantic re-extraction ran **8 general-purpose subagents** over the 171 docs (**~1.19M combined subagent tokens** — the subagent cost D59 put in this fresh session). **Finding — stale-survivor drift (rebuilt clean, not shipped):** the `--update` `build_merge` left **86 stale `document` nodes**, all `extensions/council/…` — files that *were* freshly re-extracted (chunks 03/04) under new IDs, but whose old copies went un-pruned via the source_file path-matching drift the `references/update.md` runbook itself cites (#1361/#1344), i.e. duplicated council nodes. Because D59's whole rationale is a **fresh, non-stale** baseline ("stale receipts masquerade as ground truth"), the graph was **rebuilt cleanly from the fresh full-corpus extraction** (stale over-granular `graph.json` removed, backup kept for the diff; #479 shrink-guard bypassed for a legitimately-more-consolidated graph) rather than committing the contaminated merge → **0 stale survivors** (verified). **Fresh baseline: 497 nodes / 888 edges / 44 communities**, typed **163 code + 135 document + 134 concept + 65 rationale** — smaller than the old **1013** but higher-signal (the 9 Jul build was *flat* `document`/`code` with no concept/rationale typing; concept/rationale nodes are exactly what council members query). God nodes: `/speckit-council`, `extensions.yml` hook registry, member-/chairman-prompt templates, `-approve` human gate, `suggestions.md`, implement-parallel, git-lifecycle README, graphify-context, tasks-graph. Snapshot `cp graphify-out/graph.json` → **`specs/003-workforce/graph-baseline.json`** (committed, D59, 497 nodes); live `graphify-out/` stays gitignored (D45 intact — no diff-noise). This is the exact graph the first `standard`-tier council reviews. **Next: `/speckit-plan`** (proposed seed library + `web_search` question costed both ways, D58-5d) **→ the first live `standard`-tier `/speckit-council` (D56); STOP before triage.** |
| 2026-07-10 | **M3 · Phase 2 — FIRST LIVE `standard`-tier COUNCIL (`003-workforce`, round 1) — D56's first measurement.** `/speckit-plan` authored the workforce-pair plan (seed library **7 bases + 5 skills**; the `web_search` question **costed both ways**, rec. Option A; the **D58-5 verifications as code**; committed `6f5c298`), grounded in the D59 baseline via the `before_plan` graphify-context hook. `/speckit-council` then ran the **8-session** `standard` ceremony: 1 deck-prep + 5 Sonnet stage-1 (**lazy** context, cap 6) + **1 consolidated peer** + 1 Opus chairman — **status-only returns throughout (SC-005 held)**, full grounding. **Verdict: 2 blocking · 13 strong · 10 consider (25).** **`council_spend` = 2,830,593 billable tok** (deck 515,378 + stage-1 1,319,818 + consolidated-peer 373,948 + chairman 621,449; `capture_method: transcript`, **0 unavailable**) — **vs the 5.25M `full` baseline: −46.1%, 8 sessions vs 12.** Per-member **`plan.md` read-rate 4/5** (spec 1/5, graph 3/5) — one member reviewed on deck+graph alone; the lazy lever held without quality loss. Both blockers real: **S01** an unsorted Python `set` (the grant union) serialized non-deterministically → breaks SC-005; **S02** no `workforce` gate-*write* path (git-ext's `record-gate` is council-only) → `/speckit-implement-parallel` unreachable — verified by 3 members reading the *installed* hook scripts. Receipts-checked: the plan's "`extensions.yml` degree 15" claim corrected to **degree 1** (S06). Process nit: the consolidated peer returned detail beyond its status line (not forwarded; → D62 patch). STOP before triage (per instruction). |
| 2026-07-10 | **M3 · Phase 3 — triage + owner rulings + delta-check (`003-workforce`, round 1); gate PENDING.** `/speckit-council-triage`: **all 25 accepted** (0 rejected/deferred), reasoning per D13.5 in `council/decision-record.md`. **Both blockers resolved in one revision** (`5ca7957`): **S01** total-order every set before serialization (+ golden test asserts grant *order*, not membership); **S02+S10+S13 clustered** → the two extensions become **one `workforce` extension** (categorize + agent-assign + a dedicated `/speckit-workforce-approve`), and **git-ext generalizes `on-council-approve.sh` → gate-agnostic `on-gate-approve.sh`** in its own source (D57 S2; `gates.yml` stays git-ext-owned, Q1) firing `after_workforce_approve`. **Chairman-only delta-check: 0 of 2 still blocking — all clear** (one revision + one delta-check, no second round — the SC-004 pattern). **Owner rulings → D60/D61/D62:** **D60** GRANT `web_search` to the skill-builder (the system's **first elevated grant**; skill-declared A-2, roster-visible §8 W2, trace-recorded D43; S17 stale-flag complements); **D61** flip D56's `council_tier` default → **`standard`** (basis: this round's −46%/rigor-held measurement; `full` opt-in for L/architecture features; N=1 revisit clause) — applied to `council-config.yml` + reinstalled; **D62** deck-prep-improvement task (mine member transcripts for pulled `plan.md` sections → enrich the deck-prep template, council source; after-metric = next-run read-rate) + a firmer consolidated-peer status-only clause (member-prompt source, reinstalled — **git-ext seam survived**, D57 S3). **10 trace records** (deck + 5 members + consolidated + 2 chairman + triage; 9 `transcript`, triage `unavailable`/null per D47). **Gate PENDING (human)** — HOLD at `/speckit-council-approve`; `gates.workforce` also human. Round artifacts + decision record + D-rows committed with triage (002 precedent). **Next: `/speckit-council-approve` (human) → `/speckit-tasks-graph`.** |
| 2026-07-11 | **M3 · Phase 3 (cont.) — reconcile → tasks → analyze → categorize → workforce gate PENDING (`003-workforce`).** Council gate **APPROVED** (human, `c8dfda7`) unlocked `/speckit-tasks`. **(1) Companion reconciliation (named triage obligation, `374eb33`):** the four Phase-1 companions (research/data-model/contracts·commands/quickstart), authored pre-triage, still described the drafted **two extensions** — reconciled to the authoritative one-`workforce`-extension plan @`5ca7957`: S10/S13 (one tree, three commands incl. `/speckit-workforce-approve`), S02/R8 (git-ext generalizes `on-council-approve.sh` → gate-agnostic `on-gate-approve.sh`; the old "consume verify-gate, write nothing" design *was* the S02 blocking defect), **D60/R6** (`web_search` GRANTED — Option-A retired), S01/S08/S18 determinism, S21/S04/S07/S14/S25. Residue sweep clean (only self-documenting supersession notes remain). **(2) `/speckit-tasks-graph` (`cb9ec88`):** **32 tasks** + a 10-wave DAG; `[P]` from file-disjointness (all-new code, I-13); the three required clusters landed as explicit tasks — **S02** (T018 generalize → T019 register → T020 reinstall+survival), **D62** (T030 deck-prep), **D60 web_search path** (T003 config · T014/T015 roster-display · T022 builder-declares · T027 trace). **(3) `/speckit-analyze` (`d37a846`):** 100% coverage (22 FR + 9 SC), **0 CRITICAL**, **zero two-extension residue in companions CONFIRMED**; routine remediations applied — I1 (spec.md reading-note + 3 FR-group headers reconciled to the one ext; roles unchanged), I2 (spec.md `web_search`/seed-library OPEN items → `RESOLVED` pointers, D60), P1 (T014/T017 crisped to the principle-I within-file boundary: `assemble.py` writes only the `### Roster approved` sub-table, `workforce-approve` only the decision fields). Flagged not-remediated: `graphify-context.md` is a committed D59 baseline still carrying pre-triage "two extensions" + "degree 15" (S06-corrected) — regenerate, don't rewrite. **(4) Categorize + assign BY HAND (last manual pass — the feature builds its replacement; grandfathered S28):** taxonomy v0 — `general` **0/32** (≤ 6 cap → PASS); dist. `devtools-cli`×12 · `ai-agents`×11 · `qa-automation`×7 · `security`×2; **D48 checked** — the 3 `prompt`-tagged tasks (T010/T022/T030, mechanically `docs`) route to **`agt_ai_agents` (Sonnet)**, never a docs-exempt non-Sonnet base (none exists in the seed set). Hand-assembled roster: **5 of 7 bases** exercised, the two `provisional` bases (`agt_backend_service`/`agt_data_persistence`, S11) **unexercised** (no data-model work — v0→v1 evidence); **gap-free** (zero `built`, SC-005); one reported empty lane (T031 `docs×devtools-cli` → `agt_generic`, FR-016). **The `web_search` grant surfaces on the builder-path rows T022+T025** — **the system's FIRST elevated grant a human signs** (D60/D41): the seed library is 100% `grants:[]`, so `web_search` appears because `003` *introduces* the `web_search`-declaring skill-builder, surfaced at creation. **Workforce gate PENDING (human)** — hand-written + grandfathered (S28); STOP at the gate. **Next: human signs `## Workforce Gate` in `agents/assignment.md` → `/speckit-implement-parallel`.** |
| 2026-07-11 | **M3 · Phase 3 CLOSE — workforce gate APPROVED-WITH-NOTES + grant correction (→ D63) (`003-workforce`).** Owner signed `## Workforce Gate` **`approved-with-notes`** (Naren Karthik B M) in `agents/assignment.md` — **the LAST hand-written workforce gate** (grandfathered S28; once `003` ships, `/speckit-workforce-approve` writes them). **Binding correction (→ D63):** the owner **stripped `web_search` from T022 + T025** → **every row now reads `elevated: none`**. My draft had hand-attached it as a "display convention"; corrected — an A-2 grant derives **only** from a skill declaration *injected into an assembly* (FR-013), and neither builder-path task's *work* reaches the network (T022 writes a prompt file; T025 wires a handoff). **D60 stands untouched** (*capability authorization*): `web_search` attaches to genuine **builder dispatches** via the declaration T022 authors, and the **first human-signed grant lands there** — a real gap event or the **S08 integration test** — through the honest A-2 union, not on a build task. *Capability authorization (D60) ≠ dispatch approval (a gate)* — distinct acts. **Three more binding notes:** (2) `docs × devtools-cli` empty-lane demand (T031) booked for v0→v1 (**I-15**) — `agt_generic` fallback + reporting behaved as designed; (3) `agt_backend_service`/`agt_data_persistence` **stay `provisional`** (unexercised, correctly); (4) **no by-hand discipline this run** — `002`'s per-wave commit + gate-freshness machinery is **live** and governs Phase 4 (its first unassisted outing); misbehavior = a **`002` defect** → stop + flag, never silently compensate. Committed `gate(003-workforce)`. **Next: `/speckit-implement-parallel` through the 10-wave DAG; STOP after the final wave, before the completion report.** |
| 2026-07-12 | **M3 · Phase 4 — implement-parallel through the 10-wave DAG (`003-workforce`); the workforce extension BUILT; STOP before the completion report.** Ran the 32-task / 10-wave DAG via **~30 Sonnet subagent dispatches** (dev-orchestrator model), per-wave **review → `commit.sh impl "wave K/10"` → `[X]`**. **Grandfathered** past the workforce freshness machinery (owner **Option A**, I-16/I-17): `before_implement`/per-wave `verify-gate workforce` is N/A for `003` (the hand-signed gate is the authority — the binding hook is what `003` builds); the live per-wave **COMMIT** machinery (`002`) was **used** throughout (note 4) and worked cleanly (branch self-heal, scoped `[extra-path]` staging, wave grammar — 10 wave-commits). **Two review-caught defects fixed before commit:** (1) wave-3 skill-builder **grant over-propagation** — the builder prompt stamped `grants: [web_search]` on *every generated module*; corrected — `web_search` is the builder **role's** grant (D60/D63), generated modules default `grants: []`; (2) **I-18** `gates.sh` workforce path (flat `assignment.md` → `agents/assignment.md`, fixed in the S02 cluster + the T020 fixture). **S02 workforce gate-write now correct + live end-to-end:** gate-agnostic `on-gate-approve.sh` + `after_workforce_approve` + the I-18 path + `install.sh` manual-fallback + **26/26 reinstall-survival**; git-ext reinstalled into real `.specify/` (council `verify-gate` still fresh, R1-S07 ordering held). **D62** deck-prep enriched (`deck-technical.md`: +Architecture & Data Flow, Project Structure with the "read graph metrics from `graphify-context.md`, not plan prose" S06 fix, a verifications tally; council reinstalled). **T027** trace-assembly wiring lives in **graphify source** (D57/I-14 seam; graphify reinstalled) — implement dispatch traces now carry `agent_id`/`skills[]`/`elevated_grants[]` (FR-021/SC-008). **Determinism proven on the dogfood artifact:** `assemble.py` byte-identical across `PYTHONHASHSEED` on the real `categorization.md` (SC-005); that categorization validates **0/32 general** (SC-001/002). **`test/run.sh` 12/0** (S07 seed+generated survival, assembly golden, 3 validators, SC-008 loop-closure). *Notable:* `assemble.py` flags **14 ∅-match gaps** on `003`'s own tasks (sparse seed library ⇒ the flywheel would build skills) vs. the gap-free hand gate — both legitimate; `003`'s approved hand roster (bases suffice) governs its own implement. One subagent (T028+T029) hit the account session limit; completed inline. **I-18/I-19 booked** (gates.sh path fixed; FR-020↔profile-schema-P4 workforce-auto inconsistency flagged). **SC-009 MET (M3 exit):** `categorize → assign → gate(approved-with-notes) → implement` end-to-end on `003`; **32/32 tasks `[X]`**; the workforce extension built (73 tracked files) + installed + green. Commits `impl(003-workforce)` waves 1–10. **STOP before the completion report (per instruction).** |
| 2026-07-12 | **M3 CLOSED — `speckit-ext-workforce` built, merged, installed, tagged (`003-workforce`).** Close-out in order: **(1)** completion report (`complete(003-workforce)` `a8b8d6a`) folding the two review-caught defects (D63 prompt over-stamping; I-18 path), the 14-gap flywheel signal + both levers, I-19, the T028/T029 session-limit honesty note (T005 / `002`-Finding-5 precedent), carried items + owners. **(2)** `/speckit-git-cleanup` — **the `002` machinery's first FULL lifecycle on a sibling feature**: fast-forward `abd9402..a8b8d6a` into `main` (D52; 120 files, 27,422 ins), annotated **`complete/003-workforce`** cut, branch retired; all 10 wave commits + the 27-commit trail reachable; no `traces.jsonl` (FR-007). **(3)** first live install on `main` (`infra(workforce)` `6987715`, M1/M2 precedent) — 3 commands resolve, `after_categorize`/`after_agent-assign` registered (S06 locked+atomic), seed library seeded additively (S07), `test/run.sh` **12/0** on `main`. **(4)** v0→v1 **evidence dossier** `docs/reviews/taxonomy-v0-evidence.md` — the three-feature trigger (taxonomy §8) **MET** (001/002/003 categorized); every booked item (OQ4/OQ6, endpoint↔service fusion, prompt-asset/D48, `preserves_behavior` **still unexercised** at 3 features, assembly-cap, I-13 + type-not-graph-derivable, `docs×devtools-cli` empty lane, provisional bases, the 14-gap signal+levers, small-feature cap edge, I-19) with evidence pointers, **no positions taken** (input to the review). **(5)** logs: **D64** (meta-feature gate-machinery grandfather, Option A); this row; docs/05 **M3 ✅ CLOSED**. **M3's exit *test* is M4** (the dogfooding rule — M4 builds on the workforce pipeline). Carried to M4: **I-16/I-17** (git-ext workforce-freshness), **I-19** (FR-020↔P4 ruling), the v0→v1 dossier, `HookExecutor`→M6 (D53). **Next milestone: M4 — testing agent + completion-report format.** |
| 2026-07-12 | **Taxonomy v0 → v1 BLESSED — the three-feature review applied (`docs/reviews/2026-07-12-taxonomy-v1-review.md` §2).** Read the blessed memo and applied its mechanical change list in order, substituting **D65/D66/D67** for its `D-next` placeholders (log was at D64), back-referencing from every touched file. **(1) Taxonomy contract:** `git mv docs/contracts/taxonomy-v0.md → taxonomy.md` (header **v1.0 — BLESSED 2026-07-12**); §1 derivability amended (graph-derived-where-covered + a documented path-convention fallback, I-13); new **§2.4 `runtime_consumed` modifier** (promoted from the `prompt` tag / D48, verdict 10); §4 cap → **`max(1, ⌊0.2n⌋)`** (verdict 9); §2 endpoint↔service categorizer note (verdict 3); §7/§8 OQ ledger + carried-item dispositions; §8 **v1→v2 trigger** (first non-SpecSeyal repo, or M5 close). Live references swept to `taxonomy.md`; frozen history (the 2026-07-09 memo, the evidence dossier, completed-spec artifacts, prior D-rows, the installed `.specify/` copy) preserved as written. **(2) Workforce ext code + tests:** categorizer detects `runtime_consumed` (path/consumption + `prompt`-tag fallback) as a 6th categorization column + applies the floor'd cap (`validate-categorization.py`); the assembler enforces the Sonnet floor on the *modifier*, retiring the tag-convention check (`assemble.py` — the re-homed D48 guard); **gap-batching** (`compute_gap_clusters` → `GAP_CLUSTERS` + roster notes, **D66**) and the **grant tripwire** (`GRANT_TRIPWIRE` + roster notice, **D67**) as deterministic assembler output, consumed by the two gate skills; fixtures updated to 6 columns + 5 new fixtures, golden regenerated. `test_categorize.sh` **24/0**, `test_assemble.sh` **40/0**, **`test/run.sh` 12/0**. **(3) Base config:** `agt_devtools_cli` v2.0.0 accepts `docs` (verdict 7, resolves **I-15**). **(4) `003` FR-020 amended** to the standalone-P4 + grant-tripwire ruling (D67 pointer; resolves **I-19**). **(5) Logs:** **D65** (verdicts 1–10), **D66** (gap-batching economics), **D67** (auto-mode + tripwire); this row; evidence dossier marked **CONSUMED**; I-15/I-19 resolved. **(6) Carried, out of taxonomy scope:** I-16/I-17 (git-ext workforce-freshness → git-ext follow-up), I-13 (graphify `.sh`/`.yml`/`.md` coverage → graphify backlog). **STOPPED before M4** (memo step 7 + the session instruction) — presented the diff summary, did not begin M4. |
| 2026-07-12 | **Taxonomy v1 LANDED on `main` — two commits + reinstall (owner-authorized).** After the diff summary the owner authorized the landing. **(1) `d7da3a2`** `contracts: taxonomy v1 blessed — review 2026-07-12 applied (D65-D67)` — the full §2 source diff (44 files) incl. the `taxonomy-v0.md → taxonomy.md` rename, the review memo, 5 new fixtures; a second sweep caught the *spaced* `taxonomy v0` refs (extension.yml / README / install.sh / validator messages / categorize skill+command) the hyphenated grep had missed. **(2) `0924986`** `infra(workforce): reinstall under taxonomy v1 (D65)` — `bash extensions/workforce/install.sh .` refreshed `.specify/extensions/workforce/` + the 3 command skills to v1 (installed == source, byte-identical); the additive seed step (S07) correctly preserved the installed `agt_devtools_cli` as user data, so the **v2.0.0 base bump was applied as a deliberate curated-static edit** (D44/Flag-3, D65 verdict 7); the installer flock target gitignored. **No categorization ran between the two commits** (one landing). **Verified green against the INSTALLED scripts:** cap-floor pass/fail via the installed validator; installed assembler output == frozen golden; `test/run.sh` 12/0. **(3)** docs/05 notes **taxonomy v1 in force** (M3→M4 boundary + the M0 taxonomy row); this row. **STOP — M4 kicks off in a fresh session.** |

## How we work

1. New raw idea → add an **I** row here, one line, don't over-specify.
2. Idea gets discussed → gains options/lean → becomes a **Q**.
3. Q gets answered → becomes a **D**, and the relevant spec doc gets updated in the same session.
4. When a component accumulates 3+ related D/I items → it earns its own numbered spec doc.
