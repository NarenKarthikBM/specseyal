# Contract — Feature Artifact Layout

> **Status:** 1.0 (M0). Normative.
> **Implements:** principle 1 (artifacts are the contract), principle 3 (resumability).
> **Sources:** docs/00 §3, docs/10 §3, D4, D25, D32, D37, D38.

Every feature is one directory under `specs/`. Every pipeline phase reads artifacts and writes **exactly one** artifact. Both the CLI layer and the platform layer (M5+) read and write this same tree — it is the only interface between them.

---

## 1. Directory

```
specs/NNN-feature-slug/
├── profile.yaml              ← autonomy profile          [profile-schema.md]
├── spec.md                   ← /speckit-specify
├── graphify-context.md       ← graphify (regenerated by before_* hooks; disposable)
├── plan.md                   ← /speckit-plan, revised in place across council rounds
├── research.md               ← optional, stock spec-kit
├── data-model.md             ← optional, stock spec-kit
├── contracts/                ← optional, stock spec-kit API contracts  (see §5)
├── checklists/               ← optional, stock spec-kit
│
├── council/                                              [decision-record.md]
│   ├── defense-deck/
│   │   ├── technical.md      ← Session A, for council members
│   │   └── overview.md       ← Session A, non-technical; rendered at the human gate
│   ├── round-1/
│   │   ├── opinions/
│   │   │   ├── A.md  B.md  C.md         ← stage 1, independent, anonymized
│   │   │   └── peer/ A.md  B.md  C.md   ← stage 2, anonymized peer review
│   │   └── suggestions.md    ← stage 3, chairman. The ONLY file the main thread reads.
│   └── decision-record.md    ← append-per-round; permanent audit trail
│
├── tasks.md                  ← /speckit-tasks-graph (stock format + `## Execution Waves`)
├── categorization.md         ← speckit-ext-categorize    [taxonomy-v0.md]  (M3)
├── agents/
│   └── assignment.md         ← speckit-ext-agents; roster + workforce-gate decision (M3)
│
├── implement.log.md          ← /speckit-implement-parallel, one line per wave
├── completion-report.md      ← M4
├── testing.md                ← M4 testing agent (doc-only v1)
└── traces.jsonl              ← append-only observability   [trace-schema.md]
```

`NNN` is a zero-padded three-digit ordinal; `feature-slug` is kebab-case. The pair is the **spec ID**, and it is what the git extension derives its branch name from (D25). `specs/000-sample/` is reserved as the conformance fixture.

## 2. Phase table

Each row is a phase. **Context in** is exhaustive — a phase that runs in a separate session receives nothing else (docs/00 §3, session boundary rule).

| Phase | Session | Context in | Artifact out |
|---|---|---|---|
| specify | main | user prompt | `spec.md` |
| clarify | main | `spec.md` | `spec.md` (revised) |
| branch | main | `spec.md` | *(git ref, not a file)* |
| graph-context | hook | repo graph | `graphify-context.md` |
| plan | main | `spec.md`, `graphify-context.md` | `plan.md` |
| deck-prep | separate (A) | `plan.md`, `spec.md`, graph summary | `council/defense-deck/` |
| council | separate (B) | `defense-deck/`, `plan.md`, `spec.md`, graphify query tool | `council/round-N/suggestions.md` |
| triage | main | `suggestions.md` **only** | `plan.md` (revised) + `decision-record.md` entry |
| council-gate | human | `overview.md`, `suggestions.md`, `decision-record.md` | `decision-record.md` gate section |
| tasks | main | `plan.md`, `spec.md`, `graphify-context.md`, `decision-record.md` | `tasks.md` |
| categorize | separate | `tasks.md`, `plan.md` | `categorization.md` |
| analyze | main | `spec.md`, `plan.md`, `tasks.md` | `tasks.md` patch **or** reopened `plan.md` (D11) |
| agent-assign | separate | `categorization.md`, `.claude/agents/` | `agents/assignment.md` |
| workforce-gate | human | `tasks.md`, `agents/assignment.md` | `assignment.md` gate section |
| implement | main + subagents | `tasks.md`, `plan.md`, `graphify-context.md`, `assignment.md` | code + `implement.log.md` |
| complete | main | `tasks.md`, `implement.log.md` | `completion-report.md` |
| testing | separate | `completion-report.md`, `spec.md` | `testing.md` |

Every session, in every phase, appends one record to `traces.jsonl` (principle 4). That append is not "the artifact out" — it is unconditional.

Three rows run **no session** and so leave no trace: `branch` (a git ref), `council-gate` and `workforce-gate` (a human). The gates still write artifacts — their decision sections — which is what keeps §3 working.

## 3. Resumability rule (D32)

**Phase state is inferred from artifacts. There is no state file, and adding one is forbidden.**

A phase is *complete* iff its artifact-out exists **and** validates against its contract. A phase is *pending* otherwise. The orchestrator resumes by walking §2 top-to-bottom and running the first pending phase. This is principle 3 made mechanical: kill the process at any point and the tree alone says where to restart.

Two consequences the design must honor:

1. **Gate decisions are artifacts, not events.** A human approval is a section appended to `decision-record.md` (council) or `agents/assignment.md` (workforce). No decision lives only in a transcript, a database, or an operator's memory.
2. **Validity, not mere presence.** An empty `plan.md` does not complete the plan phase. Each contract in this directory states its required sections; that statement is what "validates" means.

`graphify-context.md` is the one exception: it is disposable and may be regenerated at any time without invalidating downstream phases.

The `branch` phase has no artifact-out. Its completion check is the git ref itself — observable, idempotent, and derivable from the spec ID (D25). It is the only phase whose state lives outside `specs/`, and git is a source of truth we already trust.

## 4. Council subtree (docs/10 §3, D38)

- **Round-scoped** artifacts live under `round-N/`. Rounds are never overwritten.
- **The deck is not round-scoped.** `defense-deck/` is overwritten in place when a revision round regenerates it. Its version store is git: the feature branch carries a phase-tagged commit per revision (D25). Copying the deck per round would duplicate what git already holds.
- `opinions/` holds stage 1 (`A.md`) and stage 2 (`peer/A.md`) alike, per docs/10 §3. Members are anonymized to single letters at write time — the mapping from letter to member is never persisted.
- **`opinions/` is chairman-only.** The main thread must never read it. `suggestions.md` is the compression boundary (docs/10 §6). Any tool that pulls `opinions/` into the main thread breaks principle 1.

## 5. Two directories named `contracts/`

| Path | Holds | Audience |
|---|---|---|
| `docs/contracts/` | SpecSeyal's own schemas (this file, and its six siblings) | The pipeline itself |
| `specs/NNN-feature/contracts/` | That feature's API contracts (OpenAPI, proto, …) | Stock spec-kit |

The collision is inherited from spec-kit. It is documented rather than renamed (D39): renaming either one breaks a convention someone else owns. Always qualify the path when referring to one.

## 6. Ownership

| Writer | May write |
|---|---|
| Stock spec-kit commands | `spec.md`, `plan.md`, `tasks.md`, `research.md`, `data-model.md`, `contracts/`, `checklists/` |
| graphify extension | `graphify-context.md`, `## Execution Waves` in `tasks.md` |
| Council extension | everything under `council/` |
| Categorize extension | `categorization.md` — and **nothing else** (D37) |
| Agents extension | `agents/assignment.md` |
| Implement-parallel | `implement.log.md`, source code |
| Any session | one append to `traces.jsonl` |
| Human | `profile.yaml`, gate sections |

No writer touches another's artifact. `tasks.md` has two writers (spec-kit's task generator and graphify's wave appender) only because graphify appends a disjoint section; that is the sole shared file, and it is why categorization was given its own artifact rather than another `tasks.md` section (D37).

## 7. Validation

A feature directory conforms iff:

1. `NNN-feature-slug` matches `^[0-9]{3}-[a-z0-9]+(-[a-z0-9]+)*$`.
2. Every artifact present validates against its contract (§3.2).
3. No artifact exists whose upstream phase (§2) is incomplete — e.g. `tasks.md` without an approved `decision-record.md` gate section is a contract violation unless `profile.yaml` sets `gates.council.mode: auto`.
4. `traces.jsonl` has at least one record per completed phase that runs a session (i.e. all but `branch`, `council-gate`, `workforce-gate`).
5. No file **inside the feature directory** but outside `council/` mentions the `opinions/` path.

Rule 5 is deliberately blunt — a `grep` for the path, not a judgment about whether a reference is a real read. Context hygiene (§4) fails silently and late; a mechanical invariant that occasionally rejects innocent prose is worth more than a nuanced one nobody can run in CI. Docs outside `specs/` (this contract, for instance) are out of scope.

`specs/000-sample/` is the executable statement of this contract: it exercises every artifact above, and any conformance checker built later must pass it.
